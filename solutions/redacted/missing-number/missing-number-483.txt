class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int sum2 = 0;
        for (int i = 0; i < nums.length; i++) {
            sum = sum + nums[i];
            sum2 = sum2 + i;
        }
        sum2 = sum2 + nums.length;
        return sum2 - sum;
    }
}
