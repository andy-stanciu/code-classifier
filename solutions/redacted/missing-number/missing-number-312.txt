class Solution {

    public int method1(int[] nums) {
        int N = nums.length;
        int totalSum = N * (N + 1) / 2;
        int arraySum = 0;
        for (int a : nums) {
            arraySum += a;
        }
        return totalSum - arraySum;
    }
}
