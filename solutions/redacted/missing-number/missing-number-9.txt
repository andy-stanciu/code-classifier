class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        //  if(n<=2)
        //    return n;
        // int result=0;
        // for(int  i=0 ; i<n ;i++)
        // {
        //     if(nums[i] == i)
        //       continue;
        //       else
        //          result=i;
        // }
        //     return result;
        int sum = 0;
        int sum1 = 0;
        int ans = 0;
        sum1 = (n * (n + 1)) / 2;
        for (int i = 0; i < n; i++) {
            sum += nums[i];
            ans = sum1 - sum;
        }
        return ans;
    }
}
