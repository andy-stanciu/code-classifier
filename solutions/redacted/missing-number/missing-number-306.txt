class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int total = 0;
        int len = nums.length;
        for (int i = 0; i < len; i++) {
            sum += nums[i];
            total += i;
        }
        total += len;
        return total - sum;
    }
}
