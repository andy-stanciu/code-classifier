class Solution {

    public int method1(int[] nums) {
        int txor = 0;
        for (int num : nums) txor ^= num;
        for (int n = 0; n <= nums.length; ++n) txor ^= n;
        return txor;
    }
}
