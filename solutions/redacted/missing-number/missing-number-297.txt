class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int tsum = ((nums.length + 1) * nums.length) / 2;
        for (int i = 0; i < nums.length; i++) {
            sum += nums[i];
        }
        return tsum - sum;
    }
}
