class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int ttsum = (n * (n + 1)) / 2, c = 0;
        for (int i : nums) {
            c += i;
        }
        return ttsum - c;
    }
}
