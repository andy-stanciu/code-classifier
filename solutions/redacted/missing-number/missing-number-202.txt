class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        for (int i = 0; i < nums.length; i++) {
            sum = sum + nums[i];
        }
        int n = nums.length;
        int sum1 = n * (n + 1) / 2;
        int hoi = sum1 - sum;
        return hoi;
    }
}
