class Solution {

    public int method1(int[] nums) {
        int add = 0;
        int l = nums.length;
        int ta = (l * (l + 1)) / 2;
        for (int i = 0; i < nums.length; i++) add += nums[i];
        return ta - add;
    }
}
