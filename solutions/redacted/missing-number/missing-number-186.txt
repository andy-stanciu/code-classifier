class Solution {

    public int method1(int[] nums) {
        int xor = 0;
        int n = nums.length;
        for (int i = 0; i < n; i++) {
            xor ^= nums[i];
            xor ^= i;
        }
        // XOR with n as n is missing
        xor ^= n;
        return xor;
    }
}
