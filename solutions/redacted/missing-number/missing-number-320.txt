class Solution {

    public int method1(int[] nums) {
        int total = nums.length * (nums.length + 1) / 2;
        for (int value : nums) {
            total -= value;
        }
        return total;
    }
}
