class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int sum = n * (n + 1) / 2;
        int check = 0;
        for (int i = 0; i < n; i++) {
            check += nums[i];
        }
        return sum - check;
    }
}
