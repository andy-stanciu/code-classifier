class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int n = nums.length;
        int k = n * (n + 1) / 2;
        for (int i = 0; i < n; i++) {
            sum = sum + nums[i];
        }
        return k - sum;
    }
}
