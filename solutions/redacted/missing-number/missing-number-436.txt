class Solution {

    public int method1(int[] a) {
        int n = a.length;
        int sumOfAll = (n * (n + 1)) / 2;
        int sum = 0;
        for (int nums : a) {
            sum += nums;
        }
        return sumOfAll - sum;
    }
}
