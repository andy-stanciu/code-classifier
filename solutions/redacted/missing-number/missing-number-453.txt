class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int count = (n * n + n) / 2;
        for (int i = 0; i < n; i++) {
            count -= nums[i];
        }
        return count;
    }
}
