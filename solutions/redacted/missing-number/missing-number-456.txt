class Solution {

    public int method1(int[] nums) {
        int actual = 0;
        int n = nums.length;
        int sum = (n * (n + 1)) / 2;
        for (int i = 0; i < nums.length; i++) {
            actual += actual[i];
        }
        return sum - actual;
    }
}
