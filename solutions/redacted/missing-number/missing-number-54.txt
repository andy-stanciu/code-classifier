class Solution {

    public int method1(int[] nums) {
        boolean[] flag = new boolean[nums.length + 1];
        for (int i : nums) flag[i] = true;
        for (int i = 0; i < flag.length; i++) if (!flag[i])
            return i;
        return -1;
    }
}
