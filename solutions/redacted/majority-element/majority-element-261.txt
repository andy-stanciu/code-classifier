class Solution {

    public int method1(int[] nums) {
        // Moore\'s Voting algorithm--> Using constant space O(1)
        int count = 1, counter = 0;
        int ansIndex = 0;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] == nums[ansIndex]) {
                count++;
            } else {
                count--;
            }
            if (count == 0) {
                ansIndex = i;
                count = 1;
            }
        }
        //Since it is guaranteed that majority element is there, we need not check the presence of it in the array.
        return nums[ansIndex];
    }
}
