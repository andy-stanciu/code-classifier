class Solution {

    public int method1(int[] nums) {
        int count = 0;
        Integer c = 0;
        for (int num : nums) {
            if (count == 0) {
                c = num;
            }
            count += (c == num) ? 1 : -1;
        }
        return c;
    }
}
