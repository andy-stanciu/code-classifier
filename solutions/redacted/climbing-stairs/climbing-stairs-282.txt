class Solution {

    public int method1(int n) {
        // path can be reached
        if (n == 0)
            return 1;
        // path cannot be reached
        if (n < 0)
            return 0;
        return method1(n - 1) + method1(n - 2);
    }
}
