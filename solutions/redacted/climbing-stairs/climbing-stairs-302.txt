class Solution {

    private Map<Integer, Integer> map = new HashMap<>();

    public int method1(int n) {
        if (map.containsKey(n))
            return map.get(n);
        if (n == 1)
            return 1;
        if (n == 2)
            return 2;
        int count = method1(n - 1) + method1(n - 2);
        map.put(n, count);
        return count;
    }
}
