class Solution {

    public int method1(int n) {
        // int[] dp = new int[n+1];
        int pre2 = 1;
        int prev = 1;
        for (int i = 2; i <= n; i++) {
            int curr = prev + pre2;
            pre2 = prev;
            prev = curr;
        }
        return prev;
    }
}
