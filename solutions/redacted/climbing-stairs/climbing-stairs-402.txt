class Solution {

    public int method1(int n) {
        int[] memo = new int[n + 1];
        return method2(n, memo);
    }

    private int method2(int n, int[] memo) {
        if (n == 0 || n == 1) {
            return 1;
        } else if (n == 2) {
            return 2;
        }
        if (memo[n] == 0) {
            memo[n] = method2(n - 1, memo) + method2(n - 2, memo);
        }
        return memo[n];
    }
}
