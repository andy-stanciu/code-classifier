class Solution {

    public int method1(int n) {
        // Case - A (Base Condition)
        if (n <= 1)
            return 1;
        // Case - C (Recurring Condition)
        return (method1(n - 1) + method1(n - 2));
    }
}
