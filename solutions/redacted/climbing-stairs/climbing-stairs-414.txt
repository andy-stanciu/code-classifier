class Solution {

    public int method1(int n) {
        int prev = 0;
        int current = 1;
        int next = 1;
        for (int i = 0; i < n; i++) {
            next = prev + current;
            prev = current;
            current = next;
        }
        return next;
    }
}
