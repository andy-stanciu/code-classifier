class Solution {

    private int count = 1;

    public int method1(TreeNode root, int k) {
        if (root == null) {
            return -1;
        }
        int res = method1(root.left, k);
        if (res != -1) {
            return res;
        } else if (count++ == k) {
            return root.val;
        }
        return method1(root.right, k);
    }
}
