/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    // To keep track of remaining k
    private int count;

    // To store the k-th smallest value
    private int result = 0;

    public int method1(TreeNode root, int k) {
        count = k;
        method2(root);
        return result;
    }

    private void method2(TreeNode node) {
        if (node == null)
            return;
        // Traverse left subtree
        method2(node.left);
        // reduce when come to current node
        count--;
        if (count == 0) {
            result = node.val;
            return;
        }
        // Traverse right subtree
        method2(node.right);
    }
}
