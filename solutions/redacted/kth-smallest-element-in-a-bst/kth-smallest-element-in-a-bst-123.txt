/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int method1(TreeNode root, int K) {
        if (root.left == null && root.right == null)
            return root.val;
        int[] ans = new int[1];
        method2(root, K, ans);
        return ans[0];
    }

    int var1 = 0;

    public boolean method2(TreeNode root, int k, int[] ans) {
        if (root == null)
            return false;
        if (method2(root.left, k, ans))
            return true;
        var1++;
        if (k - var1 == 0) {
            ans[0] = root.val;
            return true;
        }
        if (method2(root.right, k, ans))
            return true;
        return false;
    }
}
