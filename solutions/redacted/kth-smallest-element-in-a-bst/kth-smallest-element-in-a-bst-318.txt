/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    private int count = 0;

    private int result = Integer.MIN_VALUE;

    public int method1(TreeNode root, int k) {
        method2(root, k);
        return result;
    }

    private void method2(TreeNode root, int k) {
        if (root == null)
            return;
        // Traverse the left subtree
        method2(root.left, k);
        count++;
        if (count == k) {
            result = root.val;
            // Found the k-th smallest, no need to continue
            return;
        }
        //Now hanle the right substree
        method2(root.right, k);
    }
}
