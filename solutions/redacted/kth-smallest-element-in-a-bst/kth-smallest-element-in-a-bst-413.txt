/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    private int count;

    private int result;

    public int method1(TreeNode root, int k) {
        count = 0;
        result = 0;
        method2(root, k);
        return result;
    }

    private void method2(TreeNode node, int k) {
        if (node == null)
            return;
        method2(node.left, k);
        count++;
        if (count == k) {
            result = node.val;
            return;
        }
        method2(node.right, k);
    }
}
