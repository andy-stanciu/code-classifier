class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> stack = new Stack<>();
        for (int var1 : param1) {
            boolean isDestroyed = false;
            // Handle collision when current asteroid is moving left
            while (!stack.isEmpty() && var1 < 0 && stack.peek() > 0) {
                if (stack.peek() < -var1) {
                    // Top asteroid is destroyed
                    stack.pop();
                } else if (stack.peek() == -var1) {
                    // Both asteroids are destroyed
                    stack.pop();
                    isDestroyed = true;
                    break;
                } else {
                    // Current asteroid is destroyed
                    isDestroyed = true;
                    break;
                }
            }
            // Push the current asteroid to the stack if it\'s not destroyed
            if (!isDestroyed) {
                stack.push(var1);
            }
        }
        // Convert stack to array
        int[] result = new int[stack.size()];
        for (int i = stack.size() - 1; i >= 0; i--) {
            result[i] = stack.pop();
        }
        return result;
    }
}
