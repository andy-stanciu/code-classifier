class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> stack = new Stack<>();
        for (int i = 0; i < param1.length; i++) {
            if (stack.isEmpty() || param1[i] > 0) {
                stack.push(param1[i]);
            } else {
                while (!stack.isEmpty()) {
                    int top = stack.peek();
                    if (top < 0) {
                        stack.push(param1[i]);
                        break;
                    }
                    if (top + param1[i] == 0) {
                        stack.pop();
                        break;
                    }
                    if (top + param1[i] > 0) {
                        break;
                    }
                    stack.pop();
                    if (stack.isEmpty()) {
                        stack.push(param1[i]);
                        break;
                    }
                }
            }
        }
        int[] newArr = new int[stack.size()];
        int count = stack.size() - 1;
        while (!stack.isEmpty()) {
            newArr[count--] = stack.pop();
        }
        return newArr;
    }
}
