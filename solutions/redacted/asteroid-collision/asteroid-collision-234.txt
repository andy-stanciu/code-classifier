class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> s = new Stack<>();
        for (int i = 0; i < param1.length; i++) {
            if (s.isEmpty() || param1[i] > 0) {
                s.push(param1[i]);
            } else {
                while (!s.isEmpty() && s.peek() > 0 && s.peek() < Math.abs(param1[i])) {
                    s.pop();
                }
                if (s.isEmpty() || s.peek() < 0) {
                    s.push(param1[i]);
                } else if (s.peek() == Math.abs(param1[i])) {
                    s.pop();
                }
            }
        }
        int n = s.size();
        int[] arr = new int[n];
        for (int i = n - 1; i >= 0; i--) {
            arr[i] = s.pop();
        }
        return arr;
    }
}
