class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> stack = new Stack<>();
        for (int i = 0; i < param1.length; i++) {
            if (stack.isEmpty() || param1[i] > 0) {
                stack.push(param1[i]);
            } else {
                while (true) {
                    int top = stack.peek();
                    if (top < 0 && param1[i] < 0 || (top > 0 && param1[i] > 0)) {
                        stack.push(param1[i]);
                        break;
                    } else if (top == Math.abs(param1[i])) {
                        stack.pop();
                        break;
                    } else if (top > Math.abs(param1[i])) {
                        break;
                    } else {
                        stack.pop();
                        if (stack.isEmpty()) {
                            stack.push(param1[i]);
                            break;
                        }
                    }
                }
            }
        }
        int[] ans = new int[stack.size()];
        for (int i = 0; i < ans.length; i++) {
            ans[i] = stack.get(i);
        }
        return ans;
    }
}
