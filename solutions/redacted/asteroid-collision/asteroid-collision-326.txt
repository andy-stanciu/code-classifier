class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> stack = new Stack<>();
        for (int i : param1) {
            if (stack.isEmpty() || i > 0) {
                stack.push(i);
            } else {
                while (!stack.isEmpty()) {
                    int top = stack.peek();
                    if (top < 0) {
                        stack.push(i);
                        break;
                    }
                    int absolutevalue = Math.abs(i);
                    if (absolutevalue == top) {
                        stack.pop();
                        break;
                    } else if (absolutevalue < top) {
                        break;
                    } else {
                        stack.pop();
                        if (stack.isEmpty()) {
                            stack.push(i);
                            break;
                        }
                    }
                }
            }
        }
        int[] arr = new int[stack.size()];
        for (int i = arr.length - 1; i >= 0; i--) {
            arr[i] = stack.pop();
        }
        return arr;
    }
}
