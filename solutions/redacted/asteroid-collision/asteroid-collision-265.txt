final class Solution {

    public int[] method1(final int[] param1) {
        final Deque<Integer> stack = new ArrayDeque<>(param1.length);
        for (int a : param1) {
            while (!stack.isEmpty() && a < 0 && stack.peek() > 0) {
                if (stack.peek() + a < 0)
                    stack.pop();
                else if (stack.peek() + a > 0)
                    a = 0;
                else {
                    stack.pop();
                    a = 0;
                }
            }
            if (a != 0)
                stack.push(a);
        }
        final int[] result = new int[stack.size()];
        for (int i = result.length - 1; i >= 0; i--) result[i] = stack.pop();
        return result;
    }
}
