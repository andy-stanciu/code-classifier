class Solution {

    public boolean canConstruct(String param1, String magazine) {
        for (int i = 0; i < param1.length(); i++) {
            char r = param1.charAt(i);
            int m = magazine.indexOf(r);
            if (m == -1) {
                return false;
            }
            magazine = magazine.substring(0, m) + magazine.substring(m + 1);
        }
        return true;
    }
}
