class Solution {

    public boolean canConstruct(String param1, String magazine) {
        int[] chars = new int[26];
        int OFFSET = 97;
        for (int i = 0; i < magazine.length(); i++) {
            int c = magazine.charAt(i) - OFFSET;
            chars[c]++;
        }
        for (int i = 0; i < param1.length(); i++) {
            int c = param1.charAt(i) - OFFSET;
            chars[c]--;
            if (chars[c] < 0)
                return false;
        }
        return true;
    }
}
