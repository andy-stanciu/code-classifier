class Solution {

    public boolean canConstruct(String param1, String magazine) {
        HashMap<Character, Integer> map = new HashMap();
        if (magazine.length() < param1.length()) {
            return false;
        }
        for (int i = 0; i < magazine.length(); i++) {
            if (map.containsKey(magazine.charAt(i))) {
                map.replace(magazine.charAt(i), map.get(magazine.charAt(i)), map.get(magazine.charAt(i)) + 1);
            } else {
                map.put(magazine.charAt(i), 1);
            }
        }
        for (int i = 0; i < param1.length(); i++) {
            if (!map.containsKey(param1.charAt(i))) {
                return false;
            } else {
                map.replace(param1.charAt(i), map.get(param1.charAt(i)), map.get(param1.charAt(i)) - 1);
                if (map.get(param1.charAt(i)) == -1) {
                    return false;
                }
            }
        }
        return true;
    }
}
