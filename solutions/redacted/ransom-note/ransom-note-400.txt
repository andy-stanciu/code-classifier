class Solution {

    public boolean canConstruct(String param1, String magazine) {
        int[] arr = new int[256];
        for (int i = 0; i < param1.length(); i++) {
            arr[(int) param1.charAt(i)]++;
        }
        for (int i = 0; i < magazine.length(); i++) {
            if (arr[(int) magazine.charAt(i)] > 0)
                arr[(int) magazine.charAt(i)]--;
        }
        for (int i = 0; i < 256; i++) {
            if (arr[i] != 0)
                return false;
        }
        return true;
    }
}
