class Solution {

    public boolean canConstruct(String param1, String magazine) {
        HashMap<Character, Integer> map1 = new HashMap<>();
        HashMap<Character, Integer> map2 = new HashMap<>();
        for (int i = 0; i < magazine.length(); i++) {
            map1.put(magazine.charAt(i), map1.getOrDefault(magazine.charAt(i), 0) + 1);
        }
        for (int i = 0; i < param1.length(); i++) {
            map2.put(param1.charAt(i), map2.getOrDefault(param1.charAt(i), 0) + 1);
        }
        boolean check = true;
        for (int i = 0; i < param1.length(); i++) {
            if (map1.getOrDefault(param1.charAt(i), 0) < map2.getOrDefault(param1.charAt(i), 0))
                return false;
        }
        return check;
    }
}
