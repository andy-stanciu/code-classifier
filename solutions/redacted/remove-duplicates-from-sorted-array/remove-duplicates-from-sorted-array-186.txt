class Solution {

    public int method1(int[] nums) {
        int i = 0, j;
        for (//iterates the array
        //iterates the array
        //iterates the array
        j = 0; j < nums.length; j++) {
            if (// to find the next unique element for replacing it with the duplicate element.
            nums[i] != nums[j]) {
                i++;
                nums[i] = nums[j];
            }
        }
        return i + 1;
    }
}
