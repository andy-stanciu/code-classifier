class Solution {

    public int method1(int[] nums) {
        Set<Integer> uniqueElement = new HashSet<>();
        int index = 0;
        for (int num : nums) {
            if (uniqueElement.add(num))
                nums[index++] = num;
        }
        return index;
    }
}
