class Solution {

    public int method1(int[] nums) {
        int uniq = 1;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] != nums[uniq - 1]) {
                nums[uniq] = nums[i];
                uniq++;
            }
        }
        return uniq;
    }
}
