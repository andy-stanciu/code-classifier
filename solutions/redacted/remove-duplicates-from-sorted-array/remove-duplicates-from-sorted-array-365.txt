class Solution {

    public int method1(int[] nums) {
        int realPosition = 1;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] != nums[i - 1]) {
                nums[realPosition] = nums[i];
                realPosition++;
            }
        }
        return realPosition;
    }
}
