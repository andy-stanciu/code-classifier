class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int total = 0, remaining = 0, index = 0;
        for (int i = 0; i < param1.length; i++) {
            total += (param1[i] - cost[i]);
            remaining += (param1[i] - cost[i]);
            if (remaining < 0) {
                remaining = 0;
                index = i + 1;
            }
        }
        if (total < 0)
            return -1;
        return index;
    }
}
