class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int totalSum = 0;
        int currentSum = 0;
        int start = 0;
        for (int i = 0; i < param1.length; i++) {
            totalSum += param1[i] - cost[i];
            currentSum += param1[i] - cost[i];
            if (currentSum < 0) {
                start = i + 1;
                currentSum = 0;
            }
        }
        return totalSum < 0 ? -1 : start;
    }
}
