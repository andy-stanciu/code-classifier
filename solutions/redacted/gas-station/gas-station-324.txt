class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int n = param1.length;
        int t = 0;
        for (int i = 0; i < n; ++i) t += param1[i] - cost[i];
        if (t < 0)
            return -1;
        t = 0;
        int ans = 0;
        for (int i = 0; i < n; ++i) {
            t += param1[i] - cost[i];
            if (t < 0) {
                ans = i + 1;
                t = 0;
            }
        }
        return ans;
    }
}
