class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int fuel = 0, sum = 0, start = 0;
        for (int i = 0; i < param1.length; i++) {
            sum += (param1[i] - cost[i]);
            fuel += (param1[i] - cost[i]);
            if (fuel < 0) {
                fuel = 0;
                start = i + 1;
            }
        }
        if (sum >= 0) {
            return start % (param1.length);
        }
        return -1;
    }
}
