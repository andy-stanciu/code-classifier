class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int def = 0;
        int bal = 0;
        int start = 0;
        for (int i = 0; i < param1.length; i++) {
            bal = bal + param1[i] - cost[i];
            if (bal < 0) {
                def += Math.abs(bal);
                start = i + 1;
                bal = 0;
            }
        }
        if (bal - def >= 0)
            return start;
        else
            return -1;
    }
}
