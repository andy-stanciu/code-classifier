class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int n = param1.length;
        int var1 = 0;
        int totalCost = 0;
        for (int a : param1) {
            var1 += a;
        }
        for (int b : cost) {
            totalCost += b;
        }
        //IF THE GAS WE HAVE IS LESS THAN WE NEED TO SPEND THEN WE CAN NOT TRAEL
        if (var1 < totalCost)
            return -1;
        int index = 0;
        int remainingFuel = 0;
        for (int i = 0; i < n; i++) {
            remainingFuel += param1[i] - cost[i];
            //IF remainig fuel gets less than 0 then we will make remainig fuel as 0 and increase index by i + 1
            if (remainingFuel < 0) {
                index = i + 1;
                remainingFuel = 0;
            }
        }
        return index;
    }
}
