class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int sum = 0;
        int minPos = 0;
        int minSum = Integer.MAX_VALUE;
        for (int i = 0; i < cost.length; i++) {
            sum -= cost[i];
            if (sum <= minSum) {
                minSum = sum;
                minPos = i;
            }
            sum += param1[(i + 1) % param1.length];
        }
        return sum >= 0 ? (minPos + 1) % param1.length : -1;
    }
}
