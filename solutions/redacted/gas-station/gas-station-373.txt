class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int tg = 0, tank = 0, gs_st = 0;
        for (int i = 0; i < param1.length; i++) {
            tg = tg + (param1[i] - cost[i]);
            tank = tank + (param1[i] - cost[i]);
            if (tank < 0) {
                tank = 0;
                gs_st = i + 1;
            }
        }
        return tg < 0 ? -1 : gs_st;
    }
}
