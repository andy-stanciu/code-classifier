class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int remaining_fuel = 0;
        int ans = 0;
        for (int i : param1) remaining_fuel += i;
        for (int j : cost) remaining_fuel -= j;
        if (remaining_fuel < 0)
            return -1;
        remaining_fuel = 0;
        for (int i = 0; i < param1.length; i++) {
            remaining_fuel = param1[i] - cost[i] + remaining_fuel;
            if (remaining_fuel < 0) {
                ans = i + 1;
                remaining_fuel = 0;
            }
        }
        return ans;
    }
}
