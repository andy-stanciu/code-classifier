class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int n = param1.length;
        int tf = 0;
        int f = 0;
        int index = 0;
        for (int i = 0; i < n; i++) {
            tf += param1[i] - cost[i];
            f += param1[i] - cost[i];
            if (f < 0) {
                f = 0;
                index = i + 1;
            }
        }
        return (tf < 0) ? -1 : index;
    }
}
