class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int total = 0;
        int current = 0;
        int start = 0;
        for (int i = 0; i < param1.length; i++) {
            //through the difference,net cost should be positive then we can start there
            int netCost = param1[i] - cost[i];
            total += netCost;
            current += netCost;
            if (current < 0) {
                start = i + 1;
                current = 0;
            }
        }
        //we should return only if the netcost be +ve although start having positive in it
        return total < 0 ? -1 : start;
    }
}
