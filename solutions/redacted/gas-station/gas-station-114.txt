class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int distance = 0;
        int index = 0;
        for (int i = 0; i < param1.length; i++) {
            distance = distance + param1[i] - cost[i];
        }
        if (distance < 0) {
            return -1;
        }
        distance = 0;
        for (int i = 0; i < param1.length - 1; i++) {
            distance = distance + param1[i] - cost[i];
            if (distance < 0) {
                index = i + 1;
                distance = 0;
            }
        }
        return index;
    }
}
