class Solution {

    public List<List<Integer>> method1(int[] candidates, int target) {
        List<List<Integer>> resultList = new ArrayList<>();
        return method2(candidates, target, resultList, new ArrayList<>(), 0, 0);
    }

    private List<List<Integer>> method2(int[] candidates, int target, List<List<Integer>> resultList, ArrayList<Integer> tempList, int first, int param1) {
        Arrays.sort(candidates);
        if (param1 == target) {
            resultList.add(new ArrayList<>(tempList));
        }
        int prev = -1;
        for (int i = first; i < candidates.length; i++) {
            if (param1 + candidates[i] > target) {
                break;
            }
            if (prev == candidates[i]) {
                continue;
            }
            tempList.add(candidates[i]);
            method2(candidates, target, resultList, tempList, i + 1, param1 + candidates[i]);
            tempList.remove(tempList.size() - 1);
            prev = candidates[i];
        }
        return resultList;
    }
}
