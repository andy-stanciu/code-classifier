class Solution {

    public void Solve(int[] candidates, int target, int param1, List<List<Integer>> output, List<Integer> ans, int index) {
        if (target == param1) {
            output.add(new ArrayList<>(ans));
            return;
        }
        if (candidates.length == index)
            return;
        if (param1 > target)
            return;
        ans.add(candidates[index]);
        Solve(candidates, target, param1 + candidates[index], output, ans, index + 1);
        ans.remove(ans.size() - 1);
        while (index < candidates.length - 1 && candidates[index] == candidates[index + 1]) index++;
        Solve(candidates, target, param1, output, ans, index + 1);
    }

    public List<List<Integer>> method1(int[] candidates, int target) {
        List<List<Integer>> output = new ArrayList<>();
        List<Integer> ans = new ArrayList<>();
        Arrays.sort(candidates);
        Solve(candidates, target, 0, output, ans, 0);
        return output;
    }
}
