class Solution {

    public int[] method1(int[] nums1, int[] nums2) {
        //Solution 1
        // HashSet<Integer> arr1 = new HashSet<Integer>();
        // HashSet<Integer> arr2 = new HashSet<Integer>();
        // for(int i = 0;i < nums1.length;i++)
        //     arr1.add(nums1[i]);
        // for(int i = 0;i < nums2.length;i++)
        //     arr2.add(nums2[i]);
        // arr1.retainAll(arr2);
        // int[] result = new int[arr1.size()];
        // int i = 0;
        // for(int num: arr1)
        //     result[i++] = num;
        // return result;
        int[] seem = new int[1000];
        for (int i : nums1) seem[i]++;
        int[] result = new int[Math.min(nums1.length, nums2.length)];
        int j = 0;
        for (int i : nums2) {
            if (seem[i] > 0) {
                result[j++] = i;
                seem[i] = 0;
            }
        }
        int[] res = Arrays.copyOf(result, j);
        return res;
    }
}
