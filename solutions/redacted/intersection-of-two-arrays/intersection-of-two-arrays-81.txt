class Solution {

    public int[] method1(int[] nums1, int[] nums2) {
        HashSet<Integer> set = new HashSet<>();
        for (int i : nums1) {
            set.add(i);
        }
        HashSet<Integer> method1 = new HashSet<>();
        for (int i : nums2) {
            if (set.contains(i)) {
                // if set1 contains that i value then we will add on intersection.
                method1.add(i);
            }
        }
        int[] result = new int[method1.size()];
        int index = 0;
        for (int i : method1) {
            result[index++] = i;
        }
        return result;
    }
}
