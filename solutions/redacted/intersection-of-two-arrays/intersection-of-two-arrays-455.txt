class Solution {

    public int[] method1(int[] nums1, int[] nums2) {
        Set<Integer> nums1_set = new HashSet<>();
        Set<Integer> nums2_set = new HashSet<>();
        // Add elements from nums1 to nums1_set
        for (int num : nums1) {
            nums1_set.add(num);
        }
        // Add elements from nums2 to nums2_set
        for (int num : nums2) {
            nums2_set.add(num);
        }
        // Find intersection
        nums1_set.retainAll(nums2_set);
        // Convert set to array
        int[] result = new int[nums1_set.size()];
        int index = 0;
        for (int num : nums1_set) {
            result[index++] = num;
        }
        return result;
    }
}
