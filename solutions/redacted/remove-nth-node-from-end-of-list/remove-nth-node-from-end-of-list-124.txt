class Solution {

    int ans = 1;

    public ListNode method1(ListNode head, int n) {
        if (head == null)
            return null;
        ListNode next = method1(head.next, n);
        if (ans++ == n)
            return next;
        head.next = next;
        return head;
    }
}
