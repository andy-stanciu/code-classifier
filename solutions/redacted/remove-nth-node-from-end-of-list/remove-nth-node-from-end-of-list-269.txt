//couting index from end instead of starting
//bottom - up
class Solution {

    int index = 0;

    public ListNode method1(ListNode head, int n) {
        method2(head.next, head, n);
        index++;
        if (index == n) {
            return head.next;
        }
        return head;
    }

    private void method2(ListNode current, ListNode prev, int n) {
        if (current == null) {
            return;
        } else {
            method2(current.next, current, n);
            index++;
            if (index == n) {
                prev.next = current.next;
            }
        }
    }
}
