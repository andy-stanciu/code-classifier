class Solution {

    public int maxProfit(int[] prices) {
        int var1 = Integer.MAX_VALUE;
        int maxprofit = 0;
        for (int i = 0; i < prices.length; i++) {
            if (var1 < prices[i]) {
                // Today\'s Profit = Selling price - Buying price
                int profit = prices[i] - var1;
                maxprofit = Math.max(maxprofit, profit);
            } else {
                //Updating buyprice to the minimum price[i] obtained
                var1 = prices[i];
            }
        }
        return maxprofit;
    }
}
