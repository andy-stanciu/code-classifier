class Solution {

    public int maxProfit(int[] prices) {
        int p = 0;
        int var1 = prices[0];
        for (int i = 1; i < prices.length; i++) {
            //if today\'s price is less than buy price, buy today
            if (var1 > prices[i]) {
                var1 = prices[i];
            } else {
                //if today\'s price is making better profit than before, sell today
                p = Math.max(p, prices[i] - var1);
            }
        }
        return p;
    }
}
