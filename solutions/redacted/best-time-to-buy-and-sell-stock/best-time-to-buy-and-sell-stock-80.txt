class Solution {

    public int maxProfit(int[] a) {
        int min = a[0];
        int p = 0;
        for (int i = 0; i < a.length; i++) {
            min = Math.min(min, a[i]);
            int maxDiff = a[i] - min;
            p = Math.max(maxDiff, p);
        }
        return p;
    }
}
