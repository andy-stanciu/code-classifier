class Solution {

    public int maxProfit(int[] prices) {
        int var1 = 0;
        int holdOne = Integer.MIN_VALUE;
        for (int price : prices) {
            var1 = Math.max(var1, holdOne + price);
            holdOne = Math.max(holdOne, -price);
        }
        return var1;
    }
}
