class Solution {

    public int maxProfit(int[] prices) {
        int result = 0;
        int min = Integer.MAX_VALUE;
        for (int p : prices) {
            min = Math.min(min, p);
            result = Math.max(result, p - min);
        }
        return result;
    }
}
