class Solution {

    public int maxProfit(int[] arr) {
        int max = 0;
        int n = arr.length;
        int min = arr[0];
        for (int i = 1; i < n; i++) {
            min = Math.min(min, arr[i]);
            max = Math.max(max, arr[i] - min);
        }
        return max;
    }
}
