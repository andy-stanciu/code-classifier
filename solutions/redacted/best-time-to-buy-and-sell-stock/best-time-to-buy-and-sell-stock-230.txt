class Solution {

    public int maxProfit(int[] prices) {
        //edge case in case stock bought and sold on same day
        if (prices == null || prices.length < 2) {
            return 0;
        }
        int maxProfit = 0;
        int var1 = 0;
        int var2 = 1;
        int n = prices.length;
        while (var2 < n) {
            //price starts from day 1 to achive profit
            int currentPrice = prices[var2];
            //price to buy
            int var3 = prices[var1];
            if (var3 < currentPrice) {
                int currentProfit = currentPrice - var3;
                maxProfit = Math.max(maxProfit, currentProfit);
            } else {
                //to set for the lesser price
                var1 = var2;
            }
            var2++;
        }
        return maxProfit;
    }
}
