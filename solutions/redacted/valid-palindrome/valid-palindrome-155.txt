class Solution {

    public boolean method1(String s) {
        String parsedInput = s.replaceAll("[^0-9a-zA-Z]", "").toLowerCase();
        String reversedInput = new StringBuilder(parsedInput).reverse().toString();
        return parsedInput.equals(reversedInput);
    }
}
