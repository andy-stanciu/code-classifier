class Solution {

    public boolean method1(String s) {
        String s_new = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
        String rev = "";
        for (int i = s_new.length() - 1; i >= 0; i--) {
            rev += s_new.charAt(i);
        }
        return s_new.equals(rev);
    }
}
