class Solution {

    public boolean method1(String str) {
        StringBuilder sb = new StringBuilder();
        str.chars().filter(Character::isLetterOrDigit).forEach(c -> sb.append((char) c));
        return sb.toString().equalsIgnoreCase(sb.reverse().toString());
    }
}
