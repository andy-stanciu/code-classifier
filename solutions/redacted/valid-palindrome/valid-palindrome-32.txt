class Solution {

    public boolean method1(String s) {
        s = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
        StringBuilder str = new StringBuilder(s);
        if (s.contentEquals(str.reverse()))
            return true;
        else
            return false;
    }
}
