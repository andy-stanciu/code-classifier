class Solution {

    public void method1(int[] nums1, int m, int[] nums2, int n) {
        int j = 0;
        for (int i = m; i <= nums1.length; i++) {
            if (j < n) {
                nums1[i] = nums2[j];
                j += 1;
            }
        }
        Arrays.sort(nums1);
    }
}
