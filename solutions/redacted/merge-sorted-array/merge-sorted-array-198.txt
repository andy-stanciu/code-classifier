class Solution {

    public void method1(int[] nums1, int m, int[] nums2, int n) {
        for (int i = m, j = 0; j < n; ++i, ++j) {
            nums1[i] = nums2[j];
        }
        Arrays.sort(nums1, 0, m + n);
    }
}
