class Solution {

    public void method1(int[] nums1, int m, int[] nums2, int n) {
        for (int i = 0; i <= nums2.length - 1; i++) {
            nums1[i + m] = nums2[i];
        }
        Arrays.sort(nums1);
    }
}
