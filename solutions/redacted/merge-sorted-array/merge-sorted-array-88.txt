class Solution {

    public static void method1(int[] nums1, int m, int[] nums2, int n) {
        int l2 = nums2.length;
        int ind = 0;
        for (int i = m; i < nums1.length; i++) {
            nums1[i] = nums2[ind];
            ind += 1;
        }
        Arrays.sort(nums1);
    }
}
