class Solution {

    private List<Integer> result;

    public List<Integer> method1(TreeNode root) {
        result = new ArrayList<>();
        method2(root);
        return result;
    }

    private void method2(TreeNode node) {
        if (node == null)
            return;
        result.add(node.val);
        if (node.left != null) {
            method2(node.left);
        }
        if (node.right != null) {
            method2(node.right);
        }
    }
}
