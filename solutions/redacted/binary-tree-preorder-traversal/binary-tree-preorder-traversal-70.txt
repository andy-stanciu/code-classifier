class Solution {

    List<Integer> ls = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return ls;
        }
        ls.add(root.val);
        method1(root.left);
        method1(root.right);
        return ls;
    }
}
