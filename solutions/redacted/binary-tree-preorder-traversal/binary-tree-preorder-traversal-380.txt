class Solution {

    public List<Integer> method1(TreeNode root) {
        ArrayList<Integer> arr = new ArrayList<>();
        if (root == null)
            return arr;
        arr.add(root.val);
        arr.addAll(method1(root.left));
        arr.addAll(method1(root.right));
        return arr;
    }
}
