/*class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;
    TreeNode(int x) { val = x; }
}
*/
class Solution {

    private void method1(TreeNode root, List<Integer> res) {
        if (root == null)
            return;
        res.add(root.val);
        method1(root.left, res);
        method1(root.right, res);
    }

    public List<Integer> method2(TreeNode root) {
        List<Integer> res = new ArrayList<>();
        method1(root, res);
        return res;
    }
}
