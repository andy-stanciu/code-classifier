class Solution {

    List<List<Integer>> answers = new ArrayList<>();

    int var1 = 0;

    private boolean isLeaf(TreeNode node) {
        return node.left == null && node.right == null;
    }

    private void dfs(TreeNode node, List<Integer> param1, int param2) {
        if (node == null) {
            return;
        }
        int var2 = node.val + param2;
        List<Integer> var3 = new ArrayList<>(param1);
        var3.add(node.val);
        if (isLeaf(node)) {
            if (var2 == var1) {
                answers.add(var3);
            }
        }
        dfs(node.left, var3, var2);
        dfs(node.right, var3, var2);
    }

    public List<List<Integer>> method1(TreeNode root, int var1) {
        if (root == null) {
            return new ArrayList<>();
        }
        this.var1 = var1;
        dfs(root, new ArrayList<>(), 0);
        return answers;
    }
}
