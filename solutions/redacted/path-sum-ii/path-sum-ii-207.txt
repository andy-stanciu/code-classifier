/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    List<List<Integer>> ans = new ArrayList<>();

    public void find(ArrayList<Integer> list, int cs, TreeNode root, int param1) {
        if (root == null)
            return;
        list.add(root.val);
        if (root.left == null && root.right == null && cs + root.val == param1) {
            ans.add(new ArrayList<>(list));
        } else {
            find(list, cs + root.val, root.left, param1);
            find(list, cs + root.val, root.right, param1);
        }
        list.remove(list.size() - 1);
    }

    public List<List<Integer>> method1(TreeNode root, int param1) {
        ArrayList<Integer> list = new ArrayList<>();
        find(list, 0, root, param1);
        return ans;
    }
}
