/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<List<Integer>> method1(TreeNode root, int param1) {
        List<List<Integer>> result = new ArrayList<>();
        if (root == null) {
            return result;
        }
        backtrack(result, new ArrayList<>(), root, param1);
        return result;
    }

    public void backtrack(List<List<Integer>> res, List<Integer> list, TreeNode root, int param2) {
        if (root == null) {
            return;
        }
        param2 -= root.val;
        list.add(root.val);
        if (root.left == null && root.right == null) {
            if (param2 == 0) {
                res.add(new ArrayList<>(list));
            }
            list.remove(list.size() - 1);
            return;
        }
        backtrack(res, list, root.left, param2);
        backtrack(res, list, root.right, param2);
        list.remove(list.size() - 1);
    }
}
