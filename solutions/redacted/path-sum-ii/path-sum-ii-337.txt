class Solution {

    public void solver(TreeNode root, int param1, List<List<Integer>> ans, List<Integer> dummy) {
        if (root == null)
            return;
        dummy.add(root.val);
        if (root.left == null && root.right == null && param1 - root.val == 0) {
            ans.add(new ArrayList<>(dummy));
        }
        if (root.left != null) {
            solver(root.left, param1 - root.val, ans, dummy);
            dummy.remove(dummy.size() - 1);
        }
        if (root.right != null) {
            solver(root.right, param1 - root.val, ans, dummy);
            dummy.remove(dummy.size() - 1);
        }
    }

    public List<List<Integer>> method1(TreeNode root, int param1) {
        List<List<Integer>> ans = new ArrayList<>();
        List<Integer> dummy = new ArrayList<>();
        solver(root, param1, ans, dummy);
        return ans;
    }
}
