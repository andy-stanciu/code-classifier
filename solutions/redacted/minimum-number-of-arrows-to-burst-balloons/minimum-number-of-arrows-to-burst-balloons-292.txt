class Solution {

    public int method1(int[][] points) {
        Arrays.sort(points, (a, b) -> Integer.compare(a[1], b[1]));
        int[] prev = points[0];
        int count = 1;
        for (int[] point : points) {
            if (point[0] > prev[1]) {
                prev = point;
                count++;
            }
        }
        return count;
    }
}
