class Solution {

    public int method1(int[][] points) {
        Arrays.sort(points, (a, b) -> Integer.compare(a[1], b[1]));
        int var1 = 1, cur = points[0][1];
        for (int i = 1; i < points.length; i++) {
            if (cur >= points[i][0])
                continue;
            var1++;
            cur = points[i][1];
        }
        return var1;
    }
}
