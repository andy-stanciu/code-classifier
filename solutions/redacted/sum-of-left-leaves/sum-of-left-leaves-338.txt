class Solution {

    public int method1(TreeNode root) {
        return method1(root, false);
    }

    //same method, but we check if we are on a left child using a boolean
    public int method1(TreeNode root, boolean param1) {
        if (root == null)
            return 0;
        //if its a leaf and a left child, we add root val
        if (param1 == true && root.param1 == null && root.right == null)
            return root.val;
        //do the same to all left and right nodes
        return method1(root.param1, true) + method1(root.right, false);
    }
}
