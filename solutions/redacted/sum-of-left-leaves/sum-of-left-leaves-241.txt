class Solution {

    public int method1(TreeNode root) {
        return traverse(root, false);
    }

    private int traverse(TreeNode root, boolean param1) {
        if (root == null)
            return 0;
        if (root.left == null && root.right == null && param1)
            return root.val;
        int var1 = traverse(root.left, true);
        int var2 = traverse(root.right, false);
        return var1 + var2;
    }
}
