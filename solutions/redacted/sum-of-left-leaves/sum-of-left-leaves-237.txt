class Solution {

    public int method1(TreeNode root) {
        return dfs(root, false);
    }

    public int dfs(TreeNode root, boolean param1) {
        if (root == null) {
            return 0;
        }
        int var1 = 0;
        if (root.left == null && root.right == null && param1) {
            var1 += root.val;
        }
        return var1 + dfs(root.left, true) + dfs(root.right, false);
    }
}
