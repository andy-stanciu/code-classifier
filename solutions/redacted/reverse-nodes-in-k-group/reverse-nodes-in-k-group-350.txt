/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {

    void method1(ListNode head, int k, ListNode lastnode) {
        if (head == null)
            return;
        ListNode temp = head;
        for (int i = 0; i < k; i++) {
            if (temp == null) {
                lastnode.next = head;
                return;
            }
            temp = temp.next;
        }
        ListNode prev = null;
        ListNode next = null;
        ListNode curr = head;
        for (int i = 0; i < k; i++) {
            next = curr.next;
            curr.next = prev;
            prev = curr;
            curr = next;
        }
        lastnode.next = prev;
        lastnode = head;
        method1(curr, k, lastnode);
        // System.out.print(lastnode.val);
        return;
    }

    public ListNode method2(ListNode head, int k) {
        ListNode var1 = new ListNode(-1);
        ListNode lastnode = var1;
        method1(head, k, lastnode);
        return var1.next;
    }
}
