class Solution {

    public List<Double> method1(TreeNode root) {
        if (root == null)
            return new ArrayList<>();
        List<Double> ans = new ArrayList<>();
        Queue<TreeNode> q = new LinkedList<>();
        q.add(root);
        while (!q.isEmpty()) {
            // Number of nodes in the current level
            int var1 = q.size();
            // Sum of values of nodes in the current level
            double var2 = 0.0;
            for (int i = 0; i < var1; i++) {
                TreeNode curr = q.poll();
                var2 += curr.val;
                if (curr.left != null) {
                    q.offer(curr.left);
                }
                if (curr.right != null) {
                    q.offer(curr.right);
                }
            }
            // Calculate average for the current level
            double levelAvg = var2 / var1;
            ans.add(levelAvg);
        }
        return ans;
    }
}
