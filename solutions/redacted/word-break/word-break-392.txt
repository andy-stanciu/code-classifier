class Solution {

    Set<String> set = new HashSet<String>();

    public boolean method1(String s, List<String> param1) {
        if (s.length() == 0)
            return true;
        if (set.contains(s))
            return false;
        for (int i = 0; i < s.length(); i++) {
            if (param1.contains(s.substring(0, i + 1))) {
                boolean ans = method1(s.substring(i + 1), param1);
                if (ans)
                    return true;
                //else set.add()
            }
        }
        set.add(s);
        return false;
    }
}
