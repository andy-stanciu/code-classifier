class Solution {

    public boolean method1(String s, List<String> param1) {
        int strLen = s.length();
        boolean[] dp = new boolean[strLen + 1];
        dp[strLen] = true;
        for (int i = strLen - 1; i >= 0; i--) {
            for (String w : param1) {
                if ((i + w.length()) <= strLen && s.substring(i, i + w.length()).equals(w)) {
                    dp[i] = dp[i + w.length()];
                }
                if (dp[i])
                    break;
            }
        }
        return dp[0];
    }
}
