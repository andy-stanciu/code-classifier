class Solution {

    public boolean method1(String s, List<String> param1) {
        int n = s.length();
        boolean[] dp = new boolean[n];
        for (int i = 0; i < n; i++) {
            // Check if the substring from 0 to i is in the wordDict
            if (param1.contains(s.substring(0, i + 1))) {
                dp[i] = true;
            } else {
                // If not, check if the substring can be segmented into words from wordDict
                for (int j = 0; j < i; j++) {
                    if (dp[j] && param1.contains(s.substring(j + 1, i + 1))) {
                        dp[i] = true;
                        break;
                    }
                }
            }
        }
        return dp[n - 1];
    }
}
