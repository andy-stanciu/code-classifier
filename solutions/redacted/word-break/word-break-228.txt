class Solution {

    public boolean method1(String s, List<String> param1) {
        // int i;
        // for(i=0;i<wordDict.size();i++)
        // {
        //     String str = s.replaceFirst(wordDict.get(i),"");
        //     if(str.equals(s))
        //     {
        //         break;
        //     }
        //     else
        //     {
        //         s = str;
        //     }
        // }
        // if(i==wordDict.size())
        // {
        //     return true;
        // }
        // else
        // {
        //     return false;
        // }
        int n = s.length();
        boolean[] dp = new boolean[n + 1];
        dp[0] = true;
        for (int i = 1; i <= n; i++) {
            for (int j = 0; j < i; j++) {
                if (dp[j] && param1.contains(s.substring(j, i))) {
                    dp[i] = true;
                    break;
                }
            }
        }
        return dp[n];
    }
}
