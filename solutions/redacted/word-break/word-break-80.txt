class //17.20
Solution {

    public boolean find(String s, List<String> param1) {
        if (s.equals(""))
            return true;
        for (String w : param1) {
            if (w.equals(s))
                return true;
            if (w.length() > s.length())
                break;
            boolean notFound = false;
            if (s.indexOf(w) > -1) {
                String[] splt = s.split(w);
                for (String spl : splt) {
                    boolean found = find(spl, param1);
                    if (!found) {
                        notFound = true;
                        break;
                    }
                }
                if (!notFound)
                    return true;
            }
        }
        return false;
    }

    public boolean method1(String s, List<String> param1) {
        Collections.sort(param1, new Comparator<String>() {

            public int compare(String thz, String that) {
                return thz.length() - that.length();
            }
        });
        return find(s, param1);
    }
}
