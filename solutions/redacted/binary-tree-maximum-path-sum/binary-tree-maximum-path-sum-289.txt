/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int method1(TreeNode root) {
        //recursive
        int[] result = new int[1];
        result[0] = Integer.MIN_VALUE;
        method2(root, result);
        return result[0];
    }

    private int method2(TreeNode root, int[] result) {
        if (root == null)
            return 0;
        int var1 = Math.max(0, method2(root.left, result));
        int var2 = Math.max(0, method2(root.right, result));
        result[0] = Math.max(result[0], var1 + var2 + root.val);
        return root.val + Math.max(var1, var2);
    }
}
