/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int method1(TreeNode root, int[] res) {
        if (root == null) {
            return 0;
        }
        int leftMax = Math.max(method1(root.left, res), 0);
        int rightMax = Math.max(method1(root.right, res), 0);
        res[0] = Math.max(res[0], leftMax + root.val + rightMax);
        return root.val + Math.max(leftMax, rightMax);
    }

    public int method1(TreeNode root) {
        if (root == null) {
            return 0;
        }
        int[] res = new int[1];
        res[0] = root.val;
        method1(root, res);
        return res[0];
    }
}
