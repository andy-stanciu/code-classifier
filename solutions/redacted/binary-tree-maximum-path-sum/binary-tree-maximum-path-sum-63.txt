class Solution {

    private int method1(TreeNode root, int[] ans) {
        if (root == null)
            return 0;
        int left = Math.max(method1(root.left, ans), 0);
        int right = Math.max(method1(root.right, ans), 0);
        ans[0] = Math.max(ans[0], root.val + left + right);
        return root.val + Math.max(left, right);
    }

    public int method2(TreeNode root) {
        int[] ans = { Integer.MIN_VALUE };
        method1(root, ans);
        return ans[0];
    }
}
