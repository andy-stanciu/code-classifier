/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    int var1;

    public int method1(TreeNode root) {
        if (root == null)
            return 0;
        var1 = root.val;
        int var2 = traverse(root);
        var1 = Math.max(var1, var2);
        return var1;
    }

    public int traverse(TreeNode root) {
        if (root == null)
            return 0;
        int var3 = traverse(root.left);
        int var4 = traverse(root.right);
        int var2 = root.val + var3 + var4;
        var1 = Math.max(var1, root.val + Math.max(var3, var4));
        var1 = Math.max(var1, Math.max(root.val, var2));
        return Math.max(root.val + Math.max(var3, var4), root.val);
    }
}
