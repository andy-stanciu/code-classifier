class Solution {

    public int method1(int target, int[] nums) {
        int n = nums.length;
        int ps = 0, i = 0, ans = Integer.MAX_VALUE;
        for (int j = 0; j < n; j++) {
            ps += nums[j];
            while (ps >= target) {
                //while subarray with sum>target is found we are trying to remove one element at a time and check if sum is still greater than target
                ans = Math.min(j - i + 1, ans);
                ps -= nums[i++];
            }
        }
        return ans == Integer.MAX_VALUE ? 0 : ans;
    }
}
