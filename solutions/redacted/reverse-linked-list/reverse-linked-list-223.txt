class // Recursive
Solution {

    public ListNode method1(ListNode head) {
        if (head == null)
            return head;
        ListNode[] ans = new ListNode[1];
        method2(head, ans).next = null;
        return ans[0];
    }

    private ListNode method2(ListNode head, ListNode[] ans) {
        if (head.next == null) {
            ans[0] = head;
            return head;
        }
        method2(head.next, ans).next = head;
        return head;
    }
}
