class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode p = method1(head.next);
        head.next.next = head;
        head.next = null;
        return p;
    }
}
