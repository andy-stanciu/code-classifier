class Solution {

    public ListNode method1(ListNode head) {
        return method2(head, null);
    }

    ListNode method2(ListNode head, ListNode prev) {
        if (head == null) {
            return prev;
        }
        ListNode next = head.next;
        head.next = prev;
        return method2(next, head);
    }
}
