class Solution {

    public ListNode method1(ListNode head) {
        //Base case
        if (head == null || head.next == null) {
            return head;
        }
        //Recursive call
        ListNode newHead = method1(head.next);
        head.next.next = head;
        head.next = null;
        return newHead;
    }
}
