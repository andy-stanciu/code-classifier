class Solution {

    public ListNode method1(ListNode head) {
        ListNode ret = null;
        while (head != null) {
            ret = new ListNode(head.val, ret);
            head = head.next;
        }
        return ret;
    }
}
