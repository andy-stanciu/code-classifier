class Solution {

    public ListNode method1(ListNode head) {
        ListNode prev = null;
        while (head != null) {
            ListNode tempNext = prev;
            prev = head;
            head = head.next;
            prev.next = tempNext;
        }
        return prev;
    }
}
