class Solution {

    public ListNode method1(ListNode head) {
        ListNode prev = null;
        while (head != null) {
            ListNode tempNext = prev;
            prev = head;
            prev.next = tempNext;
            head = head.next;
        }
        return prev;
    }
}
