class Solution {

    public ListNode method1(ListNode head) {
        ListNode cur = head, prev = null, post;
        while (cur != null) {
            post = cur.next;
            cur.next = prev;
            prev = cur;
            cur = post;
        }
        return prev;
    }
}
