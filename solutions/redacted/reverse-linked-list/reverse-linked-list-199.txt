class Solution {

    public ListNode method1(ListNode head) {
        return method2(null, head);
    }

    public ListNode method2(ListNode prev, ListNode curr) {
        if (curr == null)
            return prev;
        ListNode nextNode = curr.next;
        curr.next = prev;
        return method2(curr, nextNode);
    }
}
