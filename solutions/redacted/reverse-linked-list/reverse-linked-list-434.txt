class Solution {

    public ListNode method1(ListNode head) {
        ListNode re = null;
        while (head != null) {
            ListNode temp = new ListNode(head.val, re);
            re = temp;
            head = head.next;
        }
        return re;
    }
}
