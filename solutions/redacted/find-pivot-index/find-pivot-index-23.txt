class Solution {

    public int method1(int[] nums) {
        int lsum = 0, rsum = 0, var1 = -1;
        for (int i = 1; i < nums.length; i++) {
            rsum += nums[i];
        }
        if (rsum == 0)
            return 0;
        for (int i = 1; i < nums.length; i++) {
            lsum += nums[i - 1];
            rsum -= nums[i];
            if (lsum == rsum)
                return i;
        }
        return var1;
    }
}
