/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<List<Integer>> method1(TreeNode root) {
        List<List<Integer>> result = new ArrayList<>();
        method2(result, root, 0, true);
        return result;
    }

    private void method2(List<List<Integer>> result, TreeNode root, int param1, boolean ltr) {
        if (root == null) {
            return;
        }
        List<Integer> list;
        if (result.size() == param1) {
            list = new ArrayList<>();
            result.add(list);
        } else {
            list = result.get(param1);
        }
        if (ltr) {
            list.add(root.val);
        } else {
            list.add(0, root.val);
        }
        method2(result, root.left, param1 + 1, !ltr);
        method2(result, root.right, param1 + 1, !ltr);
    }
}
