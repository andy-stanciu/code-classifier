class Solution {

    public int method1(int[] nums) {
        // Handling Edge Cases and trivial cases.
        if (nums.length <= 1)
            return nums.length;
        //Sorting the given array in place.
        Arrays.sort(nums);
        int maxStreak = 1, currentStreak = 1;
        //Iterating through the array and counting consecutive numbers.
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] == (nums[i - 1] + 1)) {
                currentStreak++;
                if (current > max)
                    maxStreak = currentStreak;
            } else if (nums[i] == nums[i - 1]) {
                // If there are any equal numbers, simply do nothing, move ahead in array traversal
                // Donot increase the streakCount in this situation.
            } else {
                // If non consecutive numbers are found, reset the counter.
                currentStreak = 1;
            }
        }
        return maxStreak;
    }
}
