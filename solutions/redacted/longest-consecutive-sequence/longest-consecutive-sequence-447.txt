class Solution {

    public int method1(int[] nums) {
        if (nums.length == 0)
            return 0;
        Arrays.sort(nums);
        int var1 = 1;
        int currentLength = 1;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] != nums[i - 1]) {
                if (nums[i] == nums[i - 1] + 1) {
                    currentLength++;
                } else {
                    var1 = Math.max(var1, currentLength);
                    currentLength = 1;
                }
            }
        }
        return Math.max(var1, currentLength);
    }
}
