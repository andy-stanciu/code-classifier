class Solution {

    public String method1(int num) {
        int[] value = { 1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1 };
        String[] var1 = { "M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I" };
        for (int i = 0; i < value.length; i++) {
            if (num >= value[i]) {
                return var1[i] + method1(num - value[i]);
            }
        }
        return "";
    }
}
