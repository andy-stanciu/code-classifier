class Solution {

    public int[] method1(int[] a, int param1) {
        Map<Integer, Integer> map = new HashMap();
        int count = 0;
        for (int e : a) {
            int comp = param1 - e;
            if (map.containsKey(comp)) {
                int[] arr = { count, map.get(comp) };
                return arr;
            }
            map.put(e, count);
            count++;
        }
        return null;
    }
}
