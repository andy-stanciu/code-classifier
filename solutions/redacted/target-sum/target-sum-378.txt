class Solution {

    int count = 0;

    public void backtrack(int[] nums, int remain, int start) {
        if (start == nums.length && remain == 0) {
            count++;
            return;
        }
        if (start == nums.length)
            return;
        backtrack(nums, remain - nums[start], start + 1);
        backtrack(nums, remain + nums[start], start + 1);
    }

    public int method1(int[] nums, int param1) {
        backtrack(nums, param1, 0);
        return count;
    }
}
