public class Solution {

    public int method1(int[] nums, int param1) {
        HashMap<Pair, Integer> map = new HashMap<>();
        int index = nums.length - 1, var1 = 0;
        return fn(index, var1, param1, nums, map);
    }

    public static int fn(int i, int var1, int param1, int[] nums, HashMap<Pair, Integer> map) {
        Pair key = new Pair(i, var1);
        if (map.containsKey(key))
            return map.get(key);
        // Base Case
        if (i == -1) {
            if (var1 == param1)
                return 1;
            else
                return 0;
        }
        int add = fn(i - 1, var1 + nums[i], param1, nums, map);
        int sub = fn(i - 1, var1 - nums[i], param1, nums, map);
        map.put(key, add + sub);
        return add + sub;
    }
}

// ONLY RECURSION
class Solution {

    public int method1(int[] nums, int param1) {
        return travel(0, 0, nums, param1);
    }

    public static int travel(int i, int param2, int[] nums, int param1) {
        if (i == nums.length) {
            if (param2 == param1) {
                return 1;
            } else {
                return 0;
            }
        }
        int waysWithAddition = travel(i + 1, param2 + nums[i], nums, param1);
        int waysWithSubtraction = travel(i + 1, param2 - nums[i], nums, param1);
        return waysWithAddition + waysWithSubtraction;
    }
}
