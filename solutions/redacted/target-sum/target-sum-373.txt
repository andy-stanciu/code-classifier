class Solution {

    //step 1
    int count = 0;

    public int method1(int[] nums, int param1) {
        DFS(nums, param1, 0, 0);
        ;
        //step 5
        return count;
    }

    //step 2
    public void DFS(int[] nums, int param1, int param2, int index) {
        //step 3
        if (index == nums.length) {
            if (param2 == param1) {
                count++;
            }
            return;
        }
        //step 4
        DFS(nums, param1, param2 + nums[index], index + 1);
        DFS(nums, param1, param2 - nums[index], index + 1);
    }
}
