class Solution {

    public boolean method1(String s, String t) {
        if (s.length() != t.length()) {
            return false;
        }
        int i = 0;
        while (i < t.length()) {
            if (s.contains(t.charAt(i) + "")) {
                //.contains takes String so we are adding char and "" to make it string
                //Removing first occurence
                s = s.replaceFirst(t.charAt(i) + "", "");
                i++;
            } else {
                return false;
            }
        }
        return true;
    }
}
