class Solution {

    public boolean method1(String s, String t) {
        if (s.length() != t.length()) {
            return false;
        }
        List<Character> list = new ArrayList<Character>();
        for (int i = 0; i < s.length(); i++) {
            list.add(s.charAt(i));
        }
        // remove all the string t present in the list, if list is empty then it is
        // anagram otherwise it is not an anagram.
        for (int i = 0; i < t.length(); i++) {
            list.remove(new Character(t.charAt(i)));
        }
        if (list.size() == 0) {
            return true;
        }
        return false;
    }
}
