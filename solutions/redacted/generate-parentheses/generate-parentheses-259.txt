class Solution {

    List<String> result = new ArrayList<>();

    String leftBracket = "(";

    String rightBracket = ")";

    public List<String> method1(int n) {
        method2(n, 0, 0, 0, "");
        return result;
    }

    private void method2(int n, int openBracketCount, int closeBracketCount, int currentOpenCount, String brackets) {
        // Base case
        if ((openBracketCount > n) || (closeBracketCount > n))
            return;
        if ((openBracketCount == n) && closeBracketCount == n)
            result.add(brackets);
        method2(n, openBracketCount + 1, closeBracketCount, currentOpenCount + 1, brackets + leftBracket);
        if (currentOpenCount > 0)
            method2(n, openBracketCount, closeBracketCount + 1, currentOpenCount - 1, brackets + rightBracket);
    }
}
