class Solution {

    public List<String> method1(int n) {
        List<String> ans = new ArrayList<>();
        String s = "";
        method2(0, 0, n, s, ans);
        return ans;
    }

    public static void method2(int open, int close, int n, String s, List<String> ans) {
        if (open == n && close == n) {
            ans.add(s);
            return;
        }
        if (open < n) {
            method2(open + 1, close, n, s + "(", ans);
        }
        if (close < open) {
            method2(open, close + 1, n, s + ")", ans);
        }
    }
}
