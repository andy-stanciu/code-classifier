class Solution {

    public List<String> method1(int n) {
        List<String> result = new ArrayList<>();
        method2(n, 0, 0, result, new StringBuilder());
        return result;
    }

    public void method2(int n, int open, int close, List<String> ll, StringBuilder current) {
        if (open == n && close == n) {
            ll.add(current.toString());
            return;
        }
        if (open < n) {
            current.append("(");
            method2(n, open + 1, close, ll, current);
            current.deleteCharAt(current.length() - 1);
        }
        if (close < open) {
            current.append(")");
            method2(n, open, close + 1, ll, current);
            current.deleteCharAt(current.length() - 1);
        }
    }
}
