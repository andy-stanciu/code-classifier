class Solution {

    private List<String> permutations = new ArrayList<>();

    public List<String> method1(int n) {
        method2("", n, 0);
        return permutations;
    }

    private void method2(String oldString, int n, int openBrackets) {
        if (n > 0) {
            method2(oldString + "(", n - 1, openBrackets + 1);
        }
        if (openBrackets > 0) {
            method2(oldString + ")", n, openBrackets - 1);
        }
        if (n == 0 && openBrackets == 0) {
            permutations.add(oldString);
        }
    }
}
