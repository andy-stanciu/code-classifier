class Solution {

    public int strStr(String haystack, String needle) {
        //returns the index within this string with the first occurence of the specified substring
        int var1 = haystack.indexOf(needle);
        if (var1 == -1) {
            return -1;
        } else {
            return var1;
        }
    }
}
