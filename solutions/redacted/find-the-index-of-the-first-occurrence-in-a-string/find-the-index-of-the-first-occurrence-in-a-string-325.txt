class Solution {

    public int strStr(String haystack, String needle) {
        int n = haystack.length();
        int ans = 0;
        for (int i = 0; i < n; i++) {
            ans = haystack.indexOf(needle);
        }
        return ans;
    }
}
