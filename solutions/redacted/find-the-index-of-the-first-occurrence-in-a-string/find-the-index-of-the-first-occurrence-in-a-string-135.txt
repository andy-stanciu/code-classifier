class Solution {

    public int strStr(String haystack, String needle) {
        int length = needle.length();
        for (int i = 0; i <= haystack.length() - length; ) {
            if (haystack.substring(i, i + length).equals(needle)) {
                return i;
            }
            i++;
        }
        return -1;
    }
}
