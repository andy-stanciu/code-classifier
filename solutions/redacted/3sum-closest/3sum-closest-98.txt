class Solution {

    public int method1(int[] nums, int target) {
        Arrays.sort(nums);
        int n = nums.length;
        if (n < 3)
            return 0;
        int ans = nums[0] + nums[1] + nums[2];
        for (int i = 0; i < n; i++) {
            int left = i + 1, right = n - 1;
            while (left < right) {
                int sum = nums[i] + nums[left] + nums[right];
                //when new sum is near then previous
                if (Math.abs(sum - target) < Math.abs(ans - target)) {
                    ans = sum;
                }
                if (sum == target)
                    return sum;
                //if sum is greater, we need lower so will go closer
                if (sum > target) {
                    right--;
                } else {
                    // if sum is lesser, we need to increase so we go closer
                    left++;
                }
            }
        }
        return ans;
    }
}
