class Solution {

    public int method1(int[] nums, int target) {
        Arrays.sort(nums);
        if (nums.length < 3) {
            // Handle cases where the input array has less than 3 elements
            return -1;
        }
        // Initialize closestSum with the sum of the first three elements
        int var1 = nums[0] + nums[1] + nums[2];
        for (int i = 0; i < nums.length - 2; i++) {
            int start = i + 1;
            int end = nums.length - 1;
            while (start < end) {
                int sum = nums[i] + nums[start] + nums[end];
                if (Math.abs(sum - target) < Math.abs(var1 - target)) {
                    // Update closestSum if the current sum is closer to the target
                    var1 = sum;
                }
                if (sum < target) {
                    // Increment start if sum is less than target
                    start++;
                } else if (sum > target) {
                    // Decrement end if sum is greater than target
                    end--;
                } else {
                    // Return early if sum equals target
                    return sum;
                }
            }
        }
        return var1;
    }
}
