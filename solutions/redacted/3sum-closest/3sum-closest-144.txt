class Solution {

    public int method1(int[] nums, int target) {
        Arrays.sort(nums);
        int var1 = Integer.MAX_VALUE;
        for (int i = 0; i < nums.length; i++) {
            int l = i + 1;
            int r = nums.length - 1;
            while (l < r) {
                int current_sum = nums[i] + nums[l] + nums[r];
                if (Math.abs(current_sum - target) < Math.abs(var1 - target)) {
                    var1 = current_sum;
                } else if (current_sum > target)
                    r--;
                else if (current_sum < target)
                    l++;
                else
                    return current_sum;
            }
        }
        return var1;
    }
}
