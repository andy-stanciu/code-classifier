class Solution {

    public static int method1(int[] nums) {
        Arrays.sort(nums);
        int el = 1;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] == el) {
                el++;
            }
        }
        return el;
    }
}
