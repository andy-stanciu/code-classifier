class Solution {

    public void method1(int n, int k, ArrayList<Integer> temp, int count, int index, List<List<Integer>> ans) {
        if (count == k) {
            ans.add(new ArrayList<>(temp));
            return;
        }
        if (index > n) {
            return;
        }
        temp.add(index);
        method1(n, k, temp, count + 1, index + 1, ans);
        temp.remove(temp.size() - 1);
        method1(n, k, temp, count, index + 1, ans);
    }

    public List<List<Integer>> combine(int n, int k) {
        List<List<Integer>> ans = new ArrayList<>();
        method1(n, k, new ArrayList<>(), 0, 1, ans);
        return ans;
    }
}
