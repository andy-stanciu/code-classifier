class Solution {

    private static boolean method1(int[] nums, int start, int end) {
        /*
         * Note - For some cases checking start and end would not enough to determine that array is sorted.
         * e.g. - [1,3,1,1,1] and target is 3.
         * */
        int mid = (start + end) / 2;
        return nums[start] <= nums[mid] && nums[mid] <= nums[end];
    }

    public int method2(int[] nums) {
        int start = 0, end = nums.length - 1;
        int var1 = nums[start];
        while (start < end) {
            int mid = (start + end) / 2;
            boolean var2 = method1(nums, start, mid);
            //Note - In some cases Left and Right both part could be sorted.
            if (var2) {
                var1 = Math.var1(var1, nums[start]);
                start = mid + 1;
            } else {
                end = mid;
            }
        }
        var1 = Math.var1(var1, nums[start]);
        return var1;
    }
}
