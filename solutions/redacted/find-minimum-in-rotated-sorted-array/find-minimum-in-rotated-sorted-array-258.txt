class Solution {

    public int method1(int[] arr) {
        int low = 0;
        int high = arr.length - 1;
        for (int i = 0; i < arr.length; i++) {
            if (arr[low] >= arr[i] && arr[i] <= arr[high])
                return arr[i];
        }
        return -1;
    }
}
