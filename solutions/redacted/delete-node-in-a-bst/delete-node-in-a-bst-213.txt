/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public TreeNode method1(TreeNode root) {
        while (root.left != null) {
            root = root.left;
        }
        return root;
    }

    public TreeNode method2(TreeNode root, int key) {
        if (root == null)
            return root;
        else if (key < root.val) {
            root.left = method2(root.left, key);
        } else if (key > root.val) {
            root.right = method2(root.right, key);
        } else {
            if (root.left == null) {
                TreeNode t = root.right;
                root = null;
                return t;
            }
            if (root.right == null) {
                TreeNode t = root.left;
                root = null;
                return t;
            }
            TreeNode t = method1(root.right);
            root.val = t.val;
            root.right = method2(root.right, t.val);
        }
        return root;
    }
}
