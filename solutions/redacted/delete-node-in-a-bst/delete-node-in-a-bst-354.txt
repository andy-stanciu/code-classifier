/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public TreeNode method1(TreeNode root, int key) {
        if (root == null) {
            return null;
        }
        if (key > root.val) {
            root.right = method1(root.right, key);
        }
        if (key < root.val) {
            root.left = method1(root.left, key);
        }
        if (root.val == key) {
            //leaf node deletion
            if (root.left == null && root.right == null) {
                return null;
            }
            // single child node
            if (root.left == null) {
                return root.right;
            }
            if (root.right == null) {
                return root.left;
            }
            // having two child
            if (root.left != null && root.right != null) {
                TreeNode nn = method2(root.right);
                // replaced the root value with nn
                root.val = nn.val;
                //now deleting the original nn node
                root.right = method1(root.right, nn.val);
            }
        }
        return root;
    }

    public static TreeNode method2(TreeNode root) {
        while (root.left != null) {
            root = root.left;
        }
        return root;
    }
}
