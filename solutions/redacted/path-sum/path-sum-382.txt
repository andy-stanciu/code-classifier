class Solution {

    boolean var1 = false;

    int var2;

    public boolean method1(TreeNode root, int var2) {
        this.var2 = var2;
        method2(root, 0);
        return var1;
    }

    private void method2(TreeNode node, int param1) {
        if (this.var1 || node == null) {
            // we already found target path sum
            // OR node is null
            return;
        }
        int var3 = node.val + param1;
        if (node.left == null && node.right == null) {
            if (var3 == this.var2) {
                var1 = true;
            }
        } else {
            method2(node.left, var3);
            method2(node.right, var3);
        }
    }
}
