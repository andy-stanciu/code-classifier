/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public boolean method1(TreeNode root, int param1) {
        return method2(root, param1, 0);
    }

    public boolean method2(TreeNode root, int target, int curr) {
        if (root == null) {
            return false;
        }
        curr += root.val;
        if (root.left == null && root.right == null && curr == target) {
            return true;
        }
        return method2(root.left, target, curr) || method2(root.right, target, curr);
    }
}
