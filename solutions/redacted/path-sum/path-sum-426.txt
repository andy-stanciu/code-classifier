class Solution {

    public boolean method1(TreeNode root, int param1) {
        if (root == null)
            return false;
        return method2(root, 0, param1);
    }

    private static boolean method2(TreeNode node, int param2, int param1) {
        if (node.right == null && node.left == null) {
            param2 = param2 + node.val;
            return param2 == param1;
        } else {
            boolean var1 = (node.right != null) ? method2(node.right, param2 + node.val, param1) : false;
            boolean var2 = (node.left != null) ? method2(node.left, param2 + node.val, param1) : false;
            return var1 || var2;
        }
    }
}
