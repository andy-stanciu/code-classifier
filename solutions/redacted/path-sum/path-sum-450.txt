/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public boolean method1(TreeNode root, int param1) {
        if (root == null) {
            return false;
        }
        return method2(root, 0, param1);
    }

    private boolean method2(TreeNode node, int current, int target) {
        current += node.val;
        if (node.left == null && node.right == null && current == target) {
            return true;
        }
        if (node.left != null && method2(node.left, current, target)) {
            return true;
        }
        if (node.right != null && method2(node.right, current, target)) {
            return true;
        }
        return false;
    }
}
