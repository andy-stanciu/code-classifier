class Solution {

    public boolean method1(String s, String t) {
        int j = 0;
        for (int i = 0; j < s.length() && i < t.length(); i++) {
            if (t.charAt(i) == s.charAt(j)) {
                j++;
            }
        }
        return j == s.length();
    }
}
