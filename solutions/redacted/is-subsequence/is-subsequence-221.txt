class Solution {

    public boolean method1(String s, String t) {
        int j = 0, i = 0;
        if (s.length() == 0)
            return true;
        while (i < t.length() && j < s.length()) {
            if (t.charAt(i) == s.charAt(j))
                j++;
            i++;
        }
        return j == s.length();
    }
}
