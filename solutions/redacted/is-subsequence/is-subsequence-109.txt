public class Solution {

    public bool method1(string s, string t) {
        int s_i = 0, t_i = 0;
        while (s_i < s.Length && t_i < t.Length) {
            if (s[s_i] == t[t_i])
                s_i++;
            t_i++;
        }
        return s_i == s.Length;
    }
}
