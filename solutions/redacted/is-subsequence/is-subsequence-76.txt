class Solution {

    public boolean method1(String s, String t) {
        int i = t.length() - 1, j = s.length() - 1;
        for (; i > -1 && j > -1; i--) if (t.charAt(i) == s.charAt(j))
            j--;
        return j == -1;
    }
}
