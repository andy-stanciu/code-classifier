class Solution {

    public boolean method1(String s, String t) {
        return method1(s, t, 0, 0);
    }

    public boolean method1(String s, String t, int sCount, int tCount) {
        if (sCount == s.length())
            return true;
        for (int i = tCount; i < t.length(); i++) {
            if (t.charAt(i) == s.charAt(sCount)) {
                return method1(s, t, sCount + 1, i + 1);
            }
        }
        return false;
    }
}
