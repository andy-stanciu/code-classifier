class Solution {

    public String convert(String s, int numRows) {
        // Base case: if there is only one row, return the original string
        if (numRows == 1) {
            return s;
        }
        StringBuilder result = new StringBuilder();
        int length = s.length();
        int totalDistance = 2 * (numRows - 1);
        for (int row = 0; row < numRows; row++) {
            // Step between characters in the same row
            int step = totalDistance - 2 * row;
            for (int index = row; index < length; index += totalDistance) {
                result.append(s.charAt(index));
                // For middle rows, add the characters that fall between the start and end characters
                if (row > 0 && row < numRows - 1 && step < length) {
                    int middleIndex = index + step;
                    if (middleIndex < length) {
                        result.append(s.charAt(middleIndex));
                    }
                }
            }
        }
        return result.toString();
    }
}
