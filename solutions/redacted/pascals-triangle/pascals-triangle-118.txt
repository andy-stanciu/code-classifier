class Solution {

    public List<List<Integer>> generate(int numRows) {
        List<List<Integer>> list = new ArrayList<>();
        // Initiate rows with row + 1 elements
        for (int row = 0; row < numRows; row++) {
            List<Integer> current_row = Arrays.asList(new Integer[row + 1]);
            // At the start and at the end of each item in the current row arrray, the value should be 1
            current_row.set(0, 1);
            current_row.set(row, 1);
            // Performing the calculation transition as mention earlier
            for (int col = 1; col < row; col++) {
                int value = list.get(row - 1).get(col) + list.get(row - 1).get(col - 1);
                current_row.set(col, value);
            }
            // Adding the current row to the list
            list.add(current_row);
        }
        return list;
    }
}
