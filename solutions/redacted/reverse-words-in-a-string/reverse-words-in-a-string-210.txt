class Solution {

    public String method1(String s) {
        String[] s1 = s.trim().replaceAll("\\\\s+", " ").split(" ");
        String ans = "";
        for (int i = s1.length - 1; i > 0; i--) {
            ans += s1[i] + " ";
        }
        return ans + s1[0];
    }
}
