//Using In-Built Libraries
class Solution {

    public String method1(String s) {
        s = s.trim();
        List<String> var1 = Arrays.asList(s.split("\\\\s+"));
        Collections.reverse(var1);
        return String.join(" ", var1);
    }
}
