class Solution {

    public String method1(String s) {
        String[] a = s.trim().split("\\\\s+");
        String res = "";
        for (int i = a.length - 1; i > 0; i--) {
            res += a[i] + " ";
        }
        return res + a[0];
    }
}
