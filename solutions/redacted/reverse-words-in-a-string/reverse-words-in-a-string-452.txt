class Solution {

    public String method1(String s) {
        String[] whole = s.trim().split("\\\\s+");
        String out = "";
        for (int i = whole.length - 1; i > 0; i--) {
            out += whole[i] + " ";
        }
        return out + whole[0];
    }
}
