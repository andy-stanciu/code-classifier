class Solution {

    public String method1(String s) {
        String[] str = s.trim().split("\\\\s+");
        String rev_word = "";
        for (int i = str.length - 1; i >= 0; i--) {
            rev_word += str[i] + " ";
        }
        return rev_word.trim();
    }
}
