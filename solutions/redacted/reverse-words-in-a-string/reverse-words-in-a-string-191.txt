class Solution {

    public String method1(String s) {
        List<String> outList = Stream.of(s.split(" ")).map(word -> word.trim()).filter(word -> word.length() > 0).collect(Collectors.toList());
        Collections.reverse(outList);
        return String.join(" ", outList);
    }
}
