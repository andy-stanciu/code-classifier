class Solution {

    public String method1(String s) {
        s = s.trim();
        final List<String> split = Arrays.asList(s.split("[ ]+"));
        Collections.reverse(split);
        return String.join(" ", split);
    }
}
