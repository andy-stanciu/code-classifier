class Solution {

    public String method1(String s) {
        //
        String[] arr = s.trim().split("\\\\s+");
        int len = arr.length;
        for (// reverse the order of the words in array
        int i = 0; // reverse the order of the words in array
        i < len / 2; // reverse the order of the words in array
        i++) {
            String tmp = arr[i];
            arr[i] = arr[len - (i + 1)];
            arr[len - (i + 1)] = tmp;
        }
        // convert to string
        return String.join(" ", arr);
    }
}
