class Solution {

    public int compress(char[] chars) {
        int ptr = -1;
        for (int i = 0; i < chars.length; i++) {
            char c = chars[i];
            int counter = 1;
            while (i + 1 < chars.length && chars[i] == chars[i + 1]) {
                counter++;
                i++;
            }
            if (counter > 1) {
                ptr++;
                chars[ptr] = chars[i];
                String var1 = Integer.toString(counter);
                for (int x = 0; x < var1.length(); x++) {
                    ptr++;
                    chars[ptr] = var1.charAt(x);
                }
            } else {
                ptr++;
                chars[ptr] = chars[i];
            }
        }
        return ptr + 1;
    }
}
