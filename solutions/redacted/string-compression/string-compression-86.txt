class Solution {

    public int compress(char[] word) {
        StringBuilder var1 = new StringBuilder();
        int i = 0;
        int k = 0;
        while (i < word.length) {
            int cnt = 0;
            if (i < word.length) {
                char t = word[i];
                cnt++;
                i++;
                while (i < word.length && t == word[i]) {
                    cnt++;
                    i++;
                }
                if (cnt == 1) {
                    var1.append(word[i - 1]);
                } else {
                    var1.append(t);
                    var1.append(cnt);
                }
            }
        }
        for (int q = 0; q < var1.length(); q++) {
            word[q] = var1.charAt(q);
        }
        return var1.length();
    }
}
