class Solution {

    public int compress(char[] chars) {
        int n = chars.length;
        int temp = 0;
        int count = 1;
        for (int i = 1; i < n; i++) {
            if (chars[i] == chars[i - 1]) {
                count++;
            } else {
                chars[temp++] = chars[i - 1];
                if (count > 1) {
                    for (char c : Integer.toString(count).toCharArray()) {
                        chars[temp++] = c;
                    }
                }
                count = 1;
            }
        }
        chars[temp++] = chars[n - 1];
        if (count > 1) {
            for (char c : Integer.toString(count).toCharArray()) {
                chars[temp++] = c;
            }
        }
        return temp;
    }
}
