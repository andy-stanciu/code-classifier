class Solution {

    public ListNode method1(ListNode head) {
        ListNode T = head;
        while (head != null && head.next != null) {
            int temp = head.val;
            head.val = head.next.val;
            head.next.val = temp;
            head = head.next.next;
        }
        return T;
    }
}
