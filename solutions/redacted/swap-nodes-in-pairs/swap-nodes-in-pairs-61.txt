class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode tail = method1(head.next.next);
        ListNode temp = head;
        head = head.next;
        head.next = temp;
        temp.next = tail;
        return head;
    }
}
