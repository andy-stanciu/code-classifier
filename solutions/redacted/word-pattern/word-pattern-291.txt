class Solution {

    public boolean method1(String param1, String s) {
        String[] var1 = s.split(" ");
        if (param1.length() != var1.length) {
            // Different lengths means different patterns
            return false;
        }
        HashMap<Character, String> map = new HashMap<>();
        for (int i = 0; i < param1.length(); i++) {
            char ch = param1.charAt(i);
            String var2 = var1[i];
            if (map.containsKey(ch)) {
                if (!map.get(ch).equals(var2)) {
                    // If the pattern does not match the word
                    return false;
                }
            } else {
                if (map.containsValue(var2)) {
                    // If the same word corresponds to different patterns
                    return false;
                }
                // Add the pattern and word to the map
                map.put(ch, var2);
            }
        }
        // If the pattern and word mappings hold true
        return true;
    }
}
