class Solution {

    public boolean method1(String param1, String s) {
        String[] var1 = s.split(" ");
        char[] characters = param1.toCharArray();
        //check the size of both string if not same return false
        if (var1.length != characters.length)
            return false;
        HashMap<Character, String> map = new HashMap<>();
        for (int i = 0; i < characters.length; i++) {
            if (map.containsKey(characters[i])) {
                //contain pr match nhi kar raha phle kuchh tha ab kuchh hai
                if (!map.get(characters[i]).equals(var1[i]))
                    return false;
            } else {
                //contains nhi kra the pr iska dost phle se h to duplicate ho raha to return false;
                if (map.containsValue(var1[i]))
                    return false;
                map.put(characters[i], var1[i]);
            }
        }
        return true;
    }
}
