class Solution {

    public boolean method1(String param1, String s) {
        Map<Character, String> mp = new HashMap<>();
        int i = 0;
        String[] ss = s.split(" ");
        if (ss.length != param1.length())
            return false;
        for (String st : ss) {
            if (!mp.containsKey(param1.charAt(i))) {
                if (mp.containsValue(st))
                    return false;
                mp.put(param1.charAt(i++), st);
            } else {
                if (!mp.get(param1.charAt(i++)).equals(st))
                    return false;
            }
        }
        return true;
    }
}
