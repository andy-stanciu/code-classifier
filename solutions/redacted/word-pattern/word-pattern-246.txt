class Solution {

    public boolean method1(String param1, String s) {
        String[] str = s.split(" ");
        if (str.length != param1.length()) {
            return false;
        }
        int j = 0;
        HashMap<Character, String> mpp = new HashMap<>();
        for (int i = 0; i < str.length; i++) {
            if (mpp.containsKey(param1.charAt(i)) && !mpp.get(param1.charAt(i)).equals(str[i])) {
                return false;
            }
            if (!mpp.containsKey(param1.charAt(i))) {
                if (mpp.containsValue(str[i])) {
                    return false;
                }
                mpp.put(param1.charAt(i), str[i]);
            }
        }
        return true;
    }
}
