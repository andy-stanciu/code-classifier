class Solution {

    public int method1(int[][] param1) {
        int erase = 0;
        Stack<int[]> stack = new Stack<>();
        Arrays.sort(param1, (a, b) -> Integer.compare(a[0], b[0]));
        for (int[] temp : param1) {
            if (stack.isEmpty() || stack.peek()[1] <= temp[0]) {
                stack.add(temp);
            } else {
                if (stack.peek()[1] > temp[1]) {
                    stack.pop();
                    stack.add(new int[] { temp[0], temp[1] });
                }
                erase++;
            }
        }
        return erase;
    }
}
