class Solution {

    public int method1(int[][] param1) {
        Arrays.sort(param1, Solution::compare);
        int lastEnd = Integer.MIN_VALUE;
        int result = 0;
        for (int[] interval : param1) {
            if (interval[0] < lastEnd) {
                result++;
            } else {
                lastEnd = interval[1];
            }
        }
        return result;
    }

    private static int compare(int[] o1, int[] o2) {
        return o1[1] - o2[1];
    }
}
