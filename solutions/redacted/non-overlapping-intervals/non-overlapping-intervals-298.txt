class Solution {

    public int method1(int[][] param1) {
        Arrays.sort(param1, (a, b) -> (a[0] - b[0]));
        int preEnd = param1[0][1];
        int res = 0;
        for (int i = 1; i < param1.length; i++) {
            if (param1[i][0] < preEnd) {
                res++;
                preEnd = Math.min(preEnd, param1[i][1]);
            } else {
                preEnd = param1[i][1];
            }
        }
        return res;
    }
}
