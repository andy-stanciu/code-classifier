class Solution {

    public int method1(int[][] param1) {
        Arrays.sort(param1, (a, b) -> a[1] - b[1]);
        int cur = 0, res = 0;
        for (int i = 1; i < param1.length; i++) {
            if (param1[i][0] >= param1[cur][1])
                cur = i;
            else
                res++;
        }
        return res;
    }
}
