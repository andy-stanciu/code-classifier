class Solution {

    public int method1(int[][] in) {
        Arrays.sort(in, (a, b) -> a[1] - b[1]);
        int res = -1, p[] = in[0];
        for (int[] i : in) if (i[0] < p[1])
            res++;
        else
            p = i;
        return res;
    }
}
