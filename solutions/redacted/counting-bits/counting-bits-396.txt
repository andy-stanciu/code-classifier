class Solution {

    public int[] method1(int n) {
        int len = n + 1;
        int[] arr = new int[len];
        arr[0] = 0;
        for (int j = 1; j < len; j++) arr[j] = method2(j);
        return arr;
    }

    private int method2(int j) {
        int count = 0;
        for (int i = 0; i < 32; i++) if ((j & (1 << i)) > 0)
            count++;
        return count;
    }
}
