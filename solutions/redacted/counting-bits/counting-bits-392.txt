class Solution {

    public int[] method1(int n) {
        int[] ans = new int[n + 1];
        for (int i = 1; i <= n; i++) {
            int temp = i;
            int count = 0;
            while (temp != 0) {
                //removes rightmost 1
                temp = temp & (temp - 1);
                count++;
            }
            ans[i] = count;
        }
        return ans;
    }
}
