class Solution {

    public int[] method1(int n) {
        int[] dp = new int[n + 1];
        //dp[i] = dp[i >> 1] + i & 1;
        for (int i = 1; i < n + 1; i++) {
            dp[i] = dp[i >> 1] + (i & 1);
        }
        return dp;
    }
}
