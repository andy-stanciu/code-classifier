class Solution {

    public int[] method1(int n) {
        int[] ans = new int[n + 1];
        for (int i = 1; i <= n; i++) {
            // Use the property: ans[i] = ans[i / 2] + (i % 2);
            ans[i] = ans[i >> 1] + (i & 1);
        }
        return ans;
    }
}
