class Solution {

    public int[] method1(int n) {
        return IntStream.range(0, n + 1).map(value -> (int) Arrays.stream(Integer.toBinaryString(value).split("")).filter(binaryStr -> binaryStr.equals("1")).count()).toArray();
    }
}
