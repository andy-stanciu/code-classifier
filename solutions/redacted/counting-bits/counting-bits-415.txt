class Solution {

    public int[] method1(int n) {
        int[] arr = new int[n + 1];
        Arrays.fill(arr, 0);
        for (int i = 1; i <= n; i++) {
            arr[i] = method2(i);
        }
        return arr;
    }

    int method2(int x) {
        int c = 0;
        int temp = 1;
        while (x > 0) {
            temp = x & 1;
            if (temp != 0) {
                c++;
            }
            x = x >> 1;
        }
        return c;
    }
}
