class Solution {

    public int[] method1(int n) {
        //using bitCount method
        int[] arr = new int[n + 1];
        for (int i = 0; i < n + 1; i++) {
            arr[i] = Integer.bitCount(i);
        }
        return arr;
    }
}
