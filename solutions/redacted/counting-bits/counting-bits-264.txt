class Solution {

    public int[] method1(int n) {
        int[] arr = new int[n + 1];
        Arrays.fill(arr, -1);
        arr[0] = 0;
        for (int i = 0; i <= n; i++) {
            int val = (i % 2 == 0) ? 0 : 1;
            arr[i] = arr[i / 2] + val;
        }
        return arr;
    }
}
