class Solution {

    public int[] method1(int n) {
        int[] output = new int[n + 1];
        output[0] = 0;
        for (int i = 1; i <= n; i++) {
            output[i] = output[i / 2] + (i % 2);
        }
        return output;
    }
}
