class Solution {

    public boolean method1(int[] nums) {
        int g = nums.length - 1;
        for (int i = g; i >= 0; i--) {
            if (i + nums[i] >= g) {
                g = i;
            }
        }
        return g == 0;
    }
}
