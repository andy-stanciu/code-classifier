class Solution {

    public boolean method1(int[] nums) {
        int dest = nums.length - 1, pos = dest - 1;
        while (pos >= 0) {
            if (pos + nums[pos] >= dest) {
                dest = pos;
            }
            pos--;
        }
        return dest == 0;
    }
}
