class Solution {

    private boolean method1(int[] nums, boolean[] visited, int pos) {
        if (visited[pos] == true) {
            return false;
        }
        if (pos + nums[pos] >= nums.length - 1) {
            return true;
        }
        for (int i = nums[pos]; i >= 1; i--) {
            if (method1(nums, visited, i + pos)) {
                return true;
            }
        }
        visited[pos] = true;
        return false;
    }

    public boolean method2(int[] nums) {
        boolean[] visited = new boolean[nums.length];
        return method1(nums, visited, 0);
    }
}
