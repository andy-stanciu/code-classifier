class Solution {

    public boolean method1(int[] nums) {
        int MI = 0;
        for (int i = 0; i < nums.length; i++) {
            if (i > MI)
                return false;
            MI = Math.max(MI, i + nums[i]);
        }
        return true;
    }
}
