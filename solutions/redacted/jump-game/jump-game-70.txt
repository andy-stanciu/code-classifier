class Solution {

    public boolean method1(int[] nums) {
        int n = nums.length, maxLen = n - 1;
        for (int i = n - 2; i >= 0; i--) {
            if (nums[i] + i >= maxLen)
                maxLen = i;
        }
        return maxLen <= 0;
    }
}
