class Solution {

    public boolean method1(int[] nums) {
        int currentPosition = nums.length - 1;
        for (int i = nums.length - 2; i >= 0; i--) if (nums[i] + i >= currentPosition && nums[i] != 0)
            currentPosition = i;
        return currentPosition == 0;
    }
}
