class Solution {

    public int method1(int[] param1, int amount) {
        int[][] t = new int[param1.length + 1][amount + 1];
        for (int i = 0; i < t.length; i++) {
            for (int j = 0; j < t[0].length; j++) {
                if (j == 0)
                    t[i][j] = 0;
                else if (i == 0)
                    t[i][j] = Integer.MAX_VALUE - 1;
                else if (param1[i - 1] <= j)
                    t[i][j] = Math.min(1 + t[i][j - param1[i - 1]], t[i - 1][j]);
                else
                    t[i][j] = t[i - 1][j];
            }
        }
        if (t[param1.length][amount] >= Integer.MAX_VALUE - 1)
            return -1;
        else
            return t[param1.length][amount];
    }
}
