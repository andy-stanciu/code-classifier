class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int result = nums[0];
        for (int num : nums) {
            sum = sum + num;
            result = Math.max(result, sum);
            if (sum < 0) {
                sum = 0;
            }
        }
        return result;
    }
}
