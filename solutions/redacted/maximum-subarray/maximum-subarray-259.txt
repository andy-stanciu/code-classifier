class Solution {

    public int method1(int[] n) {
        int d = n[0], m = n[0];
        for (int i = 1; i < n.length; i++) m = Math.max(d = (d > 0 ? d + n[i] : n[i]), m);
        return m;
    }
}
