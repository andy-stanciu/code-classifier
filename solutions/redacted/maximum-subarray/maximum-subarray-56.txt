class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int maxi = nums[0];
        for (int num : nums) {
            sum += num;
            if (sum > maxi)
                maxi = sum;
            if (sum < 0)
                sum = 0;
        }
        return maxi;
    }
}
