class Solution {

    public int method1(int[] nums) {
        int max = Integer.MIN_VALUE;
        int sum = 0;
        for (int element : nums) {
            sum += element;
            if (sum > max)
                max = sum;
            if (sum < 0)
                sum = 0;
        }
        return max;
    }
}
