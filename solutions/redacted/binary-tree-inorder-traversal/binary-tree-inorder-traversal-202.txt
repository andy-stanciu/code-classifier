class Solution {

    public List<Integer> method1(TreeNode root) {
        List<Integer> result = new ArrayList<>();
        method1(root, result);
        return result;
    }

    private void method1(TreeNode node, List<Integer> result) {
        if (node == null) {
            return;
        }
        method1(node.left, result);
        result.add(node.val);
        method1(node.right, result);
    }
}
