class Solution {

    List<Integer> values = new ArrayList<Integer>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return values;
        method1(root.left);
        values.add(root.val);
        method1(root.right);
        return values;
    }
}
