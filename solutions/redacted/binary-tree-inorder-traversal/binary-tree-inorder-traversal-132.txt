class Solution {

    List<Integer> inList = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root != null) {
            method1(root.left);
            inList.add(root.val);
            method1(root.right);
        }
        return inList;
    }
}
