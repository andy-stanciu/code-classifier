class Solution {

    public int method1(int[] nums) {
        int var1 = 0;
        for (int i = 0; i < nums.length; i++) {
            var1 = var1 ^ nums[i];
            /*Using XOR operation, duplicate elements will be ignored & it returns single element*/
        }
        return var1;
    }
}
