class Solution {

    public int method1(int[] nums) {
        return Arrays.stream(nums).boxed().collect(Collectors.toMap(Function.identity(), i -> 1, (a, b) -> a + 1, HashMap::new)).entrySet().stream().filter(i -> i.getValue() < 2).findFirst().map(Map.Entry::getKey).get();
    }
}
