class Solution {

    public int method1(int[] nums) {
        // Initialize
        int uniqNum = 0;
        for (int idx : nums) {
            //  XOR
            uniqNum ^= idx;
        }
        return uniqNum;
    }
}
