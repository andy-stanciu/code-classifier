class Solution {

    public int method1(int[] nums) {
        int result = 0;
        for (int num : nums) {
            result = result ^ num;
        }
        return result;
    }
    /*
    HashMap<Integer,Integer> memo = new HashMap<>();
        for (int i = 0; i < nums.length ; i++) {
            int current = nums[i];
            if(memo.containsKey(current))
                memo.put(current,memo.get(current)+1);
            else memo.put(current,1);
        }
        for(Map.Entry<Integer,Integer> entry : memo.entrySet()){
            if(entry.getValue() == 1)
                return entry.getKey();
        }
        return 0;
     */
}
