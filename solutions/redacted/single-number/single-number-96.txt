class Solution {

    public int method1(int[] nums) {
        int var1 = 0;
        for (int i = 0; i < nums.length; i++) {
            var1 = var1 ^ nums[i];
        }
        return var1;
        //brute force -- linear search type
        // for(int i = 0 ; i < nums.length; i++){
        //     int count = 0;
        //     for(int j = 0 ;j< nums.length ;j++){
        //         if(nums[j] == nums[i])
        //             count++ ;
        //    }
        //     if(count == 1)
        //         return nums[i];
        // }
        // return -1;
    }
}
