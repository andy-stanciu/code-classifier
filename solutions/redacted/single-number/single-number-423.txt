class Solution {

    public int method1(int[] nums) {
        if (nums.length == 1)
            return nums[0];
        int ans = 0;
        for (int i : nums) ans = ans ^ i;
        return ans;
    }
}
