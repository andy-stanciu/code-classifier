class Solution {

    public int method1(int[] nums) {
        // first value for XOR
        int result = 0;
        for (int num : nums) {
            // Add XOR values
            result ^= num;
        }
        //return last XOR value
        return result;
    }
}
