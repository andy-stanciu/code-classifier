class Solution {

    public int maxProfit(int[] prices) {
        int var1 = 0;
        int hold = Integer.MIN_VALUE;
        for (int price : prices) {
            var1 = Math.max(var1, hold + price);
            hold = Math.max(hold, var1 - price);
        }
        return var1;
    }
}
