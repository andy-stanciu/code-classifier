class Solution {

    public int maxProfit(int[] prices) {
        int var1 = 0;
        for (int i = 1; i < prices.length; i++) {
            // If the price on the next day is higher than today, we can make a profit
            if (prices[i] > prices[i - 1]) {
                // Add the profit (difference) to the total profit
                var1 += prices[i] - prices[i - 1];
            }
        }
        return var1;
    }
}
