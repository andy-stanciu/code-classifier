class Solution {

    public int maxProfit(int[] prices) {
        Stack<Integer> st = new Stack<>();
        int sum = 0;
        for (int i : prices) {
            if (!st.isEmpty() && i > st.peek()) {
                sum += i - st.peek();
            }
            st.add(i);
        }
        return sum;
    }
}
