class Solution {

    public int maxProfit(int[] prices) {
        int var1 = 0;
        for (int i = 1; i < prices.length; i++) {
            var1 += Math.max(0, prices[i] - prices[i - 1]);
        }
        return var1;
    }
}
