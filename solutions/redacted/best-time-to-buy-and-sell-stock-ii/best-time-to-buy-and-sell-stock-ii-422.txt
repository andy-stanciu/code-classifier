class Solution {

    public int maxProfit(int[] a) {
        int maxProfit = 0;
        int var1 = a[0];
        for (int i = 1; i < a.length; i++) {
            int running_profit = 0;
            if (a[i] > var1) {
                running_profit = a[i] - var1;
                maxProfit = Math.max(maxProfit, maxProfit + running_profit);
            }
            var1 = a[i];
        }
        return maxProfit;
    }
}
