class Solution {

    public int method1(int[] nums, int k) {
        // Use length to get the size of the array
        int n = nums.length;
        // Correct the sort method to use Arrays.sort()
        Arrays.sort(nums);
        // Return the k-th largest element
        return nums[n - k];
    }
}
