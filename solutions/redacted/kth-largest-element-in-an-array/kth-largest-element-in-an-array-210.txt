class Solution {

    public int method1(int[] nums, int k) {
        PriorityQueue<Integer> p = new PriorityQueue<>();
        for (int n : nums) {
            p.add(n);
            if (p.size() > k)
                p.poll();
        }
        return p.peek();
    }
}
