class Solution {

    public int method1(int[] nums, int k) {
        Queue<Integer> pq = new PriorityQueue<>();
        for (Integer i : nums) {
            pq.add(i);
            if (pq.size() > k) {
                pq.poll();
            }
        }
        return pq.peek();
    }
}
