class Solution {

    public int method1(int[] nums, int k) {
        // PriorityQueue<Integer>ans=new PriorityQueue<>();
        int n = nums.length;
        // for(int i=0;i<n;i++){
        // ans.add(nums[i]);
        // if(ans.size()>k){
        //     ans.remove();
        // }
        // }
        // return ans.peek();
        Arrays.sort(nums);
        return nums[n - k];
    }
}
