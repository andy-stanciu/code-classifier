class Solution {

    public int method1(int[] nums, int k) {
        int n = nums.length;
        PriorityQueue<Integer> var1 = new PriorityQueue<>();
        for (int i = 0; i < n; i++) {
            var1.add(nums[i]);
            if (var1.size() > k) {
                var1.poll();
            }
        }
        return var1.peek();
    }
}
