class Solution {

    public int method1(int[] nums, int k) {
        PriorityQueue<Integer> numbers = new PriorityQueue<>(k);
        for (int i : nums) {
            // OR numbers.add(i);
            numbers.offer(i);
            if (numbers.size() > k)
                numbers.poll();
        }
        return numbers.peek();
    }
}
