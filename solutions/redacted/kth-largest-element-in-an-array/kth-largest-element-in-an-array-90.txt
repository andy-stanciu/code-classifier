class Solution {

    public int method1(int[] nums, int k) {
        PriorityQueue<Integer> pq = new PriorityQueue<>(Comparator.reverseOrder());
        for (int curr : nums) pq.add(curr);
        int var1 = -1;
        while (k > 0) {
            var1 = pq.remove();
            k--;
        }
        return var1;
    }
}
