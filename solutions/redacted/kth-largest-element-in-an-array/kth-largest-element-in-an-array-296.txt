class Solution {

    public int method1(int[] nums, int k) {
        //Sort the array
        Arrays.sort(nums);
        int n = nums.length;
        //Find the index of the k-largest element
        int var1 = n - k;
        //Return the k largest element
        return nums[var1];
    }
}
