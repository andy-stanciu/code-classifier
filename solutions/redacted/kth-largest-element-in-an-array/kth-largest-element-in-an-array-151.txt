class Solution {

    public int method1(int[] nums, int k) {
        Queue<Integer> var1 = new PriorityQueue<>();
        for (int n : nums) {
            var1.add(n);
            if (var1.size() > k) {
                var1.remove();
            }
        }
        return var1.peek();
    }
}
