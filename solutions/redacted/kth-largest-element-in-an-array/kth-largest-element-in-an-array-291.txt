class Solution {

    public int method1(int[] nums, int k) {
        PriorityQueue<Integer> maxheap = new PriorityQueue<>((a, b) -> b - a);
        for (int num : nums) {
            maxheap.add(num);
        }
        for (int i = 1; i < k; i++) {
            maxheap.poll();
        }
        return maxheap.peek();
    }
}
