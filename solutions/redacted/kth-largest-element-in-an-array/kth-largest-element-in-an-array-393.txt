class Solution {

    public int method1(int[] nums, int k) {
        int n = nums.length;
        Arrays.sort(nums);
        for (int i = 0; i < n; i++) {
            return nums[n - k];
        }
        return 1;
    }
}
