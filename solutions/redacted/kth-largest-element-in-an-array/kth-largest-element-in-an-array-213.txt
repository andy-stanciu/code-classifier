class Solution {

    public int method1(int[] nums, int k) {
        Arrays.sort(nums);
        // for(int i = nums.length; i<= 0; i--) {
        // for(int i = 0 ; i < nums.length; i++) {
        int ri = nums.length - k;
        return nums[ri];
        //     if(k == i+1)
        //     {
        //         return nums[i];
        //     }
        // }
        // return -1;
    }
}
