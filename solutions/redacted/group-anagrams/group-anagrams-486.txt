class Solution {

    public List<List<String>> method1(String[] strs) {
        Map<String, List<String>> var1 = new HashMap<>();
        for (String str : strs) {
            char[] charArray = str.toCharArray();
            Arrays.sort(charArray);
            String sortedStr = new String(charArray);
            // Add the word to the list corresponding to its sorted version in the map
            var1.computeIfAbsent(sortedStr, key -> new ArrayList<>()).add(str);
        }
        // Return the values of the map as a list of lists
        return new ArrayList<>(var1.values());
    }
}
