class Solution {

    public List<List<String>> method1(String[] strs) {
        /*

        My solution with Mistakes:
        
       HashMap<String, List<String>> hm = new HashMap<>();

        for(String s: strs){
            char[] chars = s.toCharArray(); // Convert the string to a character array
            Arrays.sort(chars);              // Sort the character array
            String sorted = new String(chars); // Convert back to a string

            if(hm.containsKey(sorted)){
                hm.put(sorted, hm.get(s));
            }
            hm.put(sorted, hm.get(s));

            // Return all values as a list of lists
                }
        return new ArrayList<>(hm.values());
        */
        HashMap<String, List<String>> hm = new HashMap<>();
        for (String s : strs) {
            // Convert the string to a character array
            char[] chars = s.toCharArray();
            // Sort the character array
            Arrays.sort(chars);
            // Convert back to a string
            String sorted = new String(chars);
            // Check if the key already exists in the map
            if (!hm.containsKey(sorted)) {
                // If it doesn\'t exist, add a new key with an empty list as its value
                hm.put(sorted, new ArrayList<>());
            }
            // Add the current string `s` to the list associated with the `sorted` key
            hm.get(sorted).add(s);
        }
        return new ArrayList<>(hm.values());
    }
}
