class Solution {

    public List<List<String>> method1(String[] strs) {
        return new // Use Java Streams to group anagrams based on sorted character representation
        ArrayList<>(Arrays.stream(strs).collect(Collectors.groupingBy(str -> str.chars().sorted().collect(StringBuilder::new, StringBuilder::appendCodePoint, StringBuilder::append).toString())).values());
    }
}
