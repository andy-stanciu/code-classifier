class Solution {

    public List<Integer> method1(int[] nums) {
        // Create a frequency map to store the count of each element
        Map<Integer, Integer> var1 = new HashMap<>();
        // Iterate through the input array to count element occurrences
        for (int i = 0; i < nums.length; i++) {
            var1.put(nums[i], var1.getOrDefault(nums[i], 0) + 1);
        }
        List<Integer> var2 = new ArrayList<>();
        int threshold = nums.length / 3;
        // Iterate through the frequency map to identify majority elements
        for (Map.Entry<Integer, Integer> entry : var1.entrySet()) {
            int var3 = entry.getKey();
            int count = entry.getValue();
            // Check if the element count is greater than the threshold
            if (count > threshold) {
                var2.add(var3);
            }
        }
        return var2;
    }
}
