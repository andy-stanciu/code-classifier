class Solution {

    public boolean method1(int[] nums, int k) {
        int ns = nums.length;
        for (int i = 0; i < ns; i++) for (int j = i + 1; j < ns && j - i <= k; j++) if (nums[i] == nums[j])
            return true;
        return false;
    }
}
