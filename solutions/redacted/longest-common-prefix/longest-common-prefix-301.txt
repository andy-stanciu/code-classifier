class Solution {

    String method1(String s, String t) {
        String lg = "";
        int b = 1;
        int m = Math.min(s.length(), t.length());
        for (int i = 0; i < m; i++) {
            if (s.charAt(i) != t.charAt(i)) {
                b++;
                return s.substring(0, i);
            }
        }
        if (b != 1)
            return lg;
        return s.substring(0, m);
    }

    public String method2(String[] strs) {
        if (strs.length == 1)
            return strs[0];
        else {
            String st = "";
            String ss = method1(strs[0], strs[1]);
            if (ss == st)
                return ss;
            else {
                String sm = method1(strs[0], strs[1]);
                for (int i = 1; i < strs.length - 1; i++) {
                    st = method1(sm, strs[i + 1]);
                    sm = st;
                }
                return sm;
            }
        }
    }
}
