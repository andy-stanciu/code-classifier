class Solution {

    public String method1(String[] strs) {
        if (strs == null || strs.length == 0)
            return "";
        String var1 = strs[0];
        int len = strs.length;
        for (int i = 1; i < len; i++) {
            while (!strs[i].startsWith(var1)) {
                var1 = var1.substring(0, var1.length() - 1);
                if (var1.isEmpty())
                    return "";
            }
        }
        return var1;
    }
}
