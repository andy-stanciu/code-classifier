class Solution {

    public boolean method1(int n) {
        int result = n;
        while (result != 1 && result != 4) {
            result = method2(result);
        }
        if (result == 1)
            return true;
        else
            return false;
    }

    private int method2(int n) {
        int sum = 0;
        while (n > 0) {
            int dig = n % 10;
            sum += dig * dig;
            n /= 10;
        }
        return sum;
    }
}
