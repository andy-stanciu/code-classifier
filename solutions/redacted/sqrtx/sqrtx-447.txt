class Solution {

    public int mySqrt(int x) {
        if (x < 0) {
            return -1;
        }
        double sqrt = Math.sqrt(x);
        int roundednum = (int) Math.floor(sqrt);
        return roundednum;
    }
}
