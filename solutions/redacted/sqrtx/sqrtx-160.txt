class Solution {

    public int mySqrt(int x) {
        long out = 0;
        for (int i = 16; i >= 0; i--) {
            long alt = out | (1 << i);
            out = alt * alt <= x ? alt : out;
        }
        return (int) out;
    }
}
