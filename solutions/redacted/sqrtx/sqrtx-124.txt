class Solution {

    public static int mySqrt(int s) {
        double x = 6;
        for (int i = 0; i < 10; i++) {
            double a = (s - x * x) / (2 * x);
            double b = x + a;
            x = b - a * a / (2 * b);
        }
        return (int) x;
    }
}
