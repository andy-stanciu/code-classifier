class Solution {

    public static int mySqrt(int x) {
        int i = 1, c = 0;
        if (x > 0) {
            while (x != 0 && i <= x) {
                x = x - i;
                i = i + 2;
                c++;
            }
            return c;
        } else
            return 0;
    }
}
