class Solution {

    public int mySqrt(int x) {
        if (x < 0 || x > Integer.MAX_VALUE) {
            return 0;
        } else {
            double s = Math.sqrt(x);
            return ((int) s);
        }
    }
}
