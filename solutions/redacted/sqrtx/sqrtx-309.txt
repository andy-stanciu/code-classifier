class Solution {

    public int mySqrt(int x) {
        if (x == 0 || x == 1)
            return x;
        double xn, xn1 = x / 2;
        do {
            xn = xn1;
            xn1 = (xn + (x / xn)) / 2;
        } while (xn != xn1);
        return (int) xn;
    }
}
