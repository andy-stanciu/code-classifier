class Solution {

    public int mySqrt(int x) {
        // made a varible root with double datatype
        double root;
        // used math.sqrt() function to find the root
        root = Math.sqrt(x);
        // made a second variable value to convert double to int
        int value = (int) root;
        // returned the value
        return value;
    }
}
