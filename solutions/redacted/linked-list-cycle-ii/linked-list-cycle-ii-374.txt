public class Solution {

    public ListNode method1(ListNode head) {
        var set = new HashSet<ListNode>();
        var curr = head;
        while (curr != null) {
            if (!set.add(curr)) {
                return curr;
            }
            curr = curr.next;
        }
        return null;
    }
}
