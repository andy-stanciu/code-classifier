class Solution {

    public String method1(String s) {
        // Step 1 : build count map for char
        Map<Character, Integer> countMap = new HashMap<>();
        for (char c : s.toCharArray()) {
            countMap.put(c, countMap.getOrDefault(c, 0) + 1);
        }
        // Step 2 : prepare sorted list of char based on count
        List<Character> var1 = new ArrayList<>(countMap.keySet());
        Collections.sort(var1, (a, b) -> countMap.get(b) - countMap.get(a));
        // Step 3 : prepare string
        StringBuilder result = new StringBuilder();
        for (char c : var1) {
            result.append(String.valueOf(c).repeat(countMap.get(c)));
        }
        return result.toString();
    }
}
