public class Solution {

    public String method1(String s) {
        Map<Character, Integer> map = new HashMap<>();
        for (//putting all characters with their frequency
        //putting all characters with their frequency
        char c : s.toCharArray()) map.put(c, map.getOrDefault(c, 0) + 1);
        // formed an array of type arraylist
        List<Character>[] bucket = new List[s.length() + 1];
        for (char key : map.keySet()) {
            //traversing map to get frquency.
            //getting freq. of char
            int var1 = map.get(key);
            //in place of freq.  in bucket initializing arraylist
            if (bucket[var1] == null)
                bucket[var1] = new ArrayList<>();
            // and adding char at freq. position
            bucket[var1].add(key);
        }
        StringBuilder sb = new StringBuilder();
        for (//cause we have to print higher freq. char 1st
        //cause we have to print higher freq. char 1st
        //cause we have to print higher freq. char 1st
        int pos = bucket.length - 1; pos >= 0; pos--) if (//index where char exist
        bucket[pos] != null)
            for (//accesing the char
            //accesing the char
            char c : bucket[pos]) for (// accesing char freq. times
            // accesing char freq. times
            // accesing char freq. times
            // storing them in sb
            int i = 0; i < pos; i++) sb.append(c);
        //convert sb to string as per our return type
        return sb.toString();
    }
}
