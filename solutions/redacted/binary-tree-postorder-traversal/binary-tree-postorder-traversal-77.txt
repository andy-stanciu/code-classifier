class Solution {

    public List<Integer> method1(TreeNode root) {
        List<Integer> res = new ArrayList<>();
        if (root == null) {
            return res;
        }
        if (root.left != null) {
            res.addAll(method1(root.left));
        }
        if (root.right != null) {
            res.addAll(method1(root.right));
        }
        res.add(root.val);
        return res;
    }
}
