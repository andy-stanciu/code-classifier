class Solution {

    ArrayList<Integer> list = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return list;
        method1(root.left);
        method1(root.right);
        list.add(root.val);
        return list;
    }
}
