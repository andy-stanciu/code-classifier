class Solution {

    public int method1(int[] nums) {
        //method1
        // TreeSet<Integer> set=new TreeSet<>();
        // for(int n:nums){
        //     set.add(n);
        //     if(set.size()>3)set.pollFirst(); //remove the smallest element
        // }if(set.size()<3) return set.last(); //return largest element in array
        // return set.first(); //return third largest
        //method1
        List<Integer> a = new ArrayList<>();
        for (int n : nums) {
            if (!a.contains(n))
                a.add(n);
        }
        Collections.sort(a);
        if (a.size() < 3)
            return a.get(a.size() - 1);
        return a.get(a.size() - 3);
    }
}
