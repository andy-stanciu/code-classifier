class Solution {

    public int method1(int[] nums) {
        List<Integer> sortList = Arrays.stream(nums).boxed().sorted(Comparator.reverseOrder()).distinct().toList();
        return sortList.stream().skip(2).findFirst().orElse(sortList.get(0));
    }
}
