class Solution {

    public int method1(int[] nums) {
        TreeSet<Integer> disnums = new TreeSet<>();
        for (int num : nums) {
            disnums.add(num);
        }
        if (disnums.size() < 3) {
            return disnums.last();
        }
        disnums.pollLast();
        disnums.pollLast();
        return disnums.last();
    }
}
