class Solution {

    public void method1(int[] nums) {
        //Method 01
        // ArrayList<Integer> list=new ArrayList<>();
        // for(int i=0;i<nums.length;i++){
        //     if(nums[i]!=0){
        //         list.add(nums[i]);
        //     }
        // }
        // Arrays.fill(nums,0);
        //     int i=0;
        // for(int e:list){
        //     nums[i]=e;
        //     System.out.println(nums[i]);
        //     i++;
        // }
        //    int i=0;
        //    int j=0;
        //    while(i<nums.length && j<nums.length){
        //     if(nums[i]!=0){
        //         i++;
        //         j=i+1;
        //     }else if(nums[i]==0 && nums[j]!=0){
        //         int temp=nums[i];
        //         nums[i]=nums[j];
        //         nums[j]=temp;
        //     }else{
        //         j++;
        //     }
        //    }
        //    for(int e:nums){
        //     System.out.println(e);
        //    }
        int nonZeroPos = 0;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] != 0) {
                // Swap only when `i` and `nonZeroPos` are different
                if (i != nonZeroPos) {
                    int temp = nums[i];
                    nums[i] = nums[nonZeroPos];
                    nums[nonZeroPos] = temp;
                }
                nonZeroPos++;
            }
        }
        // Output the result
        for (int e : nums) {
            System.out.println(e);
        }
    }
}
