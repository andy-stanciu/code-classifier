class Solution {

    public void method1(int[] nums) {
        int n = nums.length;
        Queue<Integer> q = new LinkedList<>();
        // Enqueue non-zero elements
        for (int i = 0; i < n; i++) {
            if (nums[i] != 0) {
                q.add(nums[i]);
            }
        }
        // Fill array with queue elements followed by zeros
        int i = 0;
        while (!q.isEmpty()) {
            nums[i] = q.poll();
            i++;
        }
        while (i < n) {
            nums[i] = 0;
            i++;
        }
    }
}
