class Solution {

    public void method1(int[] nums) {
        for (// outer loop for finding zero\'s in the loop
        // outer loop for finding zero\'s in the loop
        // outer loop for finding zero\'s in the loop
        int i = 0; i < nums.length; i++) {
            if (//if zero find this if block will execute
            nums[i] == 0) {
                for (// will start j from +1 index of i
                // will start j from +1 index of i
                // will start j from +1 index of i
                int j = i + 1; j < nums.length; j++) {
                    if (// If the value at that index is not zero then this block will execute and swap the zero with the number
                    nums[j] != 0) {
                        int temp = nums[i];
                        nums[i] = nums[j];
                        nums[j] = temp;
                        break;
                    }
                }
            }
        }
    }
}
