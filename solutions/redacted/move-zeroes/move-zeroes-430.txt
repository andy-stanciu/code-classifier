class Solution {

    public void method1(int[] arr) {
        int p1 = 0, p2 = 0;
        for (; p2 < arr.length; p2++) if (arr[p2] != 0)
            arr[p1++] = arr[p2];
        while (p1 < arr.length) arr[p1++] = 0;
    }
}
