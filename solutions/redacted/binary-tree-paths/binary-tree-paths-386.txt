/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<String> method1(TreeNode root) {
        String s = "";
        List<String> l = new ArrayList<>();
        return method2(root, l, s);
    }

    private List<String> method2(TreeNode root, List<String> l, String s) {
        if (root == null)
            return l;
        if (root.left == null && root.right == null) {
            l.add(s + root.val);
        }
        s = s + root.val + "->";
        method2(root.left, l, s);
        method2(root.right, l, s);
        return l;
    }
}
