class Solution {

    public boolean method1(int x) {
        int c = x;
        int y = 0;
        while (x > 0) {
            y *= 10;
            y += x % 10;
            x /= 10;
        }
        return (c == y);
    }
}
