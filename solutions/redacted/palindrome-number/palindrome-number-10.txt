class Solution {

    public boolean method1(int x) {
        int rev = 0;
        int ori = x;
        if (x < -0) {
            return false;
        }
        while (x != 0) {
            //getting last digit
            int rem = x % 10;
            //adding space for new digit
            rev = rev * 10 + rem;
            //removing last digit
            x = x / 10;
        }
        if (ori == rev) {
            return true;
        } else {
            return false;
        }
    }
}
