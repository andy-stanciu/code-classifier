class Solution {

    public boolean method1(int x) {
        StringBuilder sb = new StringBuilder();
        sb.append(x);
        int m = (sb.length() / 2);
        int l = 0;
        int r = sb.length() - 1;
        while (l <= m) {
            if (sb.charAt(l++) == sb.charAt(r--)) {
            } else {
                return false;
            }
        }
        return true;
    }
}
