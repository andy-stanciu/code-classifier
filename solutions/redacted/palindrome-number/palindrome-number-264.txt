class Solution {

    public boolean method1(int x) {
        int reverse = 0;
        int origin = x;
        while (origin > 0) {
            reverse = reverse * 10 + origin % 10;
            origin = origin / 10;
        }
        return reverse == x;
    }
}
