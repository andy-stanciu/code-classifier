class Solution {

    public boolean method1(int x) {
        int sum = 0;
        int target = x;
        while (x > 0) {
            int a = x % 10;
            x = x / 10;
            sum = sum * 10 + a;
        }
        return sum == target;
    }
}
