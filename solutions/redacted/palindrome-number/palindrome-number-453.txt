class Solution {

    public boolean method1(int x) {
        int num = x;
        int rev = 0;
        int sum = 0;
        int lastd = 0;
        while (x > 0) {
            lastd = x % 10;
            rev = (rev * 10 + lastd);
            x = x / 10;
        }
        return num == rev;
    }
}
