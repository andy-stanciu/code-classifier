class Solution {

    public boolean method1(int x) {
        int reverse = 0, copyOfX = x;
        while (x > 0) {
            reverse = reverse * 10 + x % 10;
            x = x / 10;
        }
        if (reverse == copyOfX)
            return true;
        else
            return false;
    }
}
