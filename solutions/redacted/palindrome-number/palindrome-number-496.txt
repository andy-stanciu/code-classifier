class Solution {

    public boolean method1(int x) {
        int temp = x;
        int reversed = 0;
        while (temp > 0) {
            reversed = (reversed * 10) + (temp % 10);
            temp /= 10;
        }
        return reversed == x;
    }
}
