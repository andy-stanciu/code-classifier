class Solution {

    public boolean method1(int x) {
        if (x < 0) {
            return false;
        }
        int remainder = 0;
        int copy = x;
        while (x > 0) {
            remainder = remainder * 10 + x % 10;
            x /= 10;
        }
        return remainder == copy;
    }
}
