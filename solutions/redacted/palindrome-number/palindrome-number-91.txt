class Solution {

    public boolean method1(int num) {
        if (num < 0)
            return false;
        int reverseInt = method2(num);
        if (num == reverseInt) {
            return true;
        } else {
            return false;
        }
    }

    public int method2(int num) {
        int reverseInt = 0;
        while (num > 0) {
            int digit = num % 10;
            reverseInt = (reverseInt * 10) + digit;
            num /= 10;
        }
        return reverseInt;
    }
}
