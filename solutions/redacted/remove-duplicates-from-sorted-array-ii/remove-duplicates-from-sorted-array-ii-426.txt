class Solution {

    public int method1(int[] nums) {
        int index = 0;
        // Iterate over each element in the array
        for (int num : nums) {
            if (index < 2 || num != nums[index - 2]) {
                nums[index] = num;
                index++;
            }
        }
        return index;
    }
}
