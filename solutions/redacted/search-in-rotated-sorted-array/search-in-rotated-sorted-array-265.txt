class Solution {

    public int method1(int[] arr, int target) {
        int n = arr.length, low = 0, high = n - 1;
        while (low < high) {
            int mid = (high + low) / 2;
            if (arr[mid] > arr[high])
                low = mid + 1;
            else
                high = mid;
        }
        int var1 = low;
        if (arr[low] < arr[0])
            var1 = low;
        else
            var1 = 0;
        if (target > arr[n - 1]) {
            low = 0;
            high = var1 - 1;
        } else {
            low = var1;
            high = n - 1;
        }
        while (low <= high) {
            int mid = (high + low) / 2;
            if (arr[mid] == target)
                return mid;
            else if (arr[mid] > target)
                high = mid - 1;
            else
                low = mid + 1;
        }
        return -1;
    }
}
