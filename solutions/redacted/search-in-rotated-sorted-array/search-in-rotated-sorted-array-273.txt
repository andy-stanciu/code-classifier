class Solution {

    public int method1(int[] nums, int target) {
        for (int i = 0; i <= nums.length - 1; i++) {
            if (nums[i] == target) {
                //System.out.print(nums[i]);
                return i;
            }
        }
        return -1;
    }
}
