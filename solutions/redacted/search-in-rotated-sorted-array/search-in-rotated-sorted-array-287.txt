class Solution {

    public int method1(int[] nums, int target) {
        // Arrays.sort(nums);
        for (int i = 0; i < nums.length; i++) {
            if (target == nums[i]) {
                return i;
            }
        }
        return -1;
    }
}
