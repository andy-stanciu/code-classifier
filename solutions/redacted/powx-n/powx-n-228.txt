class Solution {

    public double myPow(double x, int n) {
        if (n >= 0) {
            return Math.pow(x, n);
        } else {
            double c = Double.valueOf(n);
            double a = Math.pow(x, Math.abs(c));
            double b = 1 / a;
            return b;
        }
    }
}
