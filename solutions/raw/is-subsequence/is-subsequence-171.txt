class Solution {

    public boolean isSubsequence(String s, String t) {
        int n = s.length();
        int j = 0;
        for (int i = 0; i < t.length(); i++) {
            if (j < n && t.charAt(i) == s.charAt(j))
                j++;
        }
        return j == n;
    }
}
