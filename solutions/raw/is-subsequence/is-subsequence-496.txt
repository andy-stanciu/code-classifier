class Solution {

    public boolean isSubsequence(String s, String t) {
        char[] s1 = s.toCharArray();
        char[] t1 = t.toCharArray();
        int c = 0;
        int k = 0;
        for (int i = 0; i < s1.length; i++) {
            for (int j = k; j < t1.length; j++) {
                if (s1[i] == t1[j]) {
                    c++;
                    k = j + 1;
                    break;
                }
            }
        }
        return c == s1.length ? true : false;
    }
}
