class Solution {

    public boolean isSubsequence(String s, String t) {
        int position = 0;
        for (int i = 0; i < t.length(); i++) {
            if (s.length() != position && t.charAt(i) == s.charAt(position))
                position++;
        }
        return position == s.length();
    }
}
