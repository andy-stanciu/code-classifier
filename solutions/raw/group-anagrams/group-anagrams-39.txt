class Solution {

    public List<List<String>> groupAnagrams(String[] strs) {
        Map<String, List> map = new HashMap();
        for (String s : strs) {
            char[] charArray = s.toCharArray();
            Arrays.sort(charArray);
            String sortedString = new String(charArray);
            List<String> currentList = map.getOrDefault(sortedString, new ArrayList<>());
            // Add the current string to the list
            currentList.add(s);
            // Update the map with the modified list
            map.put(sortedString, currentList);
            // map.put(sortedString, map.getOrDefault(sortedString, new List()).add(s));
        }
        List<List<String>> list = new ArrayList(map.values());
        return list;
    }
}
