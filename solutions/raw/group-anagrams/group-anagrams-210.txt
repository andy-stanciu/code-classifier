class Solution {

    public List<List<String>> groupAnagrams(String[] strs) {
        // Correct the map to store lists of Strings, not Integers
        Map<String, List<String>> anagramGroups = new HashMap<>();
        for (String word : strs) {
            // Correct method call to toCharArray()
            char[] arr = word.toCharArray();
            Arrays.sort(arr);
            String sortedWord = new String(arr);
            // Correct containsKey() method usage and use anagramGroups as the variable name
            if (!anagramGroups.containsKey(sortedWord)) {
                anagramGroups.put(sortedWord, new ArrayList<>());
            }
            // Correctly add word to the list of its sorted key
            anagramGroups.get(sortedWord).add(word);
        }
        // Return a list of anagram groups
        List<List<String>> result = new ArrayList<>(anagramGroups.values());
        return result;
    }
}
