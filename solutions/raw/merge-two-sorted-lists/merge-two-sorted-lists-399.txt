class Solution {

    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        ListNode temHead = new ListNode(0);
        ListNode ansNode = temHead;
        while (list1 != null && list2 != null) {
            // run loop until the any one node become null
            // store list1 node value to l1
            ListNode l1 = new ListNode(list1.val);
            // store list2 node value to l2
            ListNode l2 = new ListNode(list2.val);
            if (list1.val < list2.val) {
                // check wich value is smaller
                // add smaller element to ansNode list
                ansNode.next = l1;
                // updata ansNode
                ansNode = ansNode.next;
                // update list1
                list1 = list1.next;
            } else {
                // add smaller or equele to element to ansNode list
                ansNode.next = l2;
                ansNode = ansNode.next;
                // update list2
                list2 = list2.next;
            }
        }
        //if any element was  don\'t check in given list1
        while (list1 != null) {
            ListNode l1 = new ListNode(list1.val);
            ansNode.next = l1;
            ansNode = ansNode.next;
            list1 = list1.next;
        }
        //if any element was  don\'t check in given list2
        while (list2 != null) {
            ListNode l2 = new ListNode(list2.val);
            ansrNode.next = l2;
            ansNode = ansNode.next;
            list2 = list2.next;
        }
        return temHead.next;
    }
}
