class Solution {

    public int[] asteroidCollision(int[] asteroids) {
        Stack<Integer> s = new Stack();
        for (int i = 0; i < asteroids.length; i++) {
            if (s.isEmpty() || asteroids[i] > 0) {
                s.push(asteroids[i]);
            } else {
                while (true) {
                    int peek = s.peek();
                    if (peek < 0) {
                        // if the top element is -ve, there is no problem, insert new element
                        s.push(asteroids[i]);
                        break;
                    } else if (peek > -asteroids[i]) {
                        // if peek is greater,no collision asteriod destroyed
                        break;
                    } else if (peek == -asteroids[i]) {
                        // if same mag. asteriod both will collide and destroyed
                        s.pop();
                        break;
                    } else {
                        //(peek < -asteroids[i])--> if current asteriod is greater,peek will be destroyed
                        s.pop();
                        if (s.isEmpty()) {
                            s.push(asteroids[i]);
                            break;
                        }
                    }
                }
            }
        }
        int[] output_arr = new int[s.size()];
        for (int i = s.size() - 1; i >= 0; i--) {
            output_arr[i] = s.pop();
        }
        return output_arr;
    }
}
