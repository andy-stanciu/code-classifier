class Solution {

    public boolean isAnagram(String s, String t) {
        char[] c = s.toCharArray(), c2 = t.toCharArray();
        Arrays.sort(c);
        Arrays.sort(c2);
        return Arrays.equals(c, c2);
    }
}
