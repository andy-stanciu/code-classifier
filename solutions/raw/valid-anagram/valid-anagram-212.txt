class Solution {

    public boolean isAnagram(String s, String t) {
        String[] ss = s.split("");
        String[] ts = t.split("");
        Arrays.sort(ss);
        Arrays.sort(ts);
        if (Arrays.equals(ts, ss)) {
            return true;
        }
        return false;
    }
}
