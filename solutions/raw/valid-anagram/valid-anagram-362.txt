class Solution {

    public boolean isAnagram(String s, String t) {
        char[] strS = s.toCharArray();
        char[] tStr = t.toCharArray();
        Arrays.sort(strS);
        Arrays.sort(tStr);
        return Arrays.equals(strS, tStr);
    }
}
