class Solution {

    public boolean isAnagram(String s, String t) {
        char[] chars = s.toCharArray();
        Arrays.sort(chars);
        String sorted = new String(chars);
        char[] chr = t.toCharArray();
        Arrays.sort(chr);
        String sorte = new String(chr);
        return sorted.equals(sorte);
    }
}
