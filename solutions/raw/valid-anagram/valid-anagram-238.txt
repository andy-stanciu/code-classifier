class Solution {

    public boolean isAnagram(String s, String t) {
        //if both the string have different length then they will never be anagram
        if (s.length() != t.length())
            return false;
        int[] smap = new int[26];
        //map to store the representation of character of s with t
        for (int i = 0; i < s.length(); i++) ++smap[s.charAt(i) - 97];
        for (int i = 0; i < t.length(); i++) {
            char ch = t.charAt(i);
            if (smap[ch - 97] == 0)
                return false;
            --smap[ch - 97];
        }
        return true;
    }
}
