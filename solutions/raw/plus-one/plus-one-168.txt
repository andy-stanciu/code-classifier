class Solution {

    public int[] plusOne(int[] digits) {
        // length of array
        int length = digits.length;
        for (int i = length - 1; i >= 0; i--) {
            // checking the digits
            if (digits[i] < 9) {
                // checking if digit is less than 9
                digits[i]++;
                return digits;
            } else {
                //setting 0 in the place of 10
                digits[i] = 0;
            }
        }
        // creating new array
        int[] newDigits = new int[n + 1];
        // setting 1 at initial
        newDigits[0] = 1;
        // return the array
        return newDigits;
    }
}
