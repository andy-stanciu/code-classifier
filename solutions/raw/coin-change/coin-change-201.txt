class Solution {

    public int coinChange(int[] coins, int amount) {
        int n = coins.length;
        int[][] dp = new int[n + 1][amount + 1];
        // if amount == 0 then we need minimum 0 coins...
        for (int i = 0; i <= n; i++) {
            dp[i][0] = 0;
        }
        // if coins == 0 then we can not make any amount
        for (int j = 0; j <= amount; j++) {
            dp[0][j] = Integer.MAX_VALUE - 10000;
        }
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= amount; j++) {
                int taken = Integer.MAX_VALUE - 10000;
                int notTaken = dp[i - 1][j];
                if (j - coins[i - 1] >= 0) {
                    taken = 1 + dp[i][j - coins[i - 1]];
                }
                dp[i][j] = Math.min(taken, notTaken);
            }
        }
        return dp[n][amount] == Integer.MAX_VALUE - 10000 ? -1 : dp[n][amount];
    }
}
