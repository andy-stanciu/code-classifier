class Solution {

    public int[] twoSum(int[] nums, int target) {
        int i, j, m;
        m = 0;
        int l = nums.length;
        int sum = 0;
        for (i = 0; i < l; i++) {
            m = i;
            for (j = i + 1; j < l; j++) {
                sum = (nums[i] + nums[j]);
                if (sum == target) {
                    return new int[] { m, j };
                }
            }
        }
        throw new IllegalArgumentException("no match found");
    }
}
