class Solution {

    public ListNode removeNthFromEnd(ListNode head, int n) {
        ListNode temp = head;
        int count = 0;
        while (temp != null) {
            count++;
            temp = temp.next;
        }
        int removeNodeIndex = count - n;
        temp = head;
        count = 0;
        ListNode prev = temp;
        if (removeNodeIndex == 0) {
            return temp.next;
        }
        while (temp != null) {
            if (count == removeNodeIndex) {
                prev.next = temp.next;
            }
            count++;
            prev = temp;
            temp = temp.next;
        }
        return head;
    }
}
