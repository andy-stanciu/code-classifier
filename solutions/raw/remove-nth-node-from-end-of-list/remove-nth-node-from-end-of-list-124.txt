class Solution {

    int ans = 1;

    public ListNode removeNthFromEnd(ListNode head, int n) {
        if (head == null)
            return null;
        ListNode next = removeNthFromEnd(head.next, n);
        if (ans++ == n)
            return next;
        head.next = next;
        return head;
    }
}
