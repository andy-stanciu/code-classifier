class Solution {

    public String reverseWords(String s) {
        s = s.trim();
        Scanner str = new Scanner(s);
        s = "";
        while (str.hasNext()) {
            s = str.next() + " " + s;
        }
        return s.trim();
    }
}
