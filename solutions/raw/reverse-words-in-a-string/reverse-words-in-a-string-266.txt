class Solution {

    public String reverseWords(String s) {
        return Arrays.stream(new StringBuilder(s.strip()).reverse().toString().split(" ")).filter(token -> !token.isBlank()).map(StringBuilder::new).map(StringBuilder::reverse).map(StringBuilder::toString).collect(Collectors.joining(" "));
    }
}
