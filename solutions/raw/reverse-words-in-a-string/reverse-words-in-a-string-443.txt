class Solution {

    public String reverseWords(String s) {
        String[] str = s.split("\\\\s+");
        String op = "";
        for (int i = str.length - 1; i >= 0; i--) {
            op = op + " " + str[i];
        }
        return op.trim();
    }
}
