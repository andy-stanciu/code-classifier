class Solution {

    public String reverseWords(String s) {
        String words[] = s.split(" "), res = "";
        for (String word : words) {
            if (!word.equals(""))
                res = word + " " + res;
        }
        return res.trim();
    }
}
