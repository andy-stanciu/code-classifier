class Solution {

    public String reverseWords(String s) {
        String[] ktr = s.split(" ");
        String str = " ";
        for (int i = 0; i < ktr.length; i++) {
            str = ktr[i] + " " + str;
        }
        return str.trim().replaceAll("\\\\s+", " ");
    }
}
