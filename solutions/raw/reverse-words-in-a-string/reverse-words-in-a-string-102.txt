class Solution {

    public String reverseWords(String s) {
        List<String> hehe = Arrays.stream(s.trim().split("\\\\s+")).map(String::valueOf).collect(Collectors.toList());
        Collections.reverse(hehe);
        return hehe.stream().collect(Collectors.joining(" "));
    }
}
