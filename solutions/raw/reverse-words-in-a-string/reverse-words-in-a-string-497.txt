class Solution {

    public String reverseWords(String s) {
        s.trim();
        String[] a = s.split("\\\\s+");
        StringBuilder sb = new StringBuilder();
        for (String str : a) {
            sb.insert(0, " " + str);
        }
        return sb.toString().trim();
    }
}
