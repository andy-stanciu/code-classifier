class Solution {

    public String reverseWords(String s) {
        s = s.replaceAll("\\\\s+", " ");
        String rev = "";
        String[] list = s.split(" ");
        for (int i = list.length - 1; i >= 0; i--) {
            rev += list[i] + " ";
        }
        return rev.trim();
    }
}
