class Solution {

    public String reverseWords(String s) {
        String[] words = s.trim().split(" ");
        s = "";
        for (int i = words.length - 1; i >= 0; i--) {
            if (words[i].trim() != "") {
                s += words[i].trim();
                s += " ";
            }
        }
        return s.trim();
    }
}
