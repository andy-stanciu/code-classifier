class Solution {

    public String reverseWords(String s) {
        String[] ar = s.trim().split("\\\\s+");
        String rev = "";
        for (int i = ar.length - 1; i > 0; i--) {
            rev = rev.concat(ar[i]).concat(" ");
        }
        return rev.concat(ar[0]);
    }
}
