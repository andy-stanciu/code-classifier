class Solution {

    public String reverseWords(String s) {
        return Arrays.asList(s.trim().split(" ")).reversed().stream().filter(v -> !v.equals("")).collect(Collectors.joining(" "));
    }
}
