class Solution {

    public String reverseWords(String s) {
        String str = "";
        String[] ch = s.trim().split("\\\\s+");
        for (int i = ch.length - 1; i > 0; i--) {
            str += ch[i] + " ";
        }
        return str + ch[0];
    }
}
