class Solution {

    public String reverseWords(String s) {
        return Arrays.stream(s.split("[ ]+")).filter(z -> !z.equals("")).collect(Collectors.collectingAndThen(Collectors.toList(), list -> {
            Collections.reverse(list);
            return String.join(" ", list);
        }));
    }
}
