class Solution {

    private String resp;

    public String reverseWords(String s) {
        resp = "";
        Arrays.stream(s.split(" ")).forEach(word -> {
            if (!word.strip().isBlank()) {
                resp = word.strip().concat(" ").concat(resp);
            }
        });
        return resp.trim();
    }
}
