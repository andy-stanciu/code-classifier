class Solution {

    public String reverseWords(String s) {
        ArrayList<String> rev = new ArrayList<>(Arrays.asList(s.trim().split("\\\\s+")));
        Collections.reverse(rev);
        String revStr = String.join(" ", rev);
        // sb.delete(sb.length()-1,-1);
        return revStr;
    }
}
