/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<Double> averageOfLevels(TreeNode root) {
        List<Double> avg = new ArrayList<>();
        Queue<TreeNode> q = new LinkedList<>();
        q.offer(root);
        q.offer(null);
        long levelSum = 0;
        int nodeCount = 0;
        while (!q.isEmpty()) {
            TreeNode curr = q.poll();
            if (curr == null) {
                Double currAvg = levelSum / (double) nodeCount;
                avg.add(currAvg);
                levelSum = 0;
                nodeCount = 0;
                if (q.isEmpty()) {
                    break;
                } else {
                    q.offer(null);
                }
            } else {
                levelSum += curr.val;
                nodeCount++;
                if (curr.left != null)
                    q.offer(curr.left);
                if (curr.right != null)
                    q.offer(curr.right);
            }
        }
        return avg;
    }
}
