class Solution {

    public List<Double> averageOfLevels(TreeNode root) {
        List<Double> averageAtLevel = new ArrayList<>();
        Queue<TreeNode> queue = new LinkedList<>();
        queue.add(root);
        while (!queue.isEmpty()) {
            int queuesize = queue.size();
            double sumOnLevel = 0;
            for (int i = 0; i < queuesize; i++) {
                TreeNode node = queue.remove();
                sumOnLevel += node.val;
                if (node.left != null)
                    queue.add(node.left);
                if (node.right != null)
                    queue.add(node.right);
            }
            double avg = sumOnLevel / queuesize;
            averageAtLevel.add(avg);
        }
        return averageAtLevel;
    }
}
