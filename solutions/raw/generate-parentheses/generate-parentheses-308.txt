class Solution {

    public List<String> generateParenthesis(int n) {
        var list = new ArrayList<String>();
        generateWithSharedList(list, new StringBuilder(), n, n);
        return list;
    }

    private void generateWithSharedList(List<String> list, StringBuilder seq, int opening, int closing) {
        if (opening == 0 && closing == 0) {
            list.add(seq.toString());
        }
        if (opening > 0) {
            seq.append("(");
            generateWithSharedList(list, seq, opening - 1, closing);
            // delete last
            seq.deleteCharAt(seq.length() - 1);
        }
        if (closing > opening) {
            seq.append(")");
            generateWithSharedList(list, seq, opening, closing - 1);
            // delete last
            seq.deleteCharAt(seq.length() - 1);
        }
    }
}
