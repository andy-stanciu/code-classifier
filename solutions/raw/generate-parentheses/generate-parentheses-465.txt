class Solution {

    public List<String> generateParenthesis(int n) {
        HashSet<String> result = new HashSet<>();
        generateAll(n, 0, 0, "", result);
        ArrayList<String> output = new ArrayList<>();
        output.addAll(result);
        return output;
    }

    private void generateAll(int n, int open, int close, String p, Set<String> result) {
        if (open + close == 2 * n) {
            result.add(p);
            return;
        }
        if (open < n) {
            generateAll(n, open + 1, close, p + "(", result);
        }
        if (close < open) {
            generateAll(n, open, close + 1, p + ")", result);
        }
    }
}
