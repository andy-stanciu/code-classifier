class Solution {

    static List<String> ansList;

    public List<String> generateParenthesis(int n) {
        ansList = new ArrayList<>();
        generateParenthesisHelper("", n, n);
        return ansList;
    }

    private void generateParenthesisHelper(String op, int openBrackets, int closedBrackets) {
        if (openBrackets == 0 && closedBrackets == 0) {
            ansList.add(op);
            return;
        }
        if (openBrackets != 0) {
            generateParenthesisHelper(op + "(", openBrackets - 1, closedBrackets);
        }
        if (closedBrackets > openBrackets) {
            generateParenthesisHelper(op + ")", openBrackets, closedBrackets - 1);
        }
    }
}
