class Solution {

    public List<String> generateParenthesis(int n) {
        List<String> arraylist = new ArrayList<>();
        String ans = "";
        generate(n, 0, 0, ans, arraylist);
        return arraylist;
    }

    public static void generate(int n, int op, int cl, String ans, List<String> arraylist) {
        if (op == n && cl == n) {
            arraylist.add(ans);
            return;
        }
        if (op <= n) {
            generate(n, op + 1, cl, ans + "(", arraylist);
        }
        if (cl < +op) {
            generate(n, op, cl + 1, ans + ")", arraylist);
        }
    }
}
