final class Solution {

    private final List<String> res = new ArrayList<>();

    public List<String> generateParenthesis(final int n) {
        generate(0, 0, "", n);
        return res;
    }

    private void generate(final int open, final int closed, final String path, final int n) {
        if (open == closed && open == n) {
            res.add(path);
            return;
        }
        if (open < n)
            generate(open + 1, closed, path + "(", n);
        if (closed < open)
            generate(open, closed + 1, path + ")", n);
    }
}
