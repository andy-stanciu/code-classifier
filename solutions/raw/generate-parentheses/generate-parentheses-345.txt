class Solution {

    public List<String> generateParenthesis(int n) {
        String s = "";
        List<String> l = new ArrayList<>();
        generatePar(s, n, l, 0, 0);
        return l;
    }

    public void generatePar(String s, int n, List<String> l, int left, int right) {
        if (s.length() == 2 * n) {
            l.add(s);
            return;
        }
        if (left < n) {
            generatePar(s + "(", n, l, left + 1, right);
        }
        if (right < left) {
            generatePar(s + ")", n, l, left, right + 1);
        }
    }
}
