class Solution {

    public List<String> generateParenthesis(int n) {
        List<String> ansList = new ArrayList<>();
        generateParenthesisHelpers(ansList, new StringBuilder(), n, n);
        return ansList;
    }

    private void generateParenthesisHelpers(List<String> ansList, StringBuilder sb, int left, int right) {
        if (left == 0 && right == 0) {
            ansList.add(sb.toString());
            return;
        }
        if (left == 0) {
            sb.append(")");
            generateParenthesisHelpers(ansList, sb, left, right - 1);
            sb.deleteCharAt(sb.length() - 1);
        } else if (left == right) {
            sb.append("(");
            generateParenthesisHelpers(ansList, sb, left - 1, right);
            sb.deleteCharAt(sb.length() - 1);
        } else if (left < right) {
            sb.append("(");
            generateParenthesisHelpers(ansList, sb, left - 1, right);
            sb.deleteCharAt(sb.length() - 1);
            sb.append(")");
            generateParenthesisHelpers(ansList, sb, left, right - 1);
            sb.deleteCharAt(sb.length() - 1);
        }
    }
}
