class Solution {

    public List<String> generateParenthesis(int n) {
        List<String> result = new ArrayList<>();
        generateParenthesis(n, 0, 0, "", result);
        return result;
    }

    public void generateParenthesis(int n, int openBrackets, int closedBrackets, String ans, List<String> result) {
        if (openBrackets == n && closedBrackets == openBrackets) {
            result.add(ans);
        } else {
            if (openBrackets < n) {
                generateParenthesis(n, openBrackets + 1, closedBrackets, ans + "(", result);
            }
            if (closedBrackets < openBrackets) {
                generateParenthesis(n, openBrackets, closedBrackets + 1, ans + ")", result);
            }
        }
    }
}
