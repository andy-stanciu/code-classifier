class Solution {

    public List<String> generateParenthesis(int n) {
        List<String> res = new ArrayList<>();
        generate(n, 0, 0, res, "");
        return res;
    }

    public void generate(int n, int open, int close, List<String> list, String r) {
        if (open == close && open == n) {
            list.add(r);
            return;
        }
        if (open == close) {
            generate(n, open + 1, close, list, r + "(");
        } else if (open > close) {
            if (open < n)
                generate(n, open + 1, close, list, r + "(");
            if (close < n)
                generate(n, open, close + 1, list, r + ")");
        }
    }
}
