/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    private int maxSum;

    public int maxPathSum(TreeNode root) {
        maxSum = Integer.MIN_VALUE;
        calculateMaxPath(root);
        return maxSum;
    }

    private int calculateMaxPath(TreeNode currentNode) {
        if (currentNode == null) {
            return 0;
        }
        int leftSum = Math.max(0, calculateMaxPath(currentNode.left));
        int rightSum = Math.max(0, calculateMaxPath(currentNode.right));
        maxSum = Math.max(maxSum, leftSum + rightSum + currentNode.val);
        return Math.max(leftSum, rightSum) + currentNode.val;
    }
}
