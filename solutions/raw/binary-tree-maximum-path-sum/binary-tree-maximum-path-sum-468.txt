/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int maxPathSum(TreeNode root) {
        if (root == null)
            return 0;
        int maxSum = root.val;
        Stack<TreeNode> nodes = new Stack<>();
        // max path from the node to the left or right(left or right can be optional)
        Map<TreeNode, Integer> paths = new HashMap<>();
        nodes.push(root);
        while (!nodes.isEmpty()) {
            TreeNode node = nodes.pop();
            // if all underlying nodes of the current node are already processed
            if (paths.containsKey(node)) {
                // get max left and right path if positive
                int left = Math.max(0, paths.getOrDefault(node.left, 0));
                int right = Math.max(0, paths.getOrDefault(node.right, 0));
                // calculate and write max path from the current node to the left or right
                paths.put(node, node.val + Math.max(left, right));
                // calculate max path through this node
                int path = node.val + left + right;
                if (path > maxSum)
                    maxSum = path;
            } else {
                // put node back for further processing once all underlying nodes are done
                paths.put(node, 0);
                nodes.push(node);
                // and push its children to process before
                if (node.left != null)
                    nodes.push(node.left);
                if (node.right != null)
                    nodes.push(node.right);
            }
        }
        return maxSum;
    }
}
