/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int pathsum(TreeNode root, int[] maxi) {
        if (root == null)
            return 0;
        int maxl = Math.max(0, pathsum(root.left, maxi));
        int maxr = Math.max(0, pathsum(root.right, maxi));
        maxi[0] = Math.max(maxl + maxr + root.val, maxi[0]);
        return root.val + Math.max(maxl, maxr);
    }

    public int maxPathSum(TreeNode root) {
        int[] maxi = new int[1];
        maxi[0] = Integer.MIN_VALUE;
        pathsum(root, maxi);
        return maxi[0];
    }
}
