/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public static int findmax(TreeNode root, int[] maxi) {
        if (root == null) {
            return 0;
        }
        int lsum = Math.max(0, findmax(root.left, maxi));
        int rsum = Math.max(0, findmax(root.right, maxi));
        maxi[0] = Math.max(maxi[0], lsum + rsum + root.val);
        return root.val + Math.max(lsum, rsum);
    }

    public int maxPathSum(TreeNode root) {
        int[] maxi = new int[1];
        maxi[0] = Integer.MIN_VALUE;
        findmax(root, maxi);
        return maxi[0];
    }
}
