class Solution {

    public int removeDuplicates(int[] nums) {
        int i = 2;
        int j = i;
        for (; i < nums.length && j < nums.length; i++) {
            if (nums[j - 2] != nums[i]) {
                nums[j++] = nums[i];
            }
        }
        return j;
    }
}
