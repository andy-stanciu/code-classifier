class Solution {

    public int removeDuplicates(int[] arr) {
        int count = 0;
        for (int l = 0; l < arr.length; l++) {
            if (count < 2 || arr[l] != arr[count - 2]) {
                arr[count++] = arr[l];
            }
        }
        return count;
    }
}
