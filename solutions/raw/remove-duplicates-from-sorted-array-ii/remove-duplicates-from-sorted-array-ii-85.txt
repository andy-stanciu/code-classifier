class Solution {

    public int removeDuplicates(int[] nums) {
        int i = 0;
        for (int val : nums) {
            if (i < 2 || val > nums[i - 2]) {
                nums[i] = val;
                i++;
            }
        }
        return i;
    }
}
