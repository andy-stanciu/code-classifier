/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public TreeNode deleteNode(TreeNode root, int key) {
        if (root == null)
            return null;
        if (root.val == key) {
            // for no children case
            if (root.left == null && root.right == null)
                return null;
            // for 1 child case
            if (root.left != null && root.right == null)
                return root.left;
            if (root.right != null && root.left == null)
                return root.right;
            //for 2 children case
            TreeNode childNodeForNodeToBeDeleted = helper(root, root.right);
            root.val = childNodeForNodeToBeDeleted.val;
        } else if (root.val < key) {
            root.right = deleteNode(root.right, key);
        } else {
            root.left = deleteNode(root.left, key);
        }
        return root;
    }

    public TreeNode helper(TreeNode parent, TreeNode root) {
        TreeNode previousNodeParent = parent, curr = root;
        while (curr.left != null) {
            previousNodeParent = curr;
            curr = curr.left;
        }
        deleteNode(previousNodeParent, curr.val);
        return curr;
    }
}
