/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public TreeNode deleteNode(TreeNode root, int key) {
        if (root == null) {
            return root;
        }
        int rootVal = root.val;
        if (rootVal < key) {
            root.right = deleteNode(root.right, key);
            return root;
        }
        if (rootVal > key) {
            root.left = deleteNode(root.left, key);
            return root;
        }
        if (root.left == null && root.right == null) {
            return null;
        }
        if (root.left == null) {
            return root.right;
        }
        if (root.right == null) {
            return root.left;
        }
        var result = inOrder(root.right);
        result.right = deleteNode(root.right, result.val);
        result.left = root.left;
        return result;
    }

    private TreeNode inOrder(TreeNode root) {
        while (root.left != null) {
            root = root.left;
        }
        return root;
    }
}
