/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public TreeNode delete(TreeNode node) {
        if (node.right == null) {
            return node.left;
        }
        if (node.left == null) {
            return node.right;
        }
        TreeNode temp = node.right;
        TreeNode greatest_left_sub_tree = node.left;
        while (greatest_left_sub_tree.right != null) {
            greatest_left_sub_tree = greatest_left_sub_tree.right;
        }
        greatest_left_sub_tree.right = temp;
        return node.left;
    }

    public TreeNode deleteNode(TreeNode root, int key) {
        // 1 ) find the node which you want to delete
        if (root == null)
            return null;
        if (root.val == key) {
            return delete(root);
        }
        TreeNode head = root;
        while (root != null) {
            if (root.val > key) {
                if (root.left != null && root.left.val == key) {
                    root.left = delete(root.left);
                } else {
                    root = root.left;
                }
            } else {
                if (root.right != null && root.right.val == key) {
                    root.right = delete(root.right);
                } else {
                    root = root.right;
                }
            }
        }
        return head;
    }
}
