/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<Integer> preorderTraversal(TreeNode root) {
        ArrayList<Integer> lists = new ArrayList<>();
        preorderHelper(lists, root);
        return lists;
    }

    public void preorderHelper(ArrayList lists, TreeNode root) {
        if (root == null) {
            return;
        } else {
            lists.add(root.val);
            preorderHelper(lists, root.left);
            preorderHelper(lists, root.right);
        }
    }
}
