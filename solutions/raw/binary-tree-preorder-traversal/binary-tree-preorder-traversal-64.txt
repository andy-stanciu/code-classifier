class Solution {

    public List<Integer> preorderTraversal(TreeNode root) {
        List<Integer> ans = new ArrayList<Integer>();
        if (root == null)
            return ans;
        Stack<TreeNode> st = new Stack<TreeNode>();
        st.add(root);
        TreeNode n;
        while (!st.isEmpty()) {
            n = st.pop();
            ans.add(n.val);
            if (n.right != null)
                st.push(n.right);
            if (n.left != null)
                //pushed the left node last so that we can access it first
                st.push(n.left);
        }
        return ans;
    }
}
