/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<Integer> preorderTraversal(TreeNode root) {
        List<Integer> arr = new ArrayList<Integer>();
        preorderHelper(root, arr);
        return arr;
    }

    static void preorderHelper(TreeNode root, List<Integer> p) {
        if (root == null)
            return;
        p.add(root.val);
        preorderHelper(root.left, p);
        preorderHelper(root.right, p);
    }
}
