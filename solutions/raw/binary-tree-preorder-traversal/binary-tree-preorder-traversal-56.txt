/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<Integer> preorderTraversal(TreeNode root) {
        ArrayList<Integer> list1 = new ArrayList<>();
        if (root == null) {
            return list1;
        }
        return preorder(root, list1);
    }

    public List<Integer> preorder(TreeNode root, ArrayList<Integer> list1) {
        if (root == null) {
            return null;
        }
        list1.add(root.val);
        preorder(root.left, list1);
        preorder(root.right, list1);
        return list1;
    }
}
