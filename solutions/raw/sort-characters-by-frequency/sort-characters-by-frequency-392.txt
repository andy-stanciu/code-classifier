class Solution {

    static class Node {

        int f;

        char i;

        Node(int _f, char _i) {
            f = _f;
            i = _i;
        }
    }

    public String frequencySort(String s) {
        int n = s.length();
        HashMap<Character, Node> map = new HashMap();
        // Adding element to hashmap with frequency
        for (char i : s.toCharArray()) {
            Node curr = map.get(i);
            if (curr == null)
                map.put(i, new Node(1, i));
            else
                curr.f++;
        }
        // Declaring pq
        PriorityQueue<Node> pq = new PriorityQueue<>((a, b) -> b.f - a.f);
        // Adding element in priority queue
        pq.addAll(map.values());
        String ans = "";
        // Adding the element in ans
        while (!pq.isEmpty()) {
            Node curr = pq.remove();
            ans += ("" + curr.i).repeat(curr.f);
        }
        return ans;
    }
}
