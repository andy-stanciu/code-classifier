class Solution {

    public String frequencySort(String s) {
        //store frequency in hashmap
        Map<Character, Integer> mp = new HashMap<>();
        for (char item : s.toCharArray()) {
            mp.put(item, mp.getOrDefault(item, 0) + 1);
        }
        //store map in priorityqueue that will sort based on value as i am comparing it by value
        PriorityQueue<Map.Entry<Character, Integer>> maxHeap = new PriorityQueue<>(Map.Entry.comparingByValue(Comparator.reverseOrder()));
        for (Map.Entry<Character, Integer> entry : mp.entrySet()) {
            maxHeap.offer(entry);
        }
        StringBuilder sb = new StringBuilder();
        // get key and value and store it occurances in stringbuilder
        while (!maxHeap.isEmpty()) {
            char key = maxHeap.peek().getKey();
            int value = maxHeap.peek().getValue();
            for (int i = 0; i < value; i++) {
                sb.append(key);
            }
            maxHeap.poll();
        }
        return sb.toString();
    }
}
