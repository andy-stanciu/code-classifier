class Solution {

    public String frequencySort(String s) {
        Map<Character, Integer> characterFrequencyMap = new HashMap<>();
        for (char c : s.toCharArray()) {
            characterFrequencyMap.put(c, characterFrequencyMap.getOrDefault(c, 0) + 1);
        }
        return characterFrequencyMap.entrySet().stream().sorted((a, b) -> b.getValue() - a.getValue()).map(a -> getRepeatedTimes(a)).reduce("", (x, y) -> x + y);
    }

    private String getRepeatedTimes(Map.Entry<Character, Integer> entry) {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < entry.getValue(); i++) {
            sb.append(entry.getKey());
        }
        return sb.toString();
    }
}
