class Char_Map {

    int index;

    int freq;

    public Char_Map(int index, int freq) {
        this.index = index;
        this.freq = freq;
    }
}

class Solution {

    public String frequencySort(String s) {
        int n = s.length();
        int[] freq = new int[256];
        for (int i = 0; i < n; i++) {
            freq[s.charAt(i)]++;
        }
        Char_Map[] cmap = new Char_Map[256];
        for (int i = 0; i < 256; i++) {
            cmap[i] = new Char_Map(i, freq[i]);
        }
        Comparator<Char_Map> sort_freq = (o1, o2) -> {
            if (o1.freq < o2.freq) {
                return 1;
            } else if (o1.freq == o2.freq) {
                return 0;
            } else {
                return -1;
            }
        };
        Arrays.sort(cmap, sort_freq);
        String sol = "";
        for (int i = 0; i < 256; i++) {
            if (cmap[i].freq != 0) {
                char ch = (char) (cmap[i].index);
                int frequency = cmap[i].freq;
                for (int j = 0; j < frequency; j++) {
                    sol += ch;
                }
            }
        }
        return sol;
    }
}
