class Solution {

    public int majorityElement(int[] nums) {
        int i = nums[0];
        int count = 0;
        for (int num : nums) {
            if (count == 0) {
                i = num;
            }
            count += (num == i) ? 1 : -1;
        }
        return i;
    }
}
