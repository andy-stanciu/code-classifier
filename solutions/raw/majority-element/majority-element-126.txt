class Solution {

    public int majorityElement(int[] nums) {
        Arrays.sort(nums);
        int n = nums.length - 1;
        int target = n / 2;
        if (nums[0] == nums[target])
            return nums[0];
        else
            return nums[target];
    }
}
