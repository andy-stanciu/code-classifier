class Solution {

    public int majorityElement(int[] nums) {
        int count = 0;
        Integer no = null;
        for (int k : nums) {
            if (count == 0) {
                no = k;
            }
            count += (k == no) ? 1 : -1;
        }
        return no;
    }
}
