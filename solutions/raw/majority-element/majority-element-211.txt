class Solution {

    public int majorityElement(int[] nums) {
        //sorting the array
        Arrays.sort(nums);
        // return the middle element
        return nums[nums.length / 2];
    }
}
