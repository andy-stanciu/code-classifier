class Solution {

    public int majorityElement(int[] nums) {
        // Java arrays sort uses quick sort so
        Arrays.sort(nums);
        //the time complexity is O(n log n)
        return nums[nums.length / 2];
    }
}
