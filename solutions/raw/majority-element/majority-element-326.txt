class Solution {

    public int majorityElement(int[] nums) {
        // Moore approach
        int ans = 0, freq = 0;
        for (int i : nums) {
            if (freq == 0)
                ans = i;
            if (i == ans)
                freq++;
            else
                freq--;
        }
        return ans;
    }
}
