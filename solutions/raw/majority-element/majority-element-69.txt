class Solution {

    public int majorityElement(int[] arr) {
        int target = arr.length / 2 + 1;
        Arrays.sort(arr);
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == arr[target - 1])
                return arr[i];
        }
        return -1;
    }
}
