class Solution {

    public int cnt = 0;

    private void inOrder(TreeNode root) {
        if (root == null)
            return;
        inOrder(root.left);
        cnt++;
        inOrder(root.right);
    }

    public int countNodes(TreeNode root) {
        inOrder(root);
        return cnt;
    }
}
