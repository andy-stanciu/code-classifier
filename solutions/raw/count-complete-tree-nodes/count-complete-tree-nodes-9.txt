class Solution {

    int count = 0;

    void inorder(TreeNode root) {
        // inroder traversal
        if (root == null)
            return;
        count++;
        inorder(root.left);
        inorder(root.right);
    }

    public int countNodes(TreeNode root) {
        inorder(root);
        return count;
    }
}
