class Solution {

    int sum = 0;

    public int countNodes(TreeNode root) {
        if (root == null) {
            return 0;
        } else {
            sum += 1;
        }
        countNodes(root.left);
        countNodes(root.right);
        return sum;
    }
}
