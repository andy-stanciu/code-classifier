class Solution {

    int count = 0;

    public void helper(TreeNode root) {
        if (root == null)
            return;
        helper(root.left);
        count += 1;
        helper(root.right);
        return;
    }

    public int countNodes(TreeNode root) {
        helper(root);
        return count;
    }
}
