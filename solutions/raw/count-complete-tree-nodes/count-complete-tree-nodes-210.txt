class Solution {

    int count = 0;

    public void inorder(TreeNode root) {
        if (root != null) {
            inorder(root.left);
            count++;
            inorder(root.right);
        }
    }

    public int countNodes(TreeNode root) {
        inorder(root);
        return count;
    }
}
