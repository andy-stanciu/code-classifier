/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int countNodes(TreeNode root) {
        return countNodes(root, 1);
    }

    public int countNodes(TreeNode root, int n) {
        if (root == null)
            return n - 1;
        int max = countNodes(root.left, ++n);
        max = countNodes(root.right, ++max);
        return max;
    }
}
