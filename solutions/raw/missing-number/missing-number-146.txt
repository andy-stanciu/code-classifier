class Solution {

    public int missingNumber(int[] nums) {
        //first simple math logic method
        int n = nums.length;
        int sum = (n * (n + 1)) / 2;
        for (int i = 0; i < n; i++) {
            sum = sum - nums[i];
        }
        return sum;
        //second using sorting
        // Arrays.sort(nums);
        // for(int i=0;i<nums.length;i++){
        //     if(nums[i]!=i){
        //         return i;
        //     }
        // }
        // return nums.length;
        //third method HashMap
        //    HashSet<Integer> set = new HashSet<>();
        //     for (int num : nums) {
        //         set.add(num);
        //     }
        //     for (int i = 0; i <= nums.length; i++) {
        //         if (!set.contains(i)) {
        //             return i;
        //         }
        //     }
        //     return -1; // If no missing number found
    }
}
