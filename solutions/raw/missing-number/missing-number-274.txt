class Solution {

    public int missingNumber(int[] nums) {
        Arrays.sort(nums);
        for (int j = 0; j < nums.length; j++) {
            if (j != nums[j]) {
                return j;
            }
        }
        return nums.length;
    }
}
