class Solution {

    public int missingNumber(int[] nums) {
        int size = nums.length;
        int sumOfNum = (size * (size + 1)) / 2;
        for (int num : nums) {
            sumOfNum -= num;
        }
        return sumOfNum;
    }
}
