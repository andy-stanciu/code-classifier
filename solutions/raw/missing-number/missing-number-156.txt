class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length;
        int track = 0;
        int sum = n * (n + 1) / 2;
        for (int i = 0; i < n; i++) {
            track += nums[i];
        }
        return sum - track;
    }
}
