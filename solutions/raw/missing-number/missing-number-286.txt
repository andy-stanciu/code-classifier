public class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length, sum_n = n * (n + 1) / 2, actual_sum = 0;
        for (int num : nums) {
            actual_sum += num;
        }
        return sum_n - actual_sum;
    }
}
