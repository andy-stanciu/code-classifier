class Solution {

    public int missingNumber(int[] nums) {
        int val = 0, len = nums.length;
        for (int i = 0; i < len; i++) val += nums[i];
        return (len * (len + 1) / 2) - val;
    }
}
