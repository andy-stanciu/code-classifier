class Solution {

    public int missingNumber(int[] nums) {
        int sum = (int) ((nums.length / 2.0f) * (nums.length + 1));
        int sum2 = 0;
        for (int n : nums) {
            sum2 += n;
        }
        return sum - sum2;
    }
}
