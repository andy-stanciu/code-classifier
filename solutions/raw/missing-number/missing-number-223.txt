class Solution {

    public int missingNumber(int[] nums) {
        int txor = 0;
        for (int num : nums) txor ^= num;
        for (int n = 0; n <= nums.length; ++n) txor ^= n;
        return txor;
    }
}
