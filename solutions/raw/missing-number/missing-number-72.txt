class Solution {

    public int missingNumber(int[] nums) {
        int theoSum = (nums.length * (nums.length + 1)) / 2;
        for (int I : nums) {
            theoSum -= I;
        }
        return theoSum;
    }
}
