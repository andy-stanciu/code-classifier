class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length;
        int sumOfn = (n * (n + 1)) / 2;
        int givenSum = 0;
        for (int num : nums) {
            givenSum += num;
        }
        return sumOfn - givenSum;
    }
}
