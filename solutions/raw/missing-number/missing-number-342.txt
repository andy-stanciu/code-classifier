class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length;
        int truesum = n * (n + 1) / 2;
        int calculatedsum = 0;
        for (int num : nums) {
            calculatedsum += num;
        }
        return truesum - calculatedsum;
    }
}
