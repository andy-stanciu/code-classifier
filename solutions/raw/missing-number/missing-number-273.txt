class Solution {

    public int missingNumber(int[] nums) {
        //(a0 + an)*n/2 (Arithmetic progression sum)
        int ans = ((nums.length) * (nums.length + 1)) / 2;
        for (int n : nums) {
            ans = ans - n;
        }
        return ans;
    }
}
