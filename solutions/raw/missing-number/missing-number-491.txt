class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length;
        int Sum = (n * (n + 1)) / 2;
        int Sum2 = 0;
        for (int i = 0; i < n; i++) {
            Sum2 += nums[i];
        }
        return (Sum - Sum2);
    }
}
