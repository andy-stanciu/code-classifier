class Solution {

    public int missingNumber(int[] nums) {
        int sum = 0;
        for (int i : nums) {
            sum += i;
        }
        int sum1 = ((nums.length + 1) * nums.length) / 2;
        return sum1 - sum;
    }
}
