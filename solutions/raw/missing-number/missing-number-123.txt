class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length;
        int count = n * (n + 1) / 2;
        for (int num : nums) {
            count = count - num;
        }
        return count;
    }
}
