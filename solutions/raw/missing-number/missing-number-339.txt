class Solution {

    public int missingNumber(int[] nums) {
        int sizeOfNums = nums.length;
        int idealSum = sizeOfNums * (sizeOfNums + 1) / 2;
        int actualSum = 0;
        for (int n : nums) {
            actualSum += n;
        }
        return idealSum - actualSum;
    }
}
