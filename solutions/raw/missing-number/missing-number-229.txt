class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length;
        // Calculates the sum of integers upto n
        int sum = (n * (n + 1)) / 2;
        for (int i = 0; i < n; i++) sum = sum - nums[i];
        return sum;
    }
}
