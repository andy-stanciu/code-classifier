class Solution {

    public int missingNumber(int[] nums) {
        int len = nums.length;
        int sum = (len * (len + 1)) / 2;
        int givenSum = 0;
        for (int num : nums) givenSum += num;
        return sum - givenSum;
    }
}
