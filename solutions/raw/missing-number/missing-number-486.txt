class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length;
        int sum = n * (n + 1) / 2;
        int curr_sum = 0;
        for (int i = 0; i < n; i++) {
            curr_sum += nums[i];
        }
        return sum - curr_sum;
    }
}
