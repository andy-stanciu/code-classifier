class Solution {

    public int missingNumber(int[] arr) {
        int arrSum = 0;
        int numSum = 0;
        for (int i = 0; i < arr.length; i++) {
            arrSum += arr[i];
            numSum += i + 1;
        }
        return numSum - arrSum;
    }
}
