class Solution {

    public int missingNumber(int[] nums) {
        int N = nums.length;
        int Sum = N * (N + 1) / 2;
        int S2 = 0;
        for (int i = 0; i < N; i++) {
            S2 = S2 + nums[i];
        }
        int missing_num = Sum - S2;
        return missing_num;
    }
}
