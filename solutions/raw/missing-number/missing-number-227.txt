class Solution {

    public int missingNumber(int[] nums) {
        int maxSum = 0;
        int minSum = 0;
        int n = nums.length;
        for (int i = n - 1; i >= 0; i--) {
            maxSum += n--;
            minSum += nums[i];
        }
        return maxSum - minSum;
    }
}
