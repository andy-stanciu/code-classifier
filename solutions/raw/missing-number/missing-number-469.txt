class Solution {

    public int missingNumber(int[] nums) {
        int n = nums.length;
        int arr_sum = 0;
        for (int k : nums) {
            arr_sum += k;
        }
        int sum = (n * (n + 1)) / 2;
        return sum - arr_sum;
    }
}
