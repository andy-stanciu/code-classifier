class Solution {

    public int missingNumber(int[] nums) {
        int size = nums.length;
        int sum = size * (size + 1) / 2;
        int total = 0;
        for (int i = 0; i <= size - 1; i++) total += nums[i];
        return sum - total;
    }
}
