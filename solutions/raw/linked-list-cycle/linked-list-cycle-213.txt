public class Solution {

    public bool HasCycle(ListNode head) {
        var cycle = Int32.MaxValue;
        while (head != null) {
            if (head.val == cycle)
                return true;
            head.val = cycle;
            head = head.next;
        }
        return false;
    }
}
