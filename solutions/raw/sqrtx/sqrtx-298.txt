class Solution {

    public int mySqrt(int x) {
        //    java.math.BigInteger b = java.math.BigInteger.valueOf(x);
        //    java.math.BigInteger s = b.sqrt();
        //    int z = s.intValue();
        //    return z;
        double y;
        if (x > 0) {
            y = Math.pow(x, 0.5);
        } else {
            return 0;
        }
        return (int) y;
    }
}
