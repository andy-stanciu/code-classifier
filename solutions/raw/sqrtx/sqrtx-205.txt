class Solution {

    public int mySqrt(int x) {
        //find the square root, floor sqr
        //TC:O(rootN)
        //SC:O(1)
        long ans = 0;
        long i = 1;
        while (i * i <= x) {
            ans = i;
            i++;
        }
        return (int) ans;
    }
}
