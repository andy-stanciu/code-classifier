class Solution {

    public int mySqrt(int x) {
        long currentRoot = 0;
        while (currentRoot * currentRoot <= x) {
            currentRoot++;
        }
        return (int) currentRoot - 1;
    }
}
