class Solution {

    public int mySqrt(int x) {
        //Math.sqrt = gives the result like 8 = 2.34567654
        //When we using method floor we get the res like 2.9 = 2, 3.999999 = 3
        return (int) Math.floor(Math.sqrt(x));
    }
}
