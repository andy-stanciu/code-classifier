class Solution {

    public int mySqrt(int x) {
        if (x == 1)
            return 1;
        double temp = 0;
        double sr = x / 2;
        while ((temp - sr) != 0) {
            temp = sr;
            sr = (temp + (x / temp)) / 2;
        }
        return (int) sr;
    }
}
