class Solution {

    public int mySqrt(int x) {
        if (x < 0) {
            System.out.println("Input should be positive");
        }
        int n = 0;
        while (Math.pow(n, 2) <= x) {
            n++;
        }
        return n - 1;
    }
}
