class Solution {

    public int findMin(int[] nums) {
        int n = nums.length;
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {
                if (nums[i] < nums[j]) {
                    int min = nums[j];
                } else {
                    return nums[j];
                }
            }
        }
        return nums[0];
    }
}
