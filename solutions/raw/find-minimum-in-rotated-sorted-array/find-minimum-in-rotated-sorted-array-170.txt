class Solution {

    public int findMin(int[] nums) {
        int i = 1, index = 0;
        while (i < nums.length) {
            if (nums[i - 1] > nums[i]) {
                index = i;
                break;
            }
            i++;
        }
        return nums[index];
    }
}
