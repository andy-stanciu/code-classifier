class Solution {

    public int findMin(int[] nums) {
        int left = 0;
        int right = nums.length - 1;
        int result = nums[left];
        while (left < right) {
            int mid = left + (right - left) / 2;
            // we need to compare all the values as \'mid\' could shift and move to a position of greater value
            result = Math.min(Math.min(Math.min(nums[left], nums[mid]), nums[right]), result);
            if (nums[mid] > nums[right])
                left = mid + 1;
            else if (nums[mid] <= nums[right])
                right = mid - 1;
        }
        return result;
    }
}
