class Solution {

    public List<String> binaryTreePaths(TreeNode root) {
        List<String> ans = new ArrayList<>();
        binaryTreePaths(root, ans, root.val + "");
        return ans;
    }

    public void binaryTreePaths(TreeNode root, List<String> ans, String str) {
        if (root == null)
            return;
        if (root.left == null && root.right == null) {
            ans.add(str);
            return;
        }
        if (root.left != null)
            binaryTreePaths(root.left, ans, str + "->" + root.left.val);
        if (root.right != null)
            binaryTreePaths(root.right, ans, str + "->" + root.right.val);
    }
}
