/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<String> binaryTreePaths(TreeNode root) {
        List<String> result = new ArrayList<>();
        findPaths(root, new ArrayList<>(), result);
        return result;
    }

    private void findPaths(TreeNode root, List<String> curr, List<String> result) {
        if (root == null) {
            return;
        }
        curr.add(String.valueOf(root.val));
        findPaths(root.left, curr, result);
        findPaths(root.right, curr, result);
        if (root.left == null && root.right == null)
            result.add(String.join("->", curr));
        curr.removeLast();
    }
}
