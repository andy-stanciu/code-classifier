class Solution {

    List<String> ret;

    private void paths(String s, TreeNode root) {
        s += "->";
        s += Integer.toString(root.val);
        if (root.left == null && root.right == null) {
            ret.add(s);
            return;
        }
        if (root.left != null)
            paths(s, root.left);
        if (root.right != null)
            paths(s, root.right);
    }

    public List<String> binaryTreePaths(TreeNode root) {
        ret = new ArrayList<>();
        String s = Integer.toString(root.val);
        if (root.left == null && root.right == null) {
            ret.add(s);
            return ret;
        }
        if (root.left != null)
            paths(s, root.left);
        if (root.right != null)
            paths(s, root.right);
        return ret;
    }
}
