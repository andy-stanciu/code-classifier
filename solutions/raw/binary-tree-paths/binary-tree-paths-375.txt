/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public void allPathsOfBinaryTree(TreeNode root, String smallans, List<String> ans) {
        if (root == null) {
            return;
        }
        if (root.left == null && root.right == null) {
            smallans = smallans + root.val;
            ans.add(smallans);
            return;
        }
        allPathsOfBinaryTree(root.left, smallans + root.val + "->", ans);
        allPathsOfBinaryTree(root.right, smallans + root.val + "->", ans);
    }

    public List<String> binaryTreePaths(TreeNode root) {
        List<String> ans = new ArrayList<>();
        allPathsOfBinaryTree(root, "", ans);
        return ans;
    }
}
