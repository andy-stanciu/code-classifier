/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<String> binaryTreePaths(TreeNode root) {
        List<String> rs = new ArrayList<String>();
        if (root != null) {
            searchTree(root, "", rs);
        }
        return rs;
    }

    private void searchTree(TreeNode root, String path, List<String> rs) {
        if (root.left == null && root.right == null) {
            rs.add(path + root.val);
        }
        if (root.left != null) {
            searchTree(root.left, path + root.val + "->", rs);
        }
        if (root.right != null) {
            searchTree(root.right, path + root.val + "->", rs);
        }
    }
}
