class Solution {

    public List<String> binaryTreePaths(TreeNode root) {
        ArrayList<String> s = new ArrayList<>();
        path(root, s, new String());
        return s;
    }

    public void path(TreeNode root, ArrayList<String> paths, String str) {
        Integer x = root.val;
        if (root.left == null && root.right == null)
            paths.add(str + x.toString());
        else if (root.right == null)
            path(root.left, paths, str + x.toString() + "->");
        else if (root.left == null)
            path(root.right, paths, str + x.toString() + "->");
        else {
            path(root.left, paths, str + x.toString() + "->");
            path(root.right, paths, str + x.toString() + "->");
        }
    }
}
