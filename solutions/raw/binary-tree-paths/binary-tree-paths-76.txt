/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public static List<String> binary(TreeNode root, String sb, List<String> li) {
        if (root != null) {
            sb += (root.val);
            if (root.left == null && root.right == null) {
                li.add(sb);
            } else {
                binary(root.left, sb + "->", li);
                binary(root.right, sb + "->", li);
            }
        }
        return li;
    }

    public List<String> binaryTreePaths(TreeNode root) {
        List<String> li = new ArrayList<String>();
        return binary(root, "", li);
    }
}
