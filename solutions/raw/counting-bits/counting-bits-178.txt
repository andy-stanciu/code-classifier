class Solution {

    public int[] countBits(int n) {
        //using bitwise
        int[] arr = new int[n + 1];
        for (int i = 1; i < n + 1; i++) {
            if (i % 2 != 0) {
                arr[i] = arr[i >> 1] + 1;
            } else {
                arr[i] = arr[i >> 1];
            }
        }
        return arr;
    }
}
