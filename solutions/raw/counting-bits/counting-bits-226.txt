class Solution {

    public int[] countBits(int n) {
        int[] answer = new int[n + 1];
        answer[0] = 0;
        for (int i = 1; i <= n; i++) {
            // if even x/2 , if odd 1+x/2;
            answer[i] = answer[i / 2] + i % 2;
        }
        return answer;
    }
}
