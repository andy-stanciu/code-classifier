class Solution {

    public int[] countBits(int n) {
        int[] array = new int[n + 1];
        for (int index = 1; index <= n; index++) array[index] = array[index / 2] + (index % 2);
        return array;
    }
}
