class Solution {

    public int[] countBits(int n) {
        int[] ans = new int[n + 1];
        ans[0] = 0;
        for (int i = 1; i <= n; i++) {
            // i >> 1 is i / 2, i & 1 is i % 2
            ans[i] = ans[i >> 1] + (i & 1);
        }
        return ans;
    }
}
