class Solution {

    public int[] countBits(int n) {
        // n space
        int[] ans = new int[n + 1];
        for (int i = 1; i <= n; i++) {
            // n times
            //constant time
            ans[i] = (i % 2) + ans[i / 2];
        }
        return ans;
    }
}
