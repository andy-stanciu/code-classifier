class Solution {

    public int[] countBits(int n) {
        int[] setBits = new int[n + 1];
        setBits[0] = 0;
        for (int i = 1; i <= n; i++) {
            setBits[i] = (i % 2 == 0) ? setBits[i / 2] : setBits[i - 1] + 1;
        }
        return setBits;
    }
}
// TC: O(n), SC: O(n)
