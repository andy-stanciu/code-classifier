class Solution {

    public int[] countBits(int n) {
        int[] arr = new int[n + 1];
        for (int i = 0; i <= n; i++) arr[i] = countSetBits(i);
        return arr;
    }

    static int countSetBits(int n) {
        int c = 0;
        while (n != 0) {
            c++;
            n = n & (n - 1);
        }
        return c;
    }
}
