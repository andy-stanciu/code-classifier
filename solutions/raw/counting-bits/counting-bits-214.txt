class Solution {

    public int[] countBits(int n) {
        // jay shree ram
        int count = 0;
        int[] arr = new int[n + 1];
        while (count <= n) {
            arr[count] = Integer.bitCount(count);
            count++;
        }
        return arr;
    }
}
