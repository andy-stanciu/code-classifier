class Solution {

    public int[] countBits(int n) {
        int[] result = new int[n + 1];
        for (int i = 0; i <= n; i++) {
            // Use bit manipulation to count the number of 1 bits
            result[i] = result[i >> 1] + (i & 1);
        }
        return result;
    }
}
