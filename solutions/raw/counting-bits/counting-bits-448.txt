class Solution {

    public int[] countBits(int n) {
        int i = 0;
        int[] res = new int[n + 1];
        while (i <= n) {
            res[i] = Integer.bitCount(i);
            i++;
        }
        return res;
    }
}
