class Solution {

    public int[] countBits(int n) {
        int[] ans = new int[n + 1];
        int j = 0;
        for (int i = 0; i <= n; i++) {
            int count = 0;
            count = Integer.bitCount(i);
            ans[j++] = count;
        }
        return ans;
    }
}
