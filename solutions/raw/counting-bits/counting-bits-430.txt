class Solution {

    public int[] countBits(int n) {
        int[] memo = new int[n + 1];
        memo[0] = 0;
        for (int i = 1; i <= n; i++) {
            memo[i] = memo[i & (i - 1)] + 1;
        }
        return memo;
    }
}
