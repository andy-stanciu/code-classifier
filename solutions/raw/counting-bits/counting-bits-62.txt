class Solution {

    public int[] countBits(int n) {
        var ans = new int[n + 1];
        for (int i = 1, pow = 1; i <= n; i++) {
            if (i == pow * 2)
                pow *= 2;
            ans[i] = 1 + ans[i - pow];
        }
        return ans;
    }
}
