class Solution {

    public int[] countBits(int n) {
        int[] ans = new int[n + 1];
        int k;
        for (int i = 1; i <= n; i++) {
            //if 1st bit is 1 then k=1 else k=0;
            k = i & 1;
            ans[i] = ans[i >> 1] + k;
        }
        return ans;
    }
}
