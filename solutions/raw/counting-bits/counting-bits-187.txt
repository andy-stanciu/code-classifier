class Solution {

    public int[] countBits(int n) {
        int[] ans = new int[n + 1];
        for (int i = 0; i < n + 1; i++) {
            int val = i / 2;
            int rem = (i % 2 == 0) ? 0 : 1;
            ans[i] = ans[val] + rem;
        }
        return ans;
    }
}
