class Solution {

    public int[] solve(int n) {
        int[] arr = new int[n + 1];
        arr[0] = 1;
        for (int i = 1; i <= n; i++) {
            arr[i] = (arr[i - 1] * (n - i + 1)) / i;
        }
        return arr;
    }
}
