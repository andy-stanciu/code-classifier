class Solution {

    public boolean wordBreak(String s, List<String> wordDict) {
        Set<Integer> a = new HashSet<>();
        a.add(0);
        for (int i = 0; i < s.length(); i++) {
            for (Integer n : a) {
                String s1 = s.substring(n, i + 1);
                if (wordDict.contains(s1)) {
                    a.add(i + 1);
                    break;
                }
            }
        }
        return a.contains(s.length());
    }
}
