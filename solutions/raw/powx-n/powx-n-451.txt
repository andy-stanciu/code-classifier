class Solution {

    public double myPow(double x, int n) {
        double pow = 0;
        if (n % 2 == 0) {
            pow = Math.pow(x, n / 2) * Math.pow(x, n / 2);
        } else {
            pow = Math.pow(x, n);
        }
        return pow;
    }
}
