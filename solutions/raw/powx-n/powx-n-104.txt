class Solution {

    double ans = 1;

    public double myPow(double x, long n) {
        if (n == 0)
            return 1;
        else if (n > 0)
            return x * myPow(x, --n);
        else
            return (1 / x) * (myPow(x, ++n));
    }
}
