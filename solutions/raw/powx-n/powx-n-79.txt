class Solution {

    public double myPow(double x, int n) {
        if (n == 0) {
            return 1;
        }
        if (n < 0) {
            return 1.0 / power(x, Math.abs(n));
        }
        return power(x, Math.abs(n));
    }

    public double power(double x, int n) {
        if (n == 0) {
            return 1.0;
        } else if (n == 1) {
            return x;
        } else {
            double ans = power(x, n / 2);
            if (n % 2 == 0) {
                // don\'t use power(x,n/2)*power(x,n/2) here it gives Time Limit Exceeded error here for the test case 0.00001 , 2147483647
                return ans * ans;
            } else {
                // don\'t use power(x,n/2)*power(x,n/2)*x here it gives Time Limit Exceeded error here for the test case 0.00001 , 2147483647
                return ans * ans * x;
            }
        }
    }
}
