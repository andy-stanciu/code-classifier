class Solution {

    public boolean isHappy(int n) {
        while (n > 9) {
            int x = n;
            n = 0;
            while (x > 0) {
                n += Math.pow(x % 10, 2);
                x /= 10;
            }
        }
        return n == 1 || n == 7;
    }
}
