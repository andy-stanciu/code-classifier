/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public boolean hasPathSum(TreeNode root, int targetSum) {
        return traverseTree(root, targetSum, 0);
    }

    public boolean traverseTree(TreeNode root, int targetSum, int total) {
        if (root == null) {
            return false;
        }
        total = root.val + total;
        if (total == targetSum && root.left == null && root.right == null) {
            return true;
        }
        return traverseTree(root.left, targetSum, total) || traverseTree(root.right, targetSum, total);
    }
}
