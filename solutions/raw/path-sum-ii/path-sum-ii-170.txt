/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public static void dfs(TreeNode root, int targetSum, List<Integer> currentList, List<List<Integer>> result) {
        if (root == null) {
            return;
        }
        currentList.add(root.val);
        if (root.val == targetSum && root.left == null && root.right == null) {
            result.add(new ArrayList<Integer>(currentList));
        } else {
            dfs(root.left, targetSum - root.val, currentList, result);
            dfs(root.right, targetSum - root.val, currentList, result);
        }
        currentList.remove(currentList.size() - 1);
    }

    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {
        List<List<Integer>> result = new ArrayList<>();
        List<Integer> currentList = new ArrayList<>();
        dfs(root, targetSum, currentList, result);
        return result;
    }
}
