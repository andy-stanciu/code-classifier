/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {
        List<List<Integer>> ans = new ArrayList<>();
        List<Integer> local = new ArrayList<>();
        helper(root, targetSum, local, ans);
        return ans;
    }

    static void helper(TreeNode root, int targetSum, List<Integer> local, List<List<Integer>> ans) {
        if (root == null) {
            return;
        }
        if (root.left == null && root.right == null && targetSum == root.val) {
            List<Integer> copy = new ArrayList<>(local);
            copy.add(root.val);
            ans.add(copy);
            return;
        }
        local.add(root.val);
        helper(root.left, targetSum - root.val, local, ans);
        helper(root.right, targetSum - root.val, local, ans);
        local.remove(local.size() - 1);
        return;
    }
}
