/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {
        List<List<Integer>> result = new ArrayList<>();
        dfs(result, new ArrayList<>(), root, targetSum);
        return result;
    }

    private void dfs(List<List<Integer>> result, List<Integer> dfsList, TreeNode root, int targetSum) {
        if (root == null)
            return;
        dfsList.add(root.val);
        targetSum -= root.val;
        if (root.left == null && root.right == null) {
            //System.out.println(dfsList);
            if (targetSum == 0)
                result.add(new ArrayList<>(dfsList));
            targetSum += root.val;
        }
        dfs(result, dfsList, root.left, targetSum);
        dfs(result, dfsList, root.right, targetSum);
        dfsList.remove(dfsList.size() - 1);
    }
}
