/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {
        List<List<Integer>> ans = new ArrayList<>();
        dfs(root, 0, ans, new ArrayList<>(), targetSum);
        return ans;
    }

    private void dfs(TreeNode root, int rurnningSum, List<List<Integer>> ans, List<Integer> runningList, int target) {
        if (root == null) {
            return;
        }
        int totalCurrSum = root.val + rurnningSum;
        runningList.add(root.val);
        if (root.left == null && root.right == null && target == totalCurrSum) {
            ans.add(new ArrayList<Integer>(runningList));
            return;
        }
        dfs(root.right, rurnningSum + root.val, ans, new ArrayList<>(runningList), target);
        dfs(root.left, rurnningSum + root.val, ans, new ArrayList<>(runningList), target);
    }
}
