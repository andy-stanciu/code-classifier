/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    class TreeNodeM {

        TreeNode t;

        ArrayList<Integer> arr = new ArrayList<>();

        int target;

        public TreeNodeM(TreeNode t, ArrayList<Integer> arr, int target) {
            this.t = t;
            this.arr.addAll(arr);
            this.arr.add(t.val);
            this.target = target;
        }
    }

    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {
        Queue<TreeNodeM> qq = new LinkedList<>();
        List<List<Integer>> result = new ArrayList<>();
        if (root != null)
            qq.add(new TreeNodeM(root, new ArrayList<>(), targetSum));
        while (!qq.isEmpty()) {
            TreeNodeM curr = qq.poll();
            targetSum = curr.target - curr.t.val;
            if (curr.t.left == null && curr.t.right == null && targetSum == 0) {
                result.add(curr.arr);
            }
            if (curr.t.left != null) {
                qq.add(new TreeNodeM(curr.t.left, curr.arr, targetSum));
            }
            if (curr.t.right != null) {
                qq.add(new TreeNodeM(curr.t.right, curr.arr, targetSum));
            }
        }
        return result;
    }
}
