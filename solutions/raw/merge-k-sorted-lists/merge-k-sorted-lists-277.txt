class //using merging two sorted list
Solution {

    public ListNode merge(ListNode list1, ListNode list2) {
        ListNode temp1 = list1;
        ListNode temp2 = list2;
        ListNode head = new ListNode(100);
        ListNode temp = head;
        while (temp1 != null && temp2 != null) {
            if (temp1.val < temp2.val) {
                ListNode b = new ListNode(temp1.val);
                temp.next = b;
                temp = b;
                temp1 = temp1.next;
            } else {
                ListNode b = new ListNode(temp2.val);
                temp.next = b;
                temp = b;
                temp2 = temp2.next;
            }
        }
        if (temp2 == null) {
            temp.next = temp1;
        } else {
            temp.next = temp2;
        }
        return head.next;
    }

    public ListNode mergeKLists(ListNode[] lists) {
        if (lists.length == 0)
            return null;
        if (lists.length == 1)
            return lists[0];
        ListNode l1 = merge(lists[0], lists[1]);
        for (int i = 2; i < lists.length; i++) {
            l1 = merge(lists[i], l1);
        }
        return l1;
    }
}
