/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    void zbfs(TreeNode n, int level, List<List<Integer>> ans) {
        if (n == null) {
            return;
        }
        List<Integer> currLvl;
        if (ans.size() > level) {
            currLvl = ans.get(level);
        } else {
            currLvl = new LinkedList();
            ans.add(currLvl);
        }
        if (level % 2 == 0) {
            currLvl.add(n.val);
        } else {
            currLvl.add(0, n.val);
        }
        if (n.left != null) {
            zbfs(n.left, level + 1, ans);
        }
        if (n.right != null) {
            zbfs(n.right, level + 1, ans);
        }
    }

    public List<List<Integer>> zigzagLevelOrder(TreeNode root) {
        List<List<Integer>> ans = new LinkedList();
        zbfs(root, 0, ans);
        return ans;
    }
}
