/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<List<Integer>> zigzagLevelOrder(TreeNode root) {
        List<List<Integer>> ans = new ArrayList<>();
        if (root == null) {
            return ans;
        }
        Queue<TreeNode> q = new LinkedList<>();
        q.add(root);
        boolean LeftToRight = true;
        while (!q.isEmpty()) {
            int width = q.size();
            Deque<Integer> oneLevel = new LinkedList<>();
            for (int i = 0; i < width; i++) {
                TreeNode front = q.poll();
                if (LeftToRight) {
                    oneLevel.addLast(front.val);
                } else {
                    oneLevel.addFirst(front.val);
                }
                if (front.left != null) {
                    q.add(front.left);
                }
                if (front.right != null) {
                    q.add(front.right);
                }
            }
            LeftToRight = !LeftToRight;
            ans.add(new ArrayList(oneLevel));
        }
        return ans;
    }
}
