class Solution {

    public void sortColors(int[] nums) {
        sortTwoElements(nums, 0, 1);
        sortTwoElements(nums, 0, 2);
        sortTwoElements(nums, 1, 2);
    }

    private void sortTwoElements(int[] arr, int l, int r) {
        int i = 0;
        int j = arr.length - 1;
        while (i < j) {
            int valL = arr[i];
            int valR = arr[j];
            if (valL != l && valL != r) {
                i++;
            }
            if (valR != l && valR != r) {
                j--;
            }
            if (valL == l && valR == l) {
                i++;
            } else if (valL == l && valR == r) {
                i++;
                j--;
            } else if (valL == r && valR == l) {
                arr[i] = l;
                arr[j] = r;
                i++;
                j--;
            } else if (valL == r && valR == r) {
                j--;
            }
        }
    }
}
