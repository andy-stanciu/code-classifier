class Solution {

    public boolean isPalindrome(String s) {
        String str = s.replaceAll("[^a-zA-Z0-9]", "");
        String lo = str.toLowerCase();
        String r = new StringBuilder(lo).reverse().toString();
        return lo.equals(r);
    }
}
