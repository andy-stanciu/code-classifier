class Solution {

    public boolean isPalindrome(String s) {
        String updated = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
        String original = "";
        for (int i = updated.length() - 1; i >= 0; --i) {
            original += updated.charAt(i);
        }
        return updated.equals(original);
    }
}
