class Solution {

    public boolean isPalindrome(String s) {
        String new_s = s.toLowerCase().replaceAll("[^a-zA-Z0-9]", "");
        if (new_s.length() == 0)
            return true;
        int l = 0;
        int r = new_s.length() - 1;
        while (l < r) {
            if (new_s.charAt(l) != new_s.charAt(r)) {
                return false;
            }
            l++;
            r--;
        }
        return true;
    }
}
