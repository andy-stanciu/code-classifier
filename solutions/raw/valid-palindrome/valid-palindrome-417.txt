class Solution {

    public boolean isPalindrome(String s) {
        String str = s.chars().filter(Character::isLetterOrDigit).mapToObj(c -> (char) c).map(Character::toLowerCase).map(String::valueOf).collect(Collectors.joining());
        return str.equals(new StringBuilder(str).reverse().toString());
    }
}
