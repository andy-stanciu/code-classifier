class Solution {

    public boolean isPalindrome(String s) {
        s = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
        s = s.replaceAll("\\\\s", "");
        StringBuilder sbr = new StringBuilder(s);
        sbr.reverse();
        return (sbr.toString().equals(s));
    }
}
