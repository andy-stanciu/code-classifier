class Solution {

    public boolean isPalindrome(String s) {
        String sStripped = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
        String reversed = new StringBuilder(sStripped).reverse().toString();
        return sStripped.equals(reversed);
    }
}
