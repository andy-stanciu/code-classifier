class Solution {

    public boolean isPalindrome(String s) {
        s = s.toLowerCase().replaceAll("[^a-z0-9]", "");
        StringBuilder sb1 = new StringBuilder(s);
        return sb1.reverse().toString().equals(s);
    }
}
