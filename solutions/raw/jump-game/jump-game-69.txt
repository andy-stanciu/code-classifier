class Solution {

    public boolean canJump(int[] nums) {
        int n = nums.length;
        int maxJump = 0, maxLen = n - 1;
        for (int i = 0; i < n - 1 && maxJump >= i; i++) if (nums[i] + i > maxJump)
            maxJump = nums[i] + i;
        return maxJump >= maxLen;
    }
}
