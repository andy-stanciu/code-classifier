class Solution {

    public boolean canJump(int[] nums) {
        int maxDestination = 0;
        for (int i = 0; i < nums.length; i++) {
            if (i > maxDestination) {
                return false;
            }
            maxDestination = Math.max(maxDestination, i + nums[i]);
        }
        return true;
    }
}
