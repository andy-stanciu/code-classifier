class Solution {

    public boolean canJump(int[] nums) {
        var goal = nums.length - 1;
        for (// Iterating backwards
        // Iterating backwards
        // Iterating backwards
        int i = nums.length - 2; i >= 0; i--) if (// if from previous element can we jump to current ?
        i + nums[i] >= goal)
            // if yes then change goal to previous
            goal = i;
        //If we can reach to beginning means we have a path
        return goal == 0;
    }
}
