class Solution {

    public boolean canJump(int[] nums) {
        int mx = nums[0];
        for (int i = 1; i < nums.length; i++) {
            if (mx == 0)
                return false;
            mx = Math.max(nums[i], mx - 1);
        }
        return true;
    }
}
