class Solution {

    public boolean canJump(int[] nums) {
        int dest = nums.length - 1, pos = dest - 1;
        while (pos >= 0) {
            if (pos + nums[pos] >= dest) {
                dest = pos;
            }
            pos--;
        }
        return dest == 0;
    }
}
