class Solution {

    public boolean canJump(int[] nums) {
        int ind = 0, maxReach = 0;
        while (ind < nums.length && ind <= maxReach) {
            maxReach = Math.max(maxReach, ind + nums[ind]);
            ind++;
        }
        return maxReach >= nums.length - 1;
    }
}
