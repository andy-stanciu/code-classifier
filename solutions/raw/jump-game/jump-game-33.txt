class Solution {

    public boolean canJump(int[] nums) {
        int steps = 1;
        for (int i = nums.length - 2; i >= 0; i--) {
            if (steps > nums[i]) {
                steps++;
            } else {
                steps = 1;
            }
        }
        return steps == 1;
    }
}
