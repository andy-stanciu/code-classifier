class Solution {

    public boolean canJump(int[] nums) {
        int steps = 0;
        for (int n : nums) {
            if (steps < 0) {
                return false;
            } else if (n > steps) {
                steps = n;
            }
            steps--;
        }
        return true;
    }
}
