class Solution {

    public boolean canJump(int[] nums) {
        int jump = 1;
        for (int i = nums.length - 2; i >= 0; i--) {
            if (nums[i] < jump) {
                jump++;
            } else {
                jump = 1;
            }
        }
        return jump == 1;
    }
}
