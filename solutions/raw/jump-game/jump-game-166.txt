class Solution {

    public boolean canJump(int[] nums) {
        int reachable = nums.length - 1;
        for (int i = reachable; i >= 0; i--) {
            if (i + nums[i] >= reachable) {
                reachable = i;
            }
        }
        return reachable == 0;
    }
}
