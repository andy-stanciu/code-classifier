class Solution {

    public boolean canJump(int[] nums) {
        int finalTarget = nums.length - 1;
        for (int i = finalTarget; i >= 0; i--) {
            if (i + nums[i] >= finalTarget)
                finalTarget = i;
        }
        return finalTarget == 0;
    }
}
