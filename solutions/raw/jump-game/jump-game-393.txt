class Solution {

    public boolean canJump(int[] nums) {
        int val = 0, len = nums.length - 1;
        for (int i = 0; i <= len; i++) {
            // condition 1:
            if (val < i)
                return false;
            val = Math.max(val, nums[i] + i);
            // condition 2:
            if (val >= len)
                break;
        }
        return true;
    }
}
