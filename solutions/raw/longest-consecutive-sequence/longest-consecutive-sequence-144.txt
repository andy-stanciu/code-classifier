class Solution {

    public int longestConsecutive(int[] nums) {
        int n = nums.length, count = 1, longest = 0;
        if (n == 0)
            return 0;
        Set<Integer> ans = new HashSet<>();
        for (int i = 0; i < n; i++) {
            ans.add(nums[i]);
        }
        for (int i : ans) {
            if (!ans.contains(i - 1)) {
                count = 1;
                int x = i;
                while (ans.contains(x + 1)) {
                    x += 1;
                    count += 1;
                }
                longest = Math.max(longest, count);
            }
        }
        return longest;
    }
}
