class Solution {

    public int longestConsecutive(int[] nums) {
        int result = 0;
        if (nums == null) {
            return result;
        }
        Set<Integer> numbersSet = new HashSet<>();
        for (int num : nums) {
            numbersSet.add(num);
        }
        for (int set : numbersSet) {
            if (!numbersSet.contains(set - 1)) {
                // Start with the current number
                int currentNum = set;
                int currentSeqCount = 1;
                // Continue counting consecutive numbers
                while (numbersSet.contains(currentNum + 1)) {
                    currentNum++;
                    currentSeqCount++;
                }
                // Update result if the current sequence is longer
                result = Math.max(result, currentSeqCount);
            }
        }
        return result;
    }
}
