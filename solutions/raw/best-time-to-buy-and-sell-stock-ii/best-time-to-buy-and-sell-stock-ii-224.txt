class Solution {

    public int maxProfit(int[] prices) {
        int totalProfit = 0;
        int last = prices[0];
        for (int price : prices) {
            if (price > last) {
                totalProfit += price - last;
            }
            last = price;
        }
        return totalProfit;
    }
}
