class Solution {

    public int maxProfit(int[] prices) {
        int total = 0;
        int maxprf = 0;
        int current = prices[0];
        int len = prices.length;
        for (int i = 1; i < len; i++) {
            if (prices[i] > current) {
                maxprf = prices[i] - current;
                total += maxprf;
            }
            current = prices[i];
        }
        return total;
    }
}
