class Solution {

    public int maxProfit(int[] prices) {
        int profit = 0, low = prices[0];
        for (int p : prices) {
            if (p > low) {
                profit += Math.max(0, p - low);
            }
            low = p;
        }
        return profit;
    }
}
