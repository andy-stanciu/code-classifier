class Solution {

    public int minSubArrayLen(int target, int[] nums) {
        int n = nums.length;
        int i = 0;
        int j = 0;
        // Initialize minl to a value greater than n
        int minl = n + 1;
        int sum = 0;
        while (j < n) {
            sum += nums[j];
            while (sum >= target) {
                // Use nested loop to shrink the window
                minl = Math.min(minl, j - i + 1);
                sum -= nums[i];
                i++;
            }
            j++;
        }
        // Return 0 if no subarray found
        return (minl != n + 1) ? minl : 0;
    }
}
