class Solution {

    public int minSubArrayLen(int target, int[] nums) {
        int result = 0;
        int tempLength = 0;
        int left = 0;
        int right = -1;
        int sum = nums[left];
        for (int i = 0; i < nums.length; i++) {
            sum = sum - nums[left];
            left = i;
            if (sum >= target) {
                tempLength = (right - left) + 1;
            } else if (sum < target) {
                for (int j = right + 1; j < nums.length; j++) {
                    sum = sum + nums[j];
                    if (sum >= target) {
                        right = j;
                        tempLength = (right - left) + 1;
                        break;
                    }
                    if (j == (nums.length - 1)) {
                        return result;
                    }
                }
            }
            if (tempLength == 1) {
                return 1;
            } else if (result == 0) {
                result = tempLength;
            } else if (tempLength < result) {
                result = tempLength;
            }
        }
        return result;
    }
}
