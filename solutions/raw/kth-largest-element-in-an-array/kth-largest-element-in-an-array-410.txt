class Solution {

    public int findKthLargest(int[] nums, int k) {
        Arrays.sort(nums);
        int a = nums.length - k;
        for (int i = 0; i < nums.length; i++) {
            if (i == a) {
                return nums[i];
            }
        }
        return -1;
    }
}
