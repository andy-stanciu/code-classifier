class Solution {

    public int findKthLargest(int[] nums, int k) {
        PriorityQueue<Integer> numbers = new PriorityQueue<>(k);
        for (int i : nums) {
            // OR numbers.add(i);
            numbers.offer(i);
            if (numbers.size() > k)
                numbers.poll();
        }
        return numbers.peek();
    }
}
