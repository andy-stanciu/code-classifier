class Solution {

    public int findKthLargest(int[] nums, int k) {
        PriorityQueue<Integer> pr = new PriorityQueue<>();
        for (int i : nums) {
            pr.add(-i);
        }
        for (int i = 0; i < k - 1; i++) pr.poll();
        return -pr.poll();
    }
}
