class Solution {

    public int findKthLargest(int[] nums, int k) {
        Integer[] integerArray = Arrays.stream(nums).boxed().toArray(Integer[]::new);
        Arrays.sort(integerArray, Collections.reverseOrder());
        return integerArray[k - 1];
    }
}
