class Solution {

    public int findKthLargest(int[] nums, int k) {
        Arrays.sort(nums);
        int n = nums.length;
        int resultPosition = n - k;
        int result = nums[resultPosition];
        return result;
    }
}
