class Solution {

    public int findKthLargest(int[] nums, int k) {
        int n = nums.length;
        Arrays.sort(nums);
        for (int i = n - 1; i >= 0; i--) {
            k--;
            if (k == 0) {
                return nums[i];
            }
        }
        return 0;
    }
}
