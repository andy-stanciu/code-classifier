class Solution {

    public List<List<Integer>> combine(int n, int k) {
        List<List<Integer>> res = new ArrayList<List<Integer>>();
        List<Integer> arr = new ArrayList<Integer>();
        getCombinations(res, arr, 1, n, k);
        return res;
    }

    private void getCombinations(List<List<Integer>> res, List<Integer> arr, int start, int n, int k) {
        if (arr.size() == k) {
            res.add(new ArrayList<>(arr));
            return;
        }
        for (int j = start; j <= n; j++) {
            arr.add(j);
            getCombinations(res, arr, j + 1, n, k);
            arr.remove(arr.size() - 1);
        }
    }
}
