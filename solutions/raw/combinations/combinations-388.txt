class Solution {

    public List<List<Integer>> combine(int n, int k) {
        List<List<Integer>> ans = new ArrayList();
        List<Integer> list = new ArrayList();
        for (int i = 0; i < n - k + 1; i++) {
            //recursion with new beginning
            rec(i, n, k, list, ans);
        }
        return ans;
    }

    public void rec(int i, int n, int k, List<Integer> list, List<List<Integer>> ans) {
        //add curr number
        list.add(i + 1);
        if (k == 1) {
            //if last number then add to ans
            ans.add(new ArrayList(list));
        } else {
            for (int j = i + 1; j < n - k + 2; j++) {
                //go only till n-k+2 as if we consdier numbers till end we wont have enough numbers to take k-->0
                rec(j, n, k - 1, list, ans);
            }
        }
        //remove curr number for backtracking
        list.remove(list.size() - 1);
    }
}
