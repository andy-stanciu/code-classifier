class Solution {

    public List<List<Integer>> combine(int n, int k) {
        List<List<Integer>> res = new ArrayList<>();
        generateCombinations(1, n, k, new ArrayList(), res);
        return res;
    }

    public void generateCombinations(int index, int n, int k, List<Integer> curr, List<List<Integer>> res) {
        for (int i = index; i <= n; i++) {
            curr.add(i);
            if (curr.size() == k) {
                res.add(new ArrayList(curr));
            }
            generateCombinations(i + 1, n, k, curr, res);
            curr.remove(curr.size() - 1);
        }
    }
}
