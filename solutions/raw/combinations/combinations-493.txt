class Solution {

    List<List<Integer>> result = new ArrayList<>();

    public List<List<Integer>> combine(int n, int k) {
        backtracking(n, k, 1, new ArrayList<>());
        return result;
    }

    public void backtracking(int n, int k, int startIndex, List<Integer> path) {
        // if the path satify
        if (path.size() == k) {
            result.add(new ArrayList<>(path));
            return;
        }
        // loop the optional list [startIndex,n]
        for (int i = startIndex; i <= n; i++) {
            // in current option,choose the i
            path.add(i);
            backtracking(n, k, i + 1, path);
            // in current option,don\'t choose i,so remove from path
            path.remove(path.size() - 1);
        }
    }
}
