/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public boolean isSymmetric(TreeNode root) {
        if (root != null) {
            return isSymmetricCheck(root.left, root.right);
        } else {
            return true;
        }
    }

    public boolean isSymmetricCheck(TreeNode left, TreeNode right) {
        if (left == null && right != null || left != null && right == null) {
            return false;
        } else if (left == null && right == null) {
            return true;
        } else {
            if (left.val == right.val) {
                boolean x = isSymmetricCheck(left.left, right.right);
                boolean y = isSymmetricCheck(left.right, right.left);
                return (x & y);
            } else {
                return false;
            }
        }
    }
}
