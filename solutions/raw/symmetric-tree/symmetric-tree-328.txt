/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public boolean isSymmetric(TreeNode root) {
        return checkSymmetric(root.left, root.right);
    }

    public boolean checkSymmetric(TreeNode K, TreeNode L) {
        if ((K == null) && (L == null))
            return true;
        if (K == null || L == null)
            return false;
        if (K.val == L.val) {
            return checkSymmetric(K.right, L.left) && checkSymmetric(K.left, L.right);
        }
        return false;
    }
}
