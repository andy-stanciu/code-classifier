/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public boolean isSymmetricA(TreeNode rootL, TreeNode rootR) {
        if (rootL == null || rootR == null) {
            return rootL == rootR;
        }
        return (rootL.val == rootR.val && isSymmetricA(rootL.left, rootR.right) && isSymmetricA(rootL.right, rootR.left));
    }

    public boolean isSymmetric(TreeNode root) {
        if (root == null) {
            return true;
        }
        return isSymmetricA(root.left, root.right);
    }
}
