/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public boolean isSymmetric(TreeNode root) {
        return root == null || isSymmetricOrNot(root.left, root.right);
    }

    boolean isSymmetricOrNot(TreeNode leftNode, TreeNode rightNode) {
        // Check if leftN or rightN is null
        if (leftNode == null || rightNode == null) {
            return leftNode == rightNode;
        }
        // Check if leftN.val & rightN.val are same?
        if (leftNode.val != rightNode.val)
            return false;
        return isSymmetricOrNot(leftNode.left, rightNode.right) && isSymmetricOrNot(leftNode.right, rightNode.left);
    }
}
