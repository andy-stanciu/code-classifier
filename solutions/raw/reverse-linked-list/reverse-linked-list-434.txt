class Solution {

    public ListNode reverseList(ListNode head) {
        ListNode re = null;
        while (head != null) {
            ListNode temp = new ListNode(head.val, re);
            re = temp;
            head = head.next;
        }
        return re;
    }
}
