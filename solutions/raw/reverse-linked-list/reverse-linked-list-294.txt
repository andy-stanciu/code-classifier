class Solution {

    public ListNode reverseList(ListNode head) {
        ListNode prev = null;
        while (head != null) {
            ListNode tempNext = prev;
            prev = head;
            head = head.next;
            prev.next = tempNext;
        }
        return prev;
    }
}
