class Solution {

    public ListNode reverseList(ListNode head) {
        return reverse(head, null);
    }

    public ListNode reverse(ListNode head, ListNode result) {
        if (head == null)
            return result;
        return reverse(head.next, new ListNode(head.val, result));
    }
}
