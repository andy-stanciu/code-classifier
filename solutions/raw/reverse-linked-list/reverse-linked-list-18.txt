class Solution {

    public ListNode reverseList(ListNode head) {
        // ListNode cur=head;
        // ListNode prev=null;
        // if(head==null)
        //  return head;
        // ListNode nextt=cur.next;
        // while(cur.next!=null)
        //     {
        //     cur.next=prev;
        //     prev=cur;
        //     cur=nextt;
        //     nextt=nextt.next;
        //     }
        //     cur.next=prev;
        //     return cur;
        ListNode current = head;
        ListNode next = null;
        ListNode prev = null;
        while (current != null) {
            next = current.next;
            current.next = prev;
            prev = current;
            current = next;
        }
        return prev;
    }
}
