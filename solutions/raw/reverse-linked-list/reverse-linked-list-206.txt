class Solution {

    ///The variables are used for Recursively way
    public ListNode result = null;

    public ListNode current = null;

    public ListNode reverseList(ListNode head) {
        //// Iteratively
        /*
		if(head == null){
            return null;
        }
        ListNode result = null;
        ListNode current = null;
        while(head != null){
            current = new ListNode(head.val);
            current.next = result;
            result = current;
            head = head.next;
        }
        */
        //// Recursively
        /*
		if(head != null){
            current = new ListNode(head.val);
            current.next = result;
            result = current;
            reverseList(head.next);
        }
        */
        return result;
    }
}
