class Solution {

    public ListNode reverseList(ListNode head) {
        ListNode prev = null;
        ListNode t;
        while (head != null) {
            t = head.next;
            head.next = prev;
            prev = head;
            head = t;
        }
        return prev;
    }
}
