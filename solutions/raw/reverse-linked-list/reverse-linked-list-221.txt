class Solution {

    public ListNode reverseList(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode ptr = null;
        while (head != null) {
            ListNode temp = head.next;
            head.next = ptr;
            ptr = head;
            head = temp;
        }
        return ptr;
    }
}
