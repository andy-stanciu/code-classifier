class Solution {

    public ListNode reverseList(ListNode head) {
        ListNode previous = null, next = null;
        while (head != null) {
            next = head.next;
            head.next = previous;
            previous = head;
            head = next;
        }
        return previous;
    }
}
