/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public List<Integer> postorderTraversal(TreeNode root) {
        List<Integer> post = new ArrayList<Integer>();
        postOrderHelper(root, post);
        return post;
    }

    static void postOrderHelper(TreeNode root, List<Integer> p) {
        if (root == null)
            return;
        postOrderHelper(root.left, p);
        postOrderHelper(root.right, p);
        p.add(root.val);
    }
}
