// brut force recursive
class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int[] temp = Arrays.copyOfRange(nums1, 0, m);
        getMerged(nums1, temp, nums2, 0, 0);
    }

    private void getMerged(int[] arr, int[] first, int[] second, int i, int j) {
        if (i + j >= arr.length)
            return;
        if (i < first.length && j < second.length) {
            if (first[i] < second[j]) {
                arr[i + j] = first[i];
                getMerged(arr, first, second, i + 1, j);
            } else {
                arr[i + j] = second[j];
                getMerged(arr, first, second, i, j + 1);
            }
        } else if (i < first.length) {
            arr[i + j] = first[i];
            getMerged(arr, first, second, i + 1, j);
        } else if (j < second.length) {
            arr[i + j] = second[j];
            getMerged(arr, first, second, i, j + 1);
        }
    }
}
