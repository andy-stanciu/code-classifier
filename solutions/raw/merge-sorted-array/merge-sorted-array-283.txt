class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int c = m;
        for (int i = m; i < m + n; i++) {
            nums1[i] = nums2[c - m];
            c++;
        }
        Arrays.sort(nums1);
        System.out.println(nums1);
    }
}
