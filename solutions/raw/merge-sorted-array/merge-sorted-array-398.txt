class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int i = 0;
        int p = m;
        while (p < nums1.length) {
            nums1[p++] = nums2[i++];
        }
        Arrays.sort(nums1);
    }
}
