class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {
        for (int i = nums1.length - 1; i >= m; i--, n--) {
            nums1[i] += nums2[n - 1];
        }
        Arrays.sort(nums1);
    }
}
