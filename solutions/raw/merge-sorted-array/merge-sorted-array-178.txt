class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int index = nums1.length - 1;
        for (int val : nums2) {
            nums1[index--] = val;
        }
        Arrays.sort(nums1);
    }
}
