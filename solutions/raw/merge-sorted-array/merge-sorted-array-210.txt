class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int len = nums1.length;
        int indexNums2 = 0;
        for (int i = m; i < len; i++) {
            nums1[i] = nums2[indexNums2];
            indexNums2++;
        }
        Arrays.sort(nums1);
    }
}
