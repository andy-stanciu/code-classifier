class Solution {

    public void merge(int[] nums1, int m, int[] nums2, int n) {
        // pretend int[] copy is a different array than nums1 that only contains the numbers in nums1, no trailing zeros.
        int[] copy = nums1;
        int copyIdx = m - 1;
        int nums2Idx = n - 1;
        int nums1Idx = nums1.length - 1;
        // now just insert the numbers one by one at the starting at the end , biggest to smallest.
        while (copyIdx >= 0 && nums2Idx >= 0) {
            int copyVal = copy[copyIdx];
            int nVal = nums2[nums2Idx];
            if (copyVal > nVal) {
                nums1[nums1Idx] = copyVal;
                copyIdx--;
            } else {
                nums1[nums1Idx] = nVal;
                nums2Idx--;
            }
            nums1Idx--;
        }
        while (nums2Idx >= 0) {
            nums1[nums1Idx] = nums2[nums2Idx];
            nums2Idx--;
            nums1Idx--;
        }
    }
}
