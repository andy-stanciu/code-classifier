class Solution {

    List<List<String>> res = new ArrayList<>();

    int n;

    public List<List<String>> partition(String s) {
        n = s.length();
        recur(s, 0, new ArrayList<>());
        return res;
    }

    public boolean ispalindrome(String s, int start, int end) {
        while (start < end) {
            if (s.charAt(start) != s.charAt(end)) {
                return false;
            }
            start++;
            end--;
        }
        return true;
    }

    public void recur(String s, int part_index, List<String> sublist) {
        if (part_index == n) {
            res.add(new ArrayList<>(sublist));
            return;
        }
        for (int end = part_index; end < n; end++) {
            if (ispalindrome(s, part_index, end)) {
                // substring method is underebond exclusive
                sublist.add(s.substring(part_index, end + 1));
                // recursive
                recur(s, end + 1, sublist);
                // backtrack
                sublist.remove(sublist.size() - 1);
            }
        }
    }
}
