class Solution {

    public static boolean common(String s, char ch, int i) {
        if (s.charAt(i) == ch) {
            return true;
        }
        return false;
    }

    public static int minlength(String[] strs) {
        int minLength = Integer.MAX_VALUE;
        for (String str : strs) {
            if (str.length() < minLength) {
                minLength = str.length();
            }
        }
        return minLength;
    }

    public String longestCommonPrefix(String[] strs) {
        if (strs.length == 1) {
            return strs[0];
        }
        int n = minlength(strs);
        StringBuilder x = new StringBuilder();
        for (int i = 0; i < n; i++) {
            char ch = strs[0].charAt(i);
            boolean iscommon = true;
            for (int j = 1; j < strs.length; j++) {
                String s = strs[j];
                if (!common(s, ch, i)) {
                    iscommon = false;
                    break;
                }
            }
            if (iscommon) {
                x.append(ch);
            } else {
                break;
            }
        }
        return x.toString();
    }
}
