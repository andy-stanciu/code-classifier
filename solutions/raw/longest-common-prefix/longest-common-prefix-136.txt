class Solution {

    public String longestCommonPrefix(String[] strs) {
        return commonString(strs, shortestString(strs, strs[0], 1, strs.length - 1), 0, strs.length - 1);
    }

    public String shortestString(String[] str, String ans, int s, int e) {
        if (e < s) {
            return ans;
        }
        if (ans.length() > str[s].length()) {
            ans = str[s];
        }
        return shortestString(str, ans, s + 1, e);
    }

    public String commonString(String[] str, String ans, int s, int e) {
        if (e < s) {
            return ans;
        }
        int val = ans.length();
        if (!ans.equals(str[s].substring(0, val))) {
            return commonString(str, ans.substring(0, val - 1), s, e);
        }
        return commonString(str, ans, s + 1, e);
    }
}
