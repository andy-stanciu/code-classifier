class Solution {

    public String longestCommonPrefix(String[] strs) {
        String firstString = strs[0];
        int firstStringLen = firstString.length();
        for (String currentString : strs) {
            while (!currentString.startsWith(firstString)) firstString = firstString.substring(0, --firstStringLen);
        }
        return firstString;
    }
}
