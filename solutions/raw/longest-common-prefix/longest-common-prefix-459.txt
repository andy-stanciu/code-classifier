class Solution {

    public String longestCommonPrefix(String[] strs) {
        String first = strs[0];
        int longest = strs[0].length();
        for (int i = 1; i < strs.length; i++) {
            int len = 0;
            int j = 0;
            for (j = 0; j < strs[i].length() && j < first.length(); j++) {
                if (first.charAt(j) == strs[i].charAt(j)) {
                    len++;
                } else {
                    break;
                }
            }
            longest = Math.min(len, longest);
        }
        return first.substring(0, longest);
    }
}
