class Solution {

    public String longestCommonPrefix(String[] strs) {
        // edge case
        if (strs.length == 1) {
            return strs[0];
        }
        // populating strinbuilder
        StringBuilder string = new StringBuilder();
        // looping through to find word with lowest length
        int minLen = Integer.MAX_VALUE;
        for (int i = 0; i < strs.length; i++) {
            if (strs[i].length() < minLen) {
                minLen = strs[i].length();
            }
        }
        // looping through and checking if each element has the beggining i elements
        // otherwise just return whats been added to the string
        for (int i = 0; i < minLen; i++) {
            char currChar = strs[0].charAt(i);
            for (int j = 1; j < strs.length; j++) {
                if (strs[j].charAt(i) != currChar) {
                    return string.toString();
                }
            }
            string.append(currChar);
        }
        return string.toString();
    }
}
