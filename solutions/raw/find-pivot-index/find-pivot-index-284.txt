class Solution {

    public int pivotIndex(int[] nums) {
        int[] leftSum = new int[nums.length];
        int[] rightSum = new int[nums.length];
        leftSum[0] = nums[0];
        rightSum[nums.length - 1] = nums[nums.length - 1];
        //find the sum of element to the left of index
        for (int i = 1; i < nums.length; i++) {
            leftSum[i] = leftSum[i - 1] + nums[i];
        }
        //find the sum of elements to the right of index
        for (int j = nums.length - 2; j >= 0; j--) {
            rightSum[j] = rightSum[j + 1] + nums[j];
        }
        //find the index where the value is same in both leftSum and rightSum arrays
        for (int k = 0; k < nums.length; k++) {
            if (leftSum[k] == rightSum[k]) {
                return k;
            }
        }
        return -1;
    }
}
