class Solution {

    public int minEatingSpeed(int[] piles, int h) {
        Arrays.sort(piles);
        int low = 0, high = piles[piles.length - 1], mid;
        while (low <= high) {
            mid = (low + high) / 2;
            int ans = this.countHours(piles, mid);
            if (ans <= h)
                high = mid - 1;
            else
                low = mid + 1;
        }
        return low;
    }

    public int countHours(int[] bananas, int k) {
        int ans = 0;
        for (Integer i : bananas) ans += (Math.ceil((double) i / (double) k));
        return ans;
    }
}
