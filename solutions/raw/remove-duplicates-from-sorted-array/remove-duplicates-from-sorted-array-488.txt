class Solution {

    public int removeDuplicates(int[] arr) {
        int k = 1;
        int i = 1;
        while (i < arr.length) {
            if (arr[i - 1] != arr[i]) {
                arr[k++] = arr[i];
            }
            i++;
        }
        return k;
    }
}
