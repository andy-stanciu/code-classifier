class Solution {

    public int removeDuplicates(int[] nums) {
        int up = 1;
        int length = nums.length;
        for (int i = 1; i < length; i++) {
            if (nums[i] != nums[i - 1]) {
                nums[up++] = nums[i];
            }
        }
        return up;
    }
}
