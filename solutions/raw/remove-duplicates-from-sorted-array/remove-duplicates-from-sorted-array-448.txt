class Solution {

    public int removeDuplicates(int[] nums) {
        TreeSet<Integer> num = new TreeSet<>();
        for (int value : nums) {
            num.add(value);
        }
        int index = 0;
        for (int value : num) {
            nums[index++] = value;
        }
        return num.size();
    }
}
