// Method 3 (Using Extra Space)
class Solution {

    public int removeDuplicates(int[] arr) {
        int j = 0;
        LinkedHashSet<Integer> lhs = new LinkedHashSet<>();
        for (int val : arr) lhs.add(val);
        for (int key : lhs) arr[j++] = key;
        return j;
    }
}
