class Solution {

    public int removeDuplicates(int[] nums) {
        int sorted = 0;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] <= nums[sorted])
                continue;
            sorted++;
            nums[sorted] = nums[i];
        }
        return sorted + 1;
    }
}
