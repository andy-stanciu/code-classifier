class Solution {

    public int removeDuplicates(int[] nums) {
        if (nums.length == 0)
            return 0;
        // first I define index
        int index = 1;
        for (int num : nums) {
            if (num != nums[index - 1]) {
                nums[index] = num;
                index++;
            }
        }
        return index;
    }
}
