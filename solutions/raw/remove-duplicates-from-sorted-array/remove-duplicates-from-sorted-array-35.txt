class Solution {

    public int removeDuplicates(int[] nums) {
        int j = 0;
        int prev = -898;
        for (int i : nums) {
            if (prev != i) {
                nums[j++] = i;
            }
            prev = i;
        }
        return j;
    }
}
