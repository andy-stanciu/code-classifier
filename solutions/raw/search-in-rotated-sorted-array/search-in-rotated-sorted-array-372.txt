class Solution {

    public int search(int[] nums, int target) {
        /**
         * naive approach using linear search
         */
        int index = -1;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] == target)
                return index = i;
        }
        return index;
    }
}
