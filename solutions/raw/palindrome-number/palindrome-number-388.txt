class Solution {

    public boolean isPalindrome(int x) {
        int p = 0;
        for (int i = x; i > 0; i /= 10) {
            p = (p * 10) + (i % 10);
        }
        if (p == x)
            return true;
        return false;
    }
}
