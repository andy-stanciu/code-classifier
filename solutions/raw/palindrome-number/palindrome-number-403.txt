class Solution {

    public boolean isPalindrome(int x) {
        int n = 0;
        int i = x;
        while (i > 0) {
            n = (n * 10) + (i % 10);
            i = (i - (i % 10)) / 10;
        }
        if (n == x)
            return true;
        else
            return false;
    }
}
