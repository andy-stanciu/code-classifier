class Solution {

    public boolean isPalindrome(int x) {
        if (x < 0)
            return false;
        int y = 0;
        int num = x;
        while (num > 0) {
            y = (y * 10) + num % 10;
            num /= 10;
        }
        return (x == y);
    }
}
