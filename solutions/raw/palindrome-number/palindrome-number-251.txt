class Solution {

    public boolean isPalindrome(int x) {
        String reverse = "";
        String integ = Integer.toString(x);
        for (int i = integ.length() - 1; i >= 0; i--) {
            reverse += integ.charAt(i);
        }
        return integ.equals(reverse);
    }
}
