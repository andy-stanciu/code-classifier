class Solution {

    public boolean isPalindrome(int x) {
        int k = x;
        int s = 0;
        while (k > 0) {
            s = k % 10 + s * 10;
            k /= 10;
        }
        return s == x;
    }
}
