class Solution {

    public boolean isPalindrome(int x) {
        return new StringBuilder("" + x).reverse().toString().equals("" + x);
        // if this solution helped pls, give it a thumbsup
    }
}
