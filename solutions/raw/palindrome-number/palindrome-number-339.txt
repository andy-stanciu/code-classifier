class Solution {

    public boolean isPalindrome(int x) {
        String number = String.valueOf(x);
        StringBuilder str = new StringBuilder(number);
        String nstr = str.reverse().toString();
        if (number.equals(nstr)) {
            return true;
        } else {
            return false;
        }
    }
}
