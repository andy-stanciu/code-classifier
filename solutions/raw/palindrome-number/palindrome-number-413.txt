class Solution {

    public boolean isPalindrome(int x) {
        int temp = x;
        int r = 0;
        if (x < 0) {
            return false;
        }
        while (temp != 0) {
            r = r * 10 + temp % 10;
            temp = temp / 10;
        }
        return (r == x);
    }
}
