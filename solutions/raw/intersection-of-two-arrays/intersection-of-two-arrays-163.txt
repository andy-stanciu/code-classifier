class Solution {

    public int[] intersection(int[] nums1, int[] nums2) {
        List<Integer> arr = new ArrayList<>();
        //to check if element is present in both arrays
        for (int i = 0; i < nums1.length; i++) {
            for (int j = 0; j < nums2.length; j++) {
                if (nums1[i] == nums2[j]) {
                    //to check if the common element is already present in list
                    if (!isPresent(arr, nums1[i]))
                        arr.add(nums1[i]);
                }
            }
        }
        //converting list to array
        int[] res = new int[arr.size()];
        for (int i = 0; i < arr.size(); i++) {
            res[i] = arr.get(i);
        }
        return res;
    }

    //function to check if value is already present in list
    private boolean isPresent(List<Integer> arr, int val) {
        for (int i = 0; i < arr.size(); i++) {
            int num = arr.get(i);
            if (num == val)
                return true;
        }
        return false;
    }
}
