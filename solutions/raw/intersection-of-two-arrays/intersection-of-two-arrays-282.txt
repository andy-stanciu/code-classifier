class Solution {

    public int[] intersection(int[] nums1, int[] nums2) {
        var set = new HashSet<Integer>();
        for (var num : nums1) {
            set.add(num);
        }
        return Arrays.stream(nums2).boxed().filter(x -> set.contains(x)).distinct().mapToInt(Integer::intValue).toArray();
    }
}
