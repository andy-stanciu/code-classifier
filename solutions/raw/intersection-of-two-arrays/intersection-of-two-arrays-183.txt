class Solution {

    public int[] intersection(int[] nums1, int[] nums2) {
        Arrays.sort(nums1);
        Arrays.sort(nums2);
        ArrayList<Integer> list = new ArrayList<>();
        int i = 0;
        int j = 0;
        while (i < nums1.length && j < nums2.length) {
            if (nums1[i] < nums2[j])
                i++;
            else if (nums1[i] > nums2[j])
                j++;
            else {
                // If nums1[i] == nums2[j], it\'s an intersection
                // Only add if it\'s not already in the intersectionList
                if (list.isEmpty() || list.get(list.size() - 1) != nums1[i])
                    list.add(nums1[i]);
                i++;
                j++;
            }
        }
        // Convert ArrayList<Integer> to Integer[] and then to int[]
        Integer[] tempArray = list.toArray(new Integer[0]);
        int[] arr = new int[tempArray.length];
        for (int k = 0; k < arr.length; k++) {
            arr[k] = tempArray[k];
        }
        return arr;
    }
}
