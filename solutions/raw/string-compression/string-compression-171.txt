class Solution {

    public int compress(char[] chars) {
        int index = 0;
        int count = 0;
        int n = chars.length;
        int i = 0;
        while (i < n) {
            char current = chars[i];
            count = 0;
            while (i < n && current == chars[i]) {
                count++;
                i++;
            }
            // put current char at index
            chars[index++] = current;
            // now put the count if count is >=10 i needto conevrt into char
            // so that i can put in the chars 1,0
            if (count > 1) {
                String str = Integer.toString(count);
                for (char c : str.toCharArray()) {
                    chars[index++] = c;
                }
            }
        }
        return index;
    }
}
