class Solution {

    public int compress(char[] chars) {
        if (chars.length == 1) {
            return chars.length;
        }
        int groupSize;
        StringBuilder stringBuilder = new StringBuilder();
        for (int i = 0; i < chars.length; i++) {
            groupSize = 1;
            stringBuilder.append(chars[i]);
            while (i + 1 < chars.length && chars[i + 1] == chars[i]) {
                i++;
                groupSize++;
            }
            if (groupSize > 10) {
                Arrays.stream(String.valueOf(groupSize).split("")).forEach(stringBuilder::append);
            } else if (groupSize > 1) {
                stringBuilder.append(groupSize);
            }
        }
        String s = stringBuilder.toString();
        char[] newChars = s.toCharArray();
        System.arraycopy(newChars, 0, chars, 0, newChars.length);
        return newChars.length;
    }
}
