class Solution {

    public int compress(char[] chars) {
        int i = 0, index = 0;
        while (i < chars.length) {
            int count = 0;
            char currentchar = chars[i];
            while (i < chars.length && chars[i] == currentchar) {
                i++;
                count++;
            }
            chars[index++] = currentchar;
            if (count > 1) {
                for (char c : String.valueOf(count).toCharArray()) chars[index++] = c;
            }
        }
        return index;
    }
}
