class Solution {

    public int compress(char[] chars) {
        int n = chars.length;
        int index = 0;
        int i = 0;
        while (i < n) {
            char curr = chars[i];
            int count = 0;
            // Count occurrences of the current character
            while (i < n && chars[i] == curr) {
                i++;
                count++;
            }
            // Store the current character
            chars[index] = curr;
            index++;
            // If the count is greater than 1, convert it to string and add to the array
            if (count > 1) {
                String countStr = String.valueOf(count);
                for (char ch : countStr.toCharArray()) {
                    chars[index] = ch;
                    index++;
                }
            }
        }
        return index;
    }
}
