/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int kthSmallest(TreeNode root, int k) {
        int[] c = new int[1];
        int[] res = new int[1];
        inorder(root, k, c, res);
        return res[0];
    }

    static void inorder(TreeNode root, int k, int[] c, int[] res) {
        if (root == null) {
            return;
        }
        inorder(root.left, k, c, res);
        c[0]++;
        if (c[0] == k) {
            res[0] = root.val;
            return;
        }
        inorder(root.right, k, c, res);
    }
}
