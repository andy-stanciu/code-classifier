/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    private static int k, result;

    static void findKthValue(TreeNode root) {
        if (root == null)
            return;
        findKthValue(root.left);
        k--;
        if (k == 0)
            result = root.val;
        findKthValue(root.right);
    }

    public int kthSmallest(TreeNode root, int k) {
        this.k = k;
        findKthValue(root);
        return result;
    }
}
