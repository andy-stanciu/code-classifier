/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int kthSmallest(TreeNode root, int k) {
        List<Integer> list = new LinkedList<>();
        int[] counter = { 0 };
        smallest(root, k, counter, list);
        return list.get(0);
    }

    public void smallest(TreeNode node, int k, int[] counter, List<Integer> list) {
        if (node == null)
            return;
        smallest(node.left, k, counter, list);
        counter[0]++;
        if (counter[0] == k) {
            list.add(node.val);
            return;
        }
        smallest(node.right, k, counter, list);
    }
}
