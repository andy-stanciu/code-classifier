class Solution {

    public List<List<Integer>> permute(int[] nums) {
        List<List<Integer>> result = new ArrayList<>();
        permuteHelper(result, nums, new ArrayList<>());
        return result;
    }

    private void permuteHelper(List<List<Integer>> result, int[] nums, List<Integer> current) {
        if (current.size() == nums.length) {
            // Add a deep copy of current
            result.add(new ArrayList<>(current));
            return;
        }
        for (int i = 0; i < nums.length; i++) {
            // Skip if number is already used
            if (current.contains(nums[i]))
                continue;
            current.add(nums[i]);
            permuteHelper(result, nums, current);
            // Backtrack
            current.remove(current.size() - 1);
        }
    }
}
