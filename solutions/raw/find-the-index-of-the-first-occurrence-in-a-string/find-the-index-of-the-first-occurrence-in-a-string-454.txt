class Solution {

    public int strStr(String haystack, String needle) {
        /**
         *            check if word is in string
         */
        if (haystack.contains(needle)) {
            //char charToFind = needle.charAt(0);
            //for(char s : haystack.toCharArray()){
            // if(s == charToFind){
            return haystack.indexOf(needle);
            // }
            //}
        }
        return -1;
    }
}
