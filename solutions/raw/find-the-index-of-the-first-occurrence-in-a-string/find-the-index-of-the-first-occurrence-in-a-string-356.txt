class Solution {

    public int strStr(String haystack, String needle) {
        boolean found = haystack.contains(needle);
        if (found == true) {
            int charIndex = haystack.indexOf(needle);
            return charIndex;
        }
        return -1;
    }
}
