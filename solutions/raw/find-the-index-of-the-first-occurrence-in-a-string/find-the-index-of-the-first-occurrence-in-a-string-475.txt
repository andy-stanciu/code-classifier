class Solution {

    public int strStr(String haystack, String needle) {
        // to check needle is empty or not
        if (needle.length() == 0) {
            return -1;
            // to check whether both are equal or not
        } else if (haystack.equals(needle)) {
            return 0;
        }
        int len = needle.length();
        for (int i = 0, j = len; j <= haystack.length(); i++, j++) {
            // here we are taking the substring which is equal to length of needle
            String str = haystack.substring(i, j);
            if (str.equals(needle))
                return i;
        }
        return -1;
    }
}
