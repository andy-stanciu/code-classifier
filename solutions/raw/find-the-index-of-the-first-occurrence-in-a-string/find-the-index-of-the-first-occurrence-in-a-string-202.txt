class Solution {

    public int strStr(String haystack, String needle) {
        return haystack.indexOf(needle);
        // this method return a index of needle present in haystack ;
        // if not present returns -1;
    }
}
