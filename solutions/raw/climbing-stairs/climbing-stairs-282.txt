class Solution {

    public int climbStairs(int n) {
        // path can be reached
        if (n == 0)
            return 1;
        // path cannot be reached
        if (n < 0)
            return 0;
        return climbStairs(n - 1) + climbStairs(n - 2);
    }
}
