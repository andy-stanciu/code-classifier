class Solution {

    public int climbStairs(int n) {
        Vector<Integer> vt = new Vector<Integer>();
        vt.add(0, 1);
        vt.add(1, 2);
        for (int i = 2; i <= n; i++) {
            vt.add(i, (vt.get(i - 1) + vt.get(i - 2)));
        }
        return (vt.get(n - 1));
    }
}
