class Solution {

    //would do n+1 but n isnt global and didn\'t want to use helper function to pass n
    int[] memo = new int[1000];

    public int climbStairs(int n) {
        if (n < 0) {
            return 0;
        }
        if (n == 0) {
            return 1;
        }
        if (memo[n] != 0) {
            return memo[n];
        }
        memo[n] = climbStairs(n - 1) + climbStairs(n - 2);
        return memo[n];
    }
}
