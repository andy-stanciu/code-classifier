class Solution {

    public int climbStairs(int n) {
        int lastPrev = 1;
        int prev = 1;
        for (int i = 2; i <= n; i++) {
            int temp = prev;
            prev += lastPrev;
            lastPrev = temp;
        }
        return prev;
    }
}
