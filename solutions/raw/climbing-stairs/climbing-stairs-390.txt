class Solution {

    public int climbStairs(int n) {
        if (n == 0) {
            return 1;
        }
        if (n <= 0) {
            return 0;
        }
        int[] dp = new int[n + 1];
        if (dp[n] != 0) {
            return dp[n];
        }
        dp[n] = climbStairs(n - 1) + climbStairs(n - 2);
        return dp[n];
    }
}
