// Java Iterative with Memorization
class Solution {

    public int climbStairs(int n) {
        int[] cache = new int[46];
        cache[0] = 1;
        cache[1] = 1;
        for (int L = 2; L <= n; L++) cache[L] = cache[L - 1] + cache[L - 2];
        return cache[n];
    }
}
