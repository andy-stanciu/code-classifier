class Solution {

    private int[] ans = new int[46];

    public int climbStairs(int n) {
        if (n <= 1)
            return 1;
        if (ans[n] != 0)
            return ans[n];
        ans[n] = climbStairs(n - 1) + climbStairs(n - 2);
        return ans[n];
    }
}
