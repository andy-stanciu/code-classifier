class Solution {

    public void moveZeroes(int[] nums) {
        int n = nums.length;
        int k = 0;
        for (//to add all non zero element to start
        //to add all non zero element to start
        //to add all non zero element to start
        int i = 0; i < n; i++) {
            if (nums[i] != 0) {
                nums[k] = nums[i];
                k++;
            }
        }
        for (//remaining make zero.
        //remaining make zero.
        //remaining make zero.
        int i = k; i < n; i++) {
            nums[i] = 0;
        }
    }
}
