class Solution {

    public List<List<Integer>> combinationSum2(int[] candidates, int target) {
        List<List<Integer>> ls = new ArrayList<>();
        Arrays.sort(candidates);
        findCombination(ls, candidates, target, 0, new ArrayList<>());
        return ls;
    }

    public void findCombination(List<List<Integer>> ls, int[] arr, int target, int idx, ArrayList<Integer> temp) {
        if (target == 0) {
            ls.add(new ArrayList<>(temp));
            return;
        }
        for (int i = idx; i < arr.length; i++) {
            if (i > idx && arr[i] == arr[i - 1]) {
                continue;
            }
            if (arr[i] > target) {
                break;
            }
            temp.add(arr[i]);
            findCombination(ls, arr, target - arr[i], i + 1, temp);
            temp.remove(temp.size() - 1);
        }
    }
}
