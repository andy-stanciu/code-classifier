class Solution {

    public int sumOfLeftLeaves(TreeNode root) {
        if (root.left == null && root.right == null)
            return 0;
        int[] arr = new int[1];
        boolean flag = false;
        check(root, arr, flag);
        return arr[0];
    }

    public void check(TreeNode root, int[] sum, boolean flag) {
        if (root == null) {
            return;
        }
        if (root.left != null) {
            check(root.left, sum, true);
        }
        if (root.left == null && root.right == null && flag == true) {
            sum[0] += root.val;
            return;
        }
        if (root.right != null) {
            check(root.right, sum, false);
        }
    }
}
