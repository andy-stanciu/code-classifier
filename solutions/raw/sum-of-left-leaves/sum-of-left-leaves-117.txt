/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int sumOfLeftLeaves(TreeNode root) {
        int lr = 0;
        if (root.left != null) {
            lr += sumOfLeftLeaves(root.left);
            if ((root.left.left == null) && (root.left.right == null)) {
                lr += root.left.val;
            }
        }
        if (root.right != null) {
            lr += sumOfLeftLeaves(root.right);
        }
        return lr;
    }
}
