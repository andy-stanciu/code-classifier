/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int sumOfLeftLeaves(TreeNode root) {
        if (root == null) {
            return 0;
        }
        return getsum(root, null);
    }

    private int getsum(TreeNode node, TreeNode root) {
        if (node == null) {
            return 0;
        }
        ;
        if (node.left == null && node.right == null && root != null && root.left == node) {
            return node.val;
        }
        return getsum(node.left, node) + getsum(node.right, node);
    }
}
