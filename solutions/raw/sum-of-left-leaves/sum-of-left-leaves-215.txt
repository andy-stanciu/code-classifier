/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {

    public int sumOfLeftLeaves(TreeNode root) {
        return solveUsingDFS(root, false, false);
    }

    public int solveUsingDFS(TreeNode root, boolean leftNode, boolean rightNode) {
        if (root == null)
            return 0;
        if (leftNode && root.left == null && root.right == null)
            return root.val;
        return solveUsingDFS(root.left, true, false) + solveUsingDFS(root.right, false, true);
    }
}
