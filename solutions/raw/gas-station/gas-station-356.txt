class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        // int[] diff = new int[gas.length];
        int diffSum = 0;
        int minDiffSum = gas[0] - cost[0];
        int end = 0;
        for (int i = 0; i < gas.length; i++) {
            // diff[i] = gas[i] - cost[i];
            diffSum += gas[i] - cost[i];
            if (diffSum <= minDiffSum) {
                minDiffSum = diffSum;
                end = i;
            }
        }
        if (diffSum < 0)
            return -1;
        return (end + 1) % gas.length;
    }
}
