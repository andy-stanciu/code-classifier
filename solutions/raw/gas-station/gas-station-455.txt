class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int minIndex = 0;
        int min = 0;
        int sum = 0;
        for (int i = 0; i < gas.length; i++) {
            if (min > sum) {
                min = sum;
                minIndex = i;
            }
            sum = sum + gas[i] - cost[i];
        }
        return sum >= 0 ? minIndex : -1;
    }
}
