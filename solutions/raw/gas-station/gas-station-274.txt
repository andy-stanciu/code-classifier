class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int maxNegative = 0;
        int maxNegativeIndex = 0;
        int totalCost = 0;
        for (int i = 0; i < gas.length; i++) {
            totalCost = totalCost + gas[i] - cost[i];
            if (totalCost < maxNegative) {
                maxNegative = totalCost;
                maxNegativeIndex = i;
            }
        }
        if (totalCost < 0) {
            return -1;
        }
        return maxNegative < 0 ? (maxNegativeIndex + 1) % gas.length : 0;
    }
}
