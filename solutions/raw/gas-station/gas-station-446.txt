class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int n = gas.length;
        int bal = 0;
        int start = 0;
        int def = 0;
        for (int i = 0; i < n; i++) {
            bal += gas[i] - cost[i];
            if (bal < 0) {
                def += bal;
                start = i + 1;
                bal = 0;
            }
        }
        if (def + bal >= 0) {
            return start;
        } else {
            return -1;
        }
    }
}
