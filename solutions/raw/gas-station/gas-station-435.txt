class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int total = 0;
        int curSum = 0;
        int idx = 0;
        for (int i = 0; i < gas.length; i++) {
            total += gas[i] - cost[i];
            curSum += gas[i] - cost[i];
            if (curSum < 0) {
                curSum = 0;
                idx = i + 1;
            }
        }
        return total < 0 ? -1 : idx;
    }
}
