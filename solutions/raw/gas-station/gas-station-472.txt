class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int left = gas.length - 1;
        int right = 0;
        int tank = gas[left] - cost[left];
        while (right < left) {
            if (tank <= 0) {
                left--;
                tank += gas[left] - cost[left];
            } else {
                tank += gas[right] - cost[right];
                right++;
            }
        }
        if (tank < 0) {
            return -1;
        } else {
            return left;
        }
    }
}
