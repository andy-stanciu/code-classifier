class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int n = gas.length;
        int index = 0;
        int totalFuel = 0;
        int fuel = 0;
        for (int i = 0; i < n; i++) {
            fuel += gas[i] - cost[i];
            totalFuel += gas[i] - cost[i];
            if (fuel < 0) {
                index = 1 + i;
                fuel = 0;
            }
        }
        return totalFuel >= 0 ? index : -1;
    }
}
