class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int balance = 0;
        int start = 0;
        int total = 0;
        for (int i = 0; i < gas.length; i++) {
            int rem = gas[i] - cost[i];
            if (balance >= 0) {
                balance = balance + rem;
            } else {
                balance = rem;
                start = i;
            }
            total += rem;
        }
        if (total >= 0) {
            return start;
        } else {
            return -1;
        }
    }
}
