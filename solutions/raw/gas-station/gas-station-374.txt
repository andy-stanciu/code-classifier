class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int potencial = 0;
        int start = gas.length - 1;
        int bestPotencial = 0;
        for (int i = gas.length - 1; i >= 0; i--) {
            potencial += gas[i] - cost[i];
            if (bestPotencial <= potencial) {
                bestPotencial = potencial;
                start = i;
            }
        }
        return potencial >= 0 ? start : -1;
    }
}
