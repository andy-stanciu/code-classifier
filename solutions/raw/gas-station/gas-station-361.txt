class Solution {

    public int canCompleteCircuit(int[] gas, int[] cost) {
        int totalDiff = 0;
        int petrolLeft = 0;
        int index = 0;
        for (int i = 0; i < gas.length; i++) {
            if (((petrolLeft + gas[i]) - cost[i]) >= 0) {
                petrolLeft = (petrolLeft + gas[i]) - cost[i];
            } else {
                index = i + 1;
                petrolLeft = 0;
            }
            totalDiff += gas[i] - cost[i];
        }
        return totalDiff >= 0 ? index : -1;
    }
}
