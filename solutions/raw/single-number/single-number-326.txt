class Solution {

    public int singleNumber(int[] nums) {
        // first value for XOR
        int result = 0;
        for (int num : nums) {
            // Add XOR values
            result ^= num;
        }
        //return last XOR value
        return result;
    }
}
