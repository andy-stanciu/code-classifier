class Solution {

    public int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) {
            //2 same integer xor results in 0;
            result ^= num;
        }
        //single number is left
        return result;
    }
}
