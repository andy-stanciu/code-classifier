class Solution {

    public int singleNumber(int[] nums) {
        return Arrays.stream(nums).boxed().collect(Collectors.toMap(Function.identity(), v -> 1, (oldValue, newValue) -> oldValue + 1)).entrySet().stream().filter(e -> e.getValue() == 1).findFirst().get().getKey();
    }
}
