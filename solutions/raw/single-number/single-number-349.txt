class Solution {

    public int singleNumber(int[] nums) {
        int n = nums.length;
        Map<Integer, Integer> hash = new HashMap<>();
        //getting the frequency of the number
        for (int num : nums) {
            hash.put(num, hash.getOrDefault(num, 0) + 1);
        }
        for (Map.Entry<Integer, Integer> entry : hash.entrySet()) {
            //if the freq val is 1 then its the unique number in the array
            if (entry.getValue() == 1) {
                return entry.getKey();
            }
        }
        return -1;
    }
}
