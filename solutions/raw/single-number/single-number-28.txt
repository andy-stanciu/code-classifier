class Solution {

    public int singleNumber(int[] nums) {
        int uniqNum = 0;
        for (int idx : nums) {
            // Concept of XOR...
            uniqNum ^= idx;
        }
        return uniqNum;
    }
}
