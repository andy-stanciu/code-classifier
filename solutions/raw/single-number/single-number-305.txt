class Solution {

    public int singleNumber(int[] nums) {
        //Xor with 0 return bcz same value return 0
        int ans = 0;
        for (int i = 0; i < nums.length; i++) {
            ans = ans ^ nums[i];
        }
        return ans;
    }
}
