class Solution {

    public int singleNumber(int[] nums) {
        // Some operation that is its own inverse: XOR
        int mem = 0;
        for (int i = 0; i < nums.length; i++) {
            mem ^= nums[i];
        }
        return mem;
    }
}
