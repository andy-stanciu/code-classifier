class Solution {

    public int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) {
            // Bitwise XOR operation
            result ^= num;
        }
        return result;
    }
}
