class Solution {

    public int singleNumber(int[] nums) {
        int ans = 0;
        for (int val : nums) {
            // taking XOR(^)
            ans = ans ^ val;
        }
        return ans;
    }
}
