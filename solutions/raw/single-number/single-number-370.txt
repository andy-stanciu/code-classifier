class Solution {

    public int singleNumber(int[] nums) {
        HashSet<Integer> hs = new HashSet<>();
        for (int var : nums) {
            if (hs.contains(var))
                hs.remove(var);
            else
                hs.add(var);
        }
        return hs.iterator().next();
    }
}
