class Solution {

    public int singleNumber(int[] nums) {
        // Initialize
        int uniqNum = 0;
        for (int idx : nums) {
            //  XOR
            uniqNum ^= idx;
        }
        return uniqNum;
    }
}
