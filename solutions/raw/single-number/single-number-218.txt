public class Solution {

    public int singleNumber(int[] nums) {
        // Initialize result to 0
        int result = 0;
        // Iterate through the array and apply XOR
        for (int num : nums) {
            // XOR operation
            result ^= num;
        }
        // The result will be the single number
        return result;
    }
}
