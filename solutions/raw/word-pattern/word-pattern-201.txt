class Solution {

    public boolean wordPattern(String pattern, String s) {
        HashMap<Character, String> hashmap = new HashMap<Character, String>();
        String[] str = s.split(" ");
        String[] array = new String[pattern.length()];
        if (str.length != pattern.length()) {
            return false;
        }
        for (int i = 0; i < pattern.length(); i++) {
            if (!hashmap.containsKey(pattern.charAt(i)) && !hashmap.containsValue(str[i])) {
                hashmap.put(pattern.charAt(i), str[i]);
            } else {
                if (hashmap.containsKey(pattern.charAt(i))) {
                    boolean b = hashmap.get(pattern.charAt(i)).equals(str[i]);
                    if (b == false)
                        return false;
                } else {
                    if (hashmap.containsValue(str[i])) {
                        return false;
                    }
                }
            }
        }
        return true;
    }
}
