class Solution {

    public boolean wordPattern(String pattern, String s) {
        String[] newStr = s.split(" ");
        if (pattern.length() != newStr.length)
            return false;
        Map<Character, String> hashy = new HashMap<>();
        for (int i = 0; i < pattern.length(); i++) {
            if (hashy.containsKey(pattern.charAt(i))) {
                //String check= hashy.get(pattern.charAt(i));
                // System.out.println(check+" --");
                if (!hashy.get(pattern.charAt(i)).equals(newStr[i]))
                    return false;
            } else if (hashy.containsValue(newStr[i])) {
                return false;
            }
            hashy.put(pattern.charAt(i), newStr[i]);
        }
        return true;
    }
}
