class Solution {

    public boolean wordPattern(String pattern, String s) {
        HashMap<Character, String> output = new HashMap<>();
        String[] str_array = s.split(" ");
        if (pattern.length() != str_array.length) {
            return false;
        }
        for (int i = 0; i < pattern.length(); i++) {
            if (!output.containsKey(pattern.charAt(i)) && !output.containsValue(str_array[i])) {
                output.put(pattern.charAt(i), str_array[i]);
            } else {
                for (int j = 0; j <= output.size(); j++) {
                    if (output.containsKey(pattern.charAt(i))) {
                        String temp = output.get(pattern.charAt(i));
                        if (!temp.equals(str_array[i])) {
                            return false;
                        }
                    } else {
                        return false;
                    }
                }
            }
        }
        return true;
    }
}
