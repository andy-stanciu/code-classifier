class Solution {

    public boolean wordPattern(String pattern, String s) {
        String[] res = s.split(" ");
        Map<Character, String> map = new HashMap<>();
        if (pattern.length() != res.length) {
            // Different number of characters and words
            return false;
        }
        for (int i = 0; i < pattern.length(); i++) {
            if (map.containsKey(pattern.charAt(i))) {
                // Check if the existing mapping is consistent
                if (!map.get(pattern.charAt(i)).equals(res[i])) {
                    return false;
                }
            } else {
                // Check if the same word is not mapped to multiple characters
                if (map.containsValue(res[i])) {
                    return false;
                }
                map.put(pattern.charAt(i), res[i]);
            }
        }
        return true;
    }
}
