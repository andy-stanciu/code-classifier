public class Solution {

    public ListNode detectCycle(ListNode head) {
        var set = new HashSet<ListNode>();
        var curr = head;
        while (curr != null) {
            if (!set.add(curr)) {
                return curr;
            }
            curr = curr.next;
        }
        return null;
    }
}
