/**
 * Definition for singly-linked list.
 * class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) {
 *         val = x;
 *         next = null;
 *     }
 * }
 */
public class Solution {

    public ListNode detectCycle(ListNode head) {
        HashSet<ListNode> valList = new HashSet<>();
        while (head != null) {
            if (valList.contains(head)) {
                break;
            }
            valList.add(head);
            head = head.next;
        }
        return head;
    }
}
