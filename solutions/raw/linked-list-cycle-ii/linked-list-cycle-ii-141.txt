public class Solution {

    public ListNode detectCycle(ListNode head) {
        ListNode slow = head;
        ListNode fast = head;
        while (fast != null && fast.next != null) {
            slow = slow.next;
            fast = fast.next.next;
            if (slow == fast)
                // Cycle detected, find start of cycle
                return startOfCycle(head, slow);
        }
        return null;
    }

    private ListNode startOfCycle(ListNode current1, ListNode current2) {
        while (current1 != current2) {
            current1 = current1.next;
            current2 = current2.next;
        }
        return current1;
    }
}
