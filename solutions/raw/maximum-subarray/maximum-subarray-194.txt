class Solution {

    public int maxSubArray(int[] nums) {
        int maxSum = nums[0], curSum = nums[0];
        for (int i = 1; i < nums.length; i++) {
            curSum = nums[i] + Math.max(curSum, 0);
            maxSum = Math.max(maxSum, curSum);
        }
        return maxSum;
    }
}
