class Solution {

    public int maxSubArray(int[] nums) {
        int m = Integer.MIN_VALUE;
        int buffer = 0;
        for (int i : nums) {
            buffer += i;
            if (buffer > m)
                m = buffer;
            if (buffer < 0)
                buffer = 0;
        }
        return m;
    }
}
