class Solution {

    public int maxSubArray(int[] nums) {
        int sum = 0;
        int maxSum = Integer.MIN_VALUE;
        for (int x : nums) {
            sum = sum + x;
            if (sum < x)
                sum = x;
            maxSum = Math.max(maxSum, sum);
        }
        return maxSum;
    }
}
