class Solution {

    public int maxSubArray(int[] nums) {
        int sum = nums[0];
        int sum_k = nums[0];
        for (int i = 1; i < nums.length; i++) {
            sum_k = Math.max(sum_k + nums[i], nums[i]);
            sum = Math.max(sum_k, sum);
        }
        return sum;
    }
}
