class Solution {

    public int maxSubArray(int[] nums) {
        int cur = nums[0];
        int total = 0;
        for (int i : nums) {
            total += i;
            cur = Math.max(cur, total);
            if (total < 0) {
                total = 0;
            }
        }
        return cur;
    }
}
