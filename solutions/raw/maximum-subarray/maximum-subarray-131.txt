class Solution {

    public int maxSubArray(int[] nums) {
        int maxSub = nums[0];
        int cur = 0;
        for (int i : nums) {
            //negative sum dont contribute to maxSubArray
            if (cur < 0) {
                cur = 0;
            }
            cur += i;
            maxSub = Math.max(cur, maxSub);
        }
        return maxSub;
    }
}
