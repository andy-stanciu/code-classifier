class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        int nLength = ransomNote.length();
        int mLength = magazine.length();
        int[] noteFreq = new int[256];
        int[] magFreq = new int[256];
        // boundary case
        if (nLength > mLength) {
            return false;
        }
        // loop to get character & mag frequencies
        for (int i = 0; i < mLength; i++) {
            // save as positive value
            magFreq[magazine.charAt(i)]++;
        }
        for (int j = 0; j < nLength; j++) {
            // save as neg value
            noteFreq[ransomNote.charAt(j)]--;
        }
        // add ransom note to magazine, if any number < 0, fails
        for (int k = 0; k < 26; k++) {
            int answer = noteFreq[k + 97] + magFreq[k + 97];
            if (answer < 0)
                return false;
        }
        return true;
    }
}
