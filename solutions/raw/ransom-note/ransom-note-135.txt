class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        HashMap<Character, Integer> hp = new HashMap<>();
        for (int i = 0; i < magazine.length(); i++) {
            char c = magazine.charAt(i);
            if (!hp.containsKey(c)) {
                hp.put(c, 1);
            } else {
                hp.put(c, hp.get(c) + 1);
            }
        }
        for (int j = 0; j < ransomNote.length(); j++) {
            if (hp.containsKey(ransomNote.charAt(j))) {
                if (hp.get(ransomNote.charAt(j)) > 0) {
                    hp.put(ransomNote.charAt(j), hp.get(ransomNote.charAt(j)) - 1);
                } else {
                    return false;
                }
            } else {
                return false;
            }
        }
        return true;
    }
}
