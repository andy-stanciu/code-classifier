class Solution {

    public boolean canConstruct(String ransomeNote, String magzine) {
        HashMap<Character, Integer> mp = new HashMap<>();
        for (int i = 0; i < magzine.length(); i++) {
            if (mp.containsKey(magzine.charAt(i))) {
                int temp = mp.get(magzine.charAt(i));
                mp.put(magzine.charAt(i), ++temp);
            } else {
                mp.put(magzine.charAt(i), 1);
            }
        }
        for (int i = 0; i < ransomeNote.length(); i++) {
            if (mp.containsKey(ransomeNote.charAt(i))) {
                if (mp.get(ransomeNote.charAt(i)) == 1) {
                    mp.remove(ransomeNote.charAt(i));
                } else {
                    int temp = mp.get(ransomeNote.charAt(i));
                    mp.put(ransomeNote.charAt(i), --temp);
                }
            } else {
                return false;
            }
        }
        return true;
    }
}
