class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        HashMap<Character, Integer> magMap = new HashMap<Character, Integer>();
        for (int i = 0; i < magazine.length(); i++) {
            int count = magMap.getOrDefault(magazine.charAt(i), 0);
            magMap.put(magazine.charAt(i), ++count);
        }
        for (int i = 0; i < ransomNote.length(); i++) {
            if (magMap.containsKey(ransomNote.charAt(i)) && magMap.get(ransomNote.charAt(i)) != 0) {
                int count = magMap.get(ransomNote.charAt(i));
                magMap.put(ransomNote.charAt(i), --count);
            } else {
                return false;
            }
        }
        return true;
    }
}
