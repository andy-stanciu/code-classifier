class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        Map<Character, Integer> map = new HashMap<>();
        for (int x = 0; x < magazine.length(); x++) {
            map.putIfAbsent(magazine.charAt(x), 0);
            map.computeIfPresent(magazine.charAt(x), (key, value) -> {
                return value = value + 1;
            });
        }
        for (int x = 0; x < ransomNote.length(); x++) {
            if (map.get(ransomNote.charAt(x)) != null && map.get(ransomNote.charAt(x)) > 0) {
                map.computeIfPresent(ransomNote.charAt(x), (key, value) -> {
                    return value = value - 1;
                });
            } else {
                return false;
            }
        }
        return true;
    }
}
