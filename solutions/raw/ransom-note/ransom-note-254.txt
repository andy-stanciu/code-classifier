class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        Map<Character, Integer> map = new HashMap<>();
        for (int index = 0; index < magazine.length(); index++) {
            map.put(magazine.charAt(index), map.getOrDefault(magazine.charAt(index), 0) + 1);
        }
        for (int index = 0; index < ransomNote.length(); index++) {
            if (map.containsKey(ransomNote.charAt(index))) {
                if (map.get(ransomNote.charAt(index)) <= 0) {
                    return false;
                } else {
                    map.put(ransomNote.charAt(index), map.get(ransomNote.charAt(index)) - 1);
                }
            } else {
                return false;
            }
        }
        return true;
    }
}
