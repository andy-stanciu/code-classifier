class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        Hashtable<Character, Integer> hmR = new Hashtable<Character, Integer>();
        Hashtable<Character, Integer> hmM = new Hashtable<Character, Integer>();
        for (int i = 0; i < ransomNote.length(); i++) {
            if (hmR.containsKey(ransomNote.charAt(i))) {
                hmR.put(ransomNote.charAt(i), hmR.get(ransomNote.charAt(i)) + 1);
            } else {
                hmR.put(ransomNote.charAt(i), 1);
            }
        }
        for (int i = 0; i < magazine.length(); i++) {
            if (hmM.containsKey(magazine.charAt(i))) {
                hmM.put(magazine.charAt(i), hmM.get(magazine.charAt(i)) + 1);
            } else {
                hmM.put(magazine.charAt(i), 1);
            }
        }
        for (int i = 0; i < ransomNote.length(); i++) {
            if ((hmM.containsKey(ransomNote.charAt(i)))) {
                if ((hmR.get(ransomNote.charAt(i)) > hmM.get(ransomNote.charAt(i)))) {
                    return false;
                }
            } else {
                return false;
            }
        }
        return true;
    }
}
