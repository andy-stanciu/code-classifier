class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        int[] counter = new int[128];
        for (char ch : magazine.toCharArray()) ++counter[ch];
        for (char ch : ransomNote.toCharArray()) {
            if (--counter[ch] < 0)
                return false;
        }
        return true;
    }
}
