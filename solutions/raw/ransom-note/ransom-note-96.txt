class Solution {

    public static boolean canConstruct(String ransomNote, String magazine) {
        char[] ran = ransomNote.toCharArray();
        for (Character ch : ran) {
            if (!magazine.contains(ch.toString())) {
                return false;
            } else {
                magazine = magazine.replaceFirst(ch.toString(), "");
            }
        }
        return true;
    }
}
