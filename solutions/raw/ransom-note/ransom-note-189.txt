class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        if (ransomNote.length() > magazine.length())
            return false;
        StringBuilder sb = new StringBuilder(magazine);
        int index = 0;
        while (index < ransomNote.length()) {
            int lastIndex = index;
            for (int i = 0; i < sb.length(); i++) {
                if (ransomNote.charAt(index) == sb.charAt(i)) {
                    if (index == ransomNote.length() - 1)
                        return true;
                    sb.deleteCharAt(i);
                    index++;
                }
            }
            if (lastIndex == index)
                return false;
        }
        return true;
    }
}
