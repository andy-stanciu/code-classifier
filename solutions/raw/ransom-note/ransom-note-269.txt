class Solution {

    public boolean canConstruct(String ransomNote, String magazine) {
        int[] freq = new int[26];
        int l1 = magazine.length();
        int l2 = ransomNote.length();
        if (l2 > l1)
            return false;
        for (int i = 0; i < l1; i++) {
            freq[magazine.charAt(i) - 97]++;
        }
        for (int i = 0; i < l2; i++) {
            if (freq[ransomNote.charAt(i) - 97] < 1)
                return false;
            freq[ransomNote.charAt(i) - 97]--;
        }
        return true;
    }
}
