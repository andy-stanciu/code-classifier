class Solution {

    public int maxProfit(int[] prices) {
        int buy = prices[0];
        int prof = 0;
        for (int p : prices) {
            if (p < buy) {
                buy = p;
            } else {
                prof = Math.max(prof, p - buy);
            }
        }
        return prof;
    }
}
