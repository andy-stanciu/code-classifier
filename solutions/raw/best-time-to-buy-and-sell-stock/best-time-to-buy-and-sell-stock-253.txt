class Solution {

    public int maxProfit(int[] prices) {
        int maxP = 0, min = prices[0];
        for (int num : prices) {
            maxP = Math.max(num - min, maxP);
            min = Math.min(min, num);
        }
        return maxP;
    }
}
