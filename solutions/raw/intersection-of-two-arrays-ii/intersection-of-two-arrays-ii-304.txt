class Solution {

    public int[] intersect(int[] nums1, int[] nums2) {
        int intersections = 0;
        int[] newArray = new int[nums1.length];
        for (int i = 0; i < nums1.length; i++) {
            int compare1 = nums1[i];
            for (int j = 0; j < nums2.length; j++) {
                int compare2 = nums2[j];
                if (compare1 == compare2) {
                    newArray[intersections] = compare1;
                    intersections++;
                    nums2[j] = Integer.MIN_VALUE;
                    break;
                }
            }
        }
        System.out.println(intersections);
        int[] finalArray = new int[intersections];
        for (int k = 0; k < intersections; k++) {
            if (newArray[k] != 0) {
                finalArray[k] = newArray[k];
            }
        }
        return finalArray;
    }
}
