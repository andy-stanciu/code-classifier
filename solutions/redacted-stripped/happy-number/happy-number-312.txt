class Solution {

    public static int method1(int x) {
        int a = 0;
        while (x > 0) {
            a += (int) Math.pow(x % 10, 2);
            x = x / 10;
        }
        return a;
    }

    public boolean method2(int n) {
        int m = n;
        while (true) {
            m = method1(m);
            if (m == 1)
                return true;
            if (m < 1 || m == 89)
                return false;
        }
    }
}
