class Solution {

    public boolean method1(int n) {
        while (n != 1 && n != 4) {
            n = method2(n);
        }
        return n == 1;
    }

    public int method2(int n) {
        int temp = 0;
        while (n > 0) {
            temp += Math.pow(n % 10, 2);
            n /= 10;
        }
        return temp;
    }
}
