class Solution {

    public boolean method1(int n) {
        int sum = 0;
        while (n > 0) {
            int rem = n % 10;
            sum = sum + rem * rem;
            n = n / 10;
        }
        if (sum == 1 || sum == 7) {
            return true;
        } else if (sum < 10) {
            return false;
        } else {
            return method1(sum);
        }
    }
}
