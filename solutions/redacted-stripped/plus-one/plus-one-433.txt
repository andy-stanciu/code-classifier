class Solution {

    public int[] method1(int[] digits) {
        int[] ans = digits;
        for (int i = digits.length - 1; i >= 0; i--) {
            ans[i]++;
            if (ans[i] == 10) {
                ans[i] = 0;
                if (i == 0) {
                    ans = increaseArraySize(digits);
                }
            } else {
                break;
            }
        }
        return ans;
    }

    private int[] increaseArraySize(int[] digits) {
        int[] ans = new int[digits.length + 1];
        ans[0] = 1;
        for (int i = digits.length - 1; i >= 0; i--) {
            ans[i + 1] = digits[i];
        }
        return ans;
    }
}
