class Solution {

    public boolean method1(String s, String t) {
        char[] s_chars = s.toCharArray();
        char[] t_chars = t.toCharArray();
        int s_index = 0;
        for (int t_index = 0; (s_index < s.length()) && (t_index < t.length()); t_index++) {
            if (t_chars[t_index] == s_chars[s_index]) {
                s_index++;
            }
        }
        if (s_index == s.length()) {
            return true;
        } else {
            return false;
        }
    }
}
