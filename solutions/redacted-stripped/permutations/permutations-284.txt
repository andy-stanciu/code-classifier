class Solution {

    List<List<Integer>> res;

    public void swap(int[] arr, int idx1, int idx2) {
        int temp = arr[idx1];
        arr[idx1] = arr[idx2];
        arr[idx2] = temp;
    }

    public void method1(int[] arr, int idx) {
        if (idx == arr.length) {
            List<Integer> a = new ArrayList<Integer>();
            for (int i : arr) {
                a.add(i);
            }
            res.add(a);
        } else {
            for (int i = idx; i < arr.length; i++) {
                swap(arr, idx, i);
                method1(arr, idx + 1);
                swap(arr, idx, i);
            }
        }
    }

    public List<List<Integer>> permute(int[] nums) {
        res = new ArrayList<List<Integer>>();
        method1(nums, 0);
        return res;
    }
}
