public class Solution {

    public ListNode method1(ListNode head) {
        HashSet<ListNode> st = new HashSet<>();
        while (head != null) {
            if (st.contains(head)) {
                return head;
            }
            st.add(head);
            head = head.next;
        }
        return head;
    }
}
