public class Solution {

    public ListNode method1(ListNode head) {
        HashSet<ListNode> h = new HashSet<>();
        while (head != null) {
            if (!h.contains(head)) {
                h.add(head);
                head = head.next;
            } else {
                return head;
            }
        }
        return null;
    }
}
