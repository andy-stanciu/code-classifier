public class Solution {

    public ListNode method1(ListNode head) {
        Set<ListNode> s = new HashSet<>();
        while (head != null) {
            if (!s.add(head))
                return head;
            head = head.next;
        }
        return head;
    }
}
