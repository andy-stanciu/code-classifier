class Solution {

    public String convert(String s, int numRows) {
        if (numRows == 1)
            return s;
        char[] series = s.toCharArray();
        int len = s.length();
        int step = (numRows - 1) * 2;
        StringBuilder str = new StringBuilder();
        for (int i = 0; i < len; i += step) str.append(series[i]);
        int limit = numRows - 1;
        for (int row = 1; row < limit; row++) {
            int calc = 2 * (numRows - row - 1);
            for (int cycle = row; cycle < len; cycle += step) {
                str.append(series[cycle]);
                int ind = cycle + calc;
                if (ind < len)
                    str.append(series[ind]);
            }
        }
        for (int i = numRows - 1; i < len; i += step) str.append(series[i]);
        return str.toString();
    }
}
