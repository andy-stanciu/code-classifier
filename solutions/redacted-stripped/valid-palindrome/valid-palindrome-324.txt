class Solution {

    public boolean method1(String s) {
        s = s.toLowerCase();
        int j = s.length() - 1;
        int i = 0;
        return method2(s, i, j);
    }

    public boolean method2(String s, int i, int j) {
        if (i >= j) {
            return true;
        }
        if (i < j && !Character.isLetterOrDigit(s.charAt(i))) {
            return method2(s, i + 1, j);
        }
        if (i < j && !Character.isLetterOrDigit(s.charAt(j))) {
            return method2(s, i, j - 1);
        }
        if (s.charAt(i) != s.charAt(j)) {
            return false;
        } else {
            return method2(s, i + 1, j - 1);
        }
    }
}
