class Solution {

    public boolean method1(String s) {
        return method2(0, s.length() - 1, s);
    }

    public boolean method2(int startPtr, int endPtr, String s) {
        if (startPtr >= endPtr)
            return true;
        boolean a = Character.isLetterOrDigit(s.charAt(startPtr));
        boolean b = Character.isLetterOrDigit(s.charAt(endPtr));
        if (a && b) {
            char c1 = Character.toLowerCase(s.charAt(startPtr));
            char c2 = Character.toLowerCase(s.charAt(endPtr));
            if (c1 != c2)
                return false;
            startPtr++;
            endPtr--;
        }
        if (!a)
            startPtr++;
        if (!b)
            endPtr--;
        return method2(startPtr, endPtr, s);
    }
}
