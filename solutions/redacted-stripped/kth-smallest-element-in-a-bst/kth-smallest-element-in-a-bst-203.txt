class Solution {

    int small = 0;

    int count = 0;

    public int method1(TreeNode root, int val) {
        method2(root, val);
        return small;
    }

    public void method2(TreeNode root, int val) {
        if (root == null || count > val)
            return;
        method2(root.left, val);
        count++;
        if (count == val) {
            small = root.val;
        }
        method2(root.right, val);
        return;
    }
}
