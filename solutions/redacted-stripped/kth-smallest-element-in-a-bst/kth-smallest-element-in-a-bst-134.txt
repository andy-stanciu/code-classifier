class Solution {

    List<Integer> l = new ArrayList<Integer>();

    public List<Integer> small(TreeNode node, int k) {
        if (node == null) {
            return l;
        }
        small(node.left, k);
        l.add(node.val);
        small(node.right, k);
        return l;
    }

    public int method1(TreeNode root, int k) {
        List<Integer> num = small(root, k);
        return num.get(k - 1);
    }
}
