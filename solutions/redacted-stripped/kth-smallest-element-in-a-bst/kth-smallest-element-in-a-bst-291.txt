class Solution {

    private int count;

    private int result;

    public void method1(TreeNode root, int k) {
        if (root == null)
            return;
        method1(root.left, k);
        count++;
        if (count == k) {
            result = root.val;
            return;
        }
        method1(root.right, k);
    }

    public int method2(TreeNode root, int k) {
        count = 0;
        result = -1;
        method1(root, k);
        return result;
    }
}
