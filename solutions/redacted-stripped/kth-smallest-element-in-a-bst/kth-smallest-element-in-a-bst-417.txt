class Solution {

    int count = 1;

    int ans = Integer.MAX_VALUE;

    public int method1(TreeNode root, int k) {
        if (root == null)
            return 0;
        method1(root.left, k);
        if (count == k) {
            ans = root.val;
        }
        count++;
        method1(root.right, k);
        return ans;
    }
}
