class Solution {

    int cnt = 0;

    public int method1(TreeNode root, int k) {
        if (root == null) {
            return 0;
        }
        int l = method1(root.left, k);
        cnt++;
        if (cnt == k) {
            return root.val;
        }
        int r = method1(root.right, k);
        return l + r;
    }
}
