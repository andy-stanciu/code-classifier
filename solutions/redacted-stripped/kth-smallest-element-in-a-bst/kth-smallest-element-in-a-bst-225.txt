class Solution {

    public int method1(TreeNode root, int k) {
        ArrayList<Integer> list = new ArrayList<>();
        method2(root, list);
        Collections.sort(list);
        return list.get(k - 1);
    }

    public void method2(TreeNode root, List<Integer> list) {
        if (root == null) {
            return;
        }
        list.add(root.val);
        method2(root.left, list);
        method2(root.right, list);
    }
}
