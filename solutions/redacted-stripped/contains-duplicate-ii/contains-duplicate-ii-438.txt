class Solution {

    public boolean method1(int[] nums, int k) {
        Map<Integer, Integer> map = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            Integer a = map.put(nums[i], i);
            if (a != null && Math.abs(i - a) <= k) {
                return true;
            }
        }
        return false;
    }
}
