class Solution {

    public int method1(int[] nums) {
        int length = nums.length;
        if (length == 1) {
            return 0;
        }
        int totalSum = 0;
        int leftsum = 0;
        for (int num : nums) totalSum += num;
        for (int i = 0; i < length; leftsum += nums[i++]) if (leftsum * 2 == totalSum - nums[i])
            return i;
        return -1;
    }
}
