class Solution {

    public int method1(int[] nums, int k) {
        Arrays.sort(nums);
        int max = 0, n = nums.length;
        int r = n - k;
        for (int i = 0; i <= r; i++) {
            max = nums[i];
        }
        return max;
    }
}
