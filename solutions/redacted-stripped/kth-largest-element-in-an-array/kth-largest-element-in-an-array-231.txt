class Solution {

    public int method1(int[] nums, int k) {
        PriorityQueue<Integer> var1 = new PriorityQueue<>(k);
        for (int num : nums) {
            var1.offer(num);
            if (var1.size() > k) {
                var1.poll();
            }
        }
        return var1.peek();
    }
}
