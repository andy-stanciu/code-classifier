class Solution {

    public int method1(int[] arr, int k) {
        int res = Arrays.stream(arr).boxed().sorted(Comparator.reverseOrder()).skip(k - 1).findFirst().orElseThrow(() -> new IllegalAccessError(""));
        return res;
    }
}
