class Solution {

    public int method1(int[] nums, int k) {
        PrioorityQueue<Integer> pq = new PrioorityQueue<>();
        for (int el : nums) {
            pq.add(el);
            while (pq.size() > k) {
                pq.poll();
            }
        }
        return pq.poll();
    }
}
