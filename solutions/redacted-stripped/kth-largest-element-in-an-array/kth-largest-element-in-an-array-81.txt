class Solution {

    public int method1(int[] nums, int k) {
        PriorityQueue<Integer> heap = new PriorityQueue<Integer>();
        for (int i : nums) {
            heap.add(i);
            if (heap.size() > k)
                heap.poll();
        }
        return heap.peek();
    }
}
