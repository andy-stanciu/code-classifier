class Solution {

    public int method1(int[] nums, int k) {
        int count = 0;
        Arrays.sort(nums);
        for (int i = nums.length; i > 0; i--) {
            count++;
            if (count == k) {
                break;
            }
        }
        return nums[nums.length - count];
    }
}
