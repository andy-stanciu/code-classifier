class Solution {

    public int method1(int[] nums, int k) {
        Integer[] var1 = Arrays.stream(nums).boxed().toArray(Integer[]::new);
        Arrays.sort(var1, Collections.reverseOrder());
        return var1[k - 1];
    }
}
