class Solution {

    List<Integer> ans = new ArrayList<Integer>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return ans;
        ans.add(root.val);
        method1(root.left);
        method1(root.right);
        return ans;
    }
}
