class Solution {

    public List<Integer> method1(TreeNode root) {
        ArrayList<Integer> al = new ArrayList<>();
        if (root == null) {
            return al;
        }
        al.add(root.val);
        al.addAll(method1(root.left));
        al.addAll(method1(root.right));
        return al;
    }
}
