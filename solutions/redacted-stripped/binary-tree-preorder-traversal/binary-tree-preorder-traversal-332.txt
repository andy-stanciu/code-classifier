class Solution {

    List<Integer> Lis = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return Lis;
        }
        Lis.add(root.val);
        method1(root.left);
        method1(root.right);
        return Lis;
    }
}
