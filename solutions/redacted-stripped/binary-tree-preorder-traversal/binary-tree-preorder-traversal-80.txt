class Solution {

    List<Integer> list = new ArrayList<Integer>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return list;
        }
        list.add(root.val);
        method1(root.left);
        method1(root.right);
        return list;
    }
}
