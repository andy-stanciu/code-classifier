class Solution {

    List<Integer> result = new ArrayList<Integer>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return result;
        else {
            result.add(root.val);
            method1(root.left);
            method1(root.right);
            return result;
        }
    }
}
