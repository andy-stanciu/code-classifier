class Solution {

    public List<Integer> method1(TreeNode root) {
        TreeNode cur = root;
        List<Integer> list = new ArrayList<>();
        if (cur == null) {
            return list;
        }
        list.add(cur.val);
        list.addAll(method1(cur.left));
        list.addAll(method1(cur.right));
        return list;
    }
}
