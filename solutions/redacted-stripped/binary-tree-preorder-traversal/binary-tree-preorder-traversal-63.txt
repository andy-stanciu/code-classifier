class Solution {

    private List<Integer> ans = new ArrayList<Integer>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return ans;
        ans.add(root.val);
        ans = method1(root.left);
        ans = method1(root.right);
        return ans;
    }
}
