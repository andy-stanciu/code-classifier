class Solution {

    public List<Integer> method1(TreeNode root, List<Integer> ans) {
        if (root == null) {
            return ans;
        }
        ans.add(root.val);
        method1(root.left, ans);
        method1(root.right, ans);
        return ans;
    }

    public List<Integer> method2(TreeNode root) {
        List<Integer> ans = new ArrayList<>();
        return method1(root, ans);
    }
}
