class Solution {

    List<Integer> alt = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return alt;
        }
        alt.add(root.val);
        method1(root.left);
        method1(root.right);
        return alt;
    }
}
