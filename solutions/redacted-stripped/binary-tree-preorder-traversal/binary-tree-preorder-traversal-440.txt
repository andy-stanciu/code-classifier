class Solution {

    private List<Integer> ans = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root != null) {
            ans.add(root.val);
            method1(root.left);
            method1(root.right);
        }
        return ans;
    }
}
