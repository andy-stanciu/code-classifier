class Solution {

    public List<Integer> HelperPre(TreeNode root, List<Integer> ls) {
        if (root == null)
            return ls;
        ls.add(root.val);
        HelperPre(root.left, ls);
        HelperPre(root.right, ls);
        return ls;
    }

    public List<Integer> method1(TreeNode root) {
        List<Integer> ls = new ArrayList();
        return HelperPre(root, ls);
    }
}
