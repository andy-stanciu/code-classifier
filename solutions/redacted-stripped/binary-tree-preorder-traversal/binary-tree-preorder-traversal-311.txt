class Solution {

    public void method1(TreeNode root, List<Integer> ll) {
        if (root == null) {
            return;
        }
        ll.add(root.val);
        method1(root.left, ll);
        method1(root.right, ll);
    }

    public List<Integer> method2(TreeNode root) {
        List<Integer> ll = new ArrayList<>();
        method1(root, ll);
        return ll;
    }
}
