class Solution {

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return preOder;
        preOder.add(root.val);
        method1(root.left);
        method1(root.right);
        return preOder;
    }
}
