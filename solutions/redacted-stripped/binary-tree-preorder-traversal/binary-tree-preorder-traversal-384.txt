class Solution {

    public List<Integer> method1(TreeNode root) {
        List<Integer> l = new ArrayList<>();
        if (root == null) {
            return new ArrayList<>();
        }
        l.add(root.val);
        l.addAll(method1(root.left));
        l.addAll(method1(root.right));
        return l;
    }
}
