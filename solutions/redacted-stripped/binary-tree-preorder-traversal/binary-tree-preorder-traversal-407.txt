class Solution {

    ArrayList<Integer> al = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return al;
        }
        al.add(root.val);
        method1(root.left);
        method1(root.right);
        return al;
    }
}
