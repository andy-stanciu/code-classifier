class Solution {

    List<Integer> x = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return x;
        x.add(root.val);
        method1(root.left);
        method1(root.right);
        return x;
    }
}
