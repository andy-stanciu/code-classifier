class Solution {

    List<Integer> pre = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return pre;
        }
        pre.add(root.val);
        method1(root.left);
        method1(root.right);
        return pre;
    }
}
