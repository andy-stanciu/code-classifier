class Solution {

    List a = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return a;
        } else {
            a.add(root.val);
            method1(root.left);
            method1(root.right);
        }
        return a;
    }
}
