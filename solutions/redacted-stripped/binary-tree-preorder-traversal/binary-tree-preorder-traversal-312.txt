class Solution {

    List<Integer> ans = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        method2(root);
        return ans;
    }

    public void method2(TreeNode root1) {
        if (root1 == null) {
            return;
        }
        ans.add(root1.val);
        method2(root1.left);
        method2(root1.right);
    }
}
