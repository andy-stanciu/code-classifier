class Solution {

    private boolean var1;

    public boolean var1(TreeNode root, int param1) {
        var1 = false;
        if (root != null) {
            traverse(root, param1, 0);
        }
        return var1;
    }

    private void traverse(TreeNode node, int param1, int param2) {
        if (node.left == null && node.right == null) {
            if (((param2 + node.val) == param1)) {
                var1 = true;
            }
        } else {
            if (node.left != null) {
                traverse(node.left, param1, param2 + node.val);
            }
            if (node.right != null) {
                traverse(node.right, param1, param2 + node.val);
            }
        }
    }
}
