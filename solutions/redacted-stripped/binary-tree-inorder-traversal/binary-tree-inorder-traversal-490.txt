class Solution {

    List<Integer> a = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root != null) {
            method1(root.left);
            a.add(root.val);
            method1(root.right);
        }
        return a;
    }
}
