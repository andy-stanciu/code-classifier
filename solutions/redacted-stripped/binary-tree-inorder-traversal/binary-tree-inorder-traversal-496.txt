class Solution {

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return new ArrayList<Integer>();
        List<Integer> output = method1(root.left);
        output.add(root.val);
        output.addAll(method1(root.right));
        return output;
    }
}
