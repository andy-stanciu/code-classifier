class Solution {

    List<Integer> arr = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return arr;
        }
        method1(root.left);
        arr.add(root.val);
        method1(root.right);
        return arr;
    }
}
