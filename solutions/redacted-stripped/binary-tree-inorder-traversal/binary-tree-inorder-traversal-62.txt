class Solution {

    List<Integer> ls = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return new ArrayList<>();
        method1(root.left);
        ls.add(root.val);
        method1(root.right);
        return ls;
    }
}
