class Solution {

    public List<Integer> method1(TreeNode root) {
        List<Integer> sln = new ArrayList<>();
        if (root == null)
            return sln;
        sln.addAll(method1(root.left));
        sln.add(root.val);
        sln.addAll(method1(root.right));
        return sln;
    }
}
