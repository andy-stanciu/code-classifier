class Solution {

    List<Integer> iL = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return iL;
        }
        method1(root.left);
        iL.add(root.val);
        method1(root.right);
        return iL;
    }
}
