class Solution {

    List<Integer> res = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return res;
        }
        res = method1(root.left);
        res.add(root.val);
        res = method1(root.right);
        return res;
    }
}
