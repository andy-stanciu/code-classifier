class Solution {

    List<Integer> al = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return al;
        }
        method1(root.left);
        al.add(root.val);
        method1(root.right);
        return al;
    }
}
