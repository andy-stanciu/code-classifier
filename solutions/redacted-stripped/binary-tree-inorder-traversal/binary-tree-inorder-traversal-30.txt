class Solution {

    List<Integer> l = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root != null) {
            method1(root.left);
            l.add(root.val);
            method1(root.right);
        } else {
            return l;
        }
        return l;
    }
}
