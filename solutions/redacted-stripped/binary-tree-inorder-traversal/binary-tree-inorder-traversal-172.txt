class Solution {

    List<Integer> n = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root != null) {
            method1(root.left);
            n.add(root.val);
            method1(root.right);
        }
        return n;
    }
}
