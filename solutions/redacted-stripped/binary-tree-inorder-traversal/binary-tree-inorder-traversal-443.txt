class Solution {

    List<Integer> li = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        traverse(root);
        return li;
    }

    void traverse(TreeNode root) {
        if (root == null)
            return;
        traverse(root.left);
        li.add(root.val);
        traverse(root.right);
    }
}
