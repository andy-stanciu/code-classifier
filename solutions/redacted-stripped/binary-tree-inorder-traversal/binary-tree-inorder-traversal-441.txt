class Solution {

    public List<Integer> method1(TreeNode root) {
        List<Integer> arr = new ArrayList<>();
        method2(root, arr);
        return arr;
    }

    public void method2(TreeNode root, List<Integer> arr) {
        if (root == null) {
            return;
        }
        method2(root.left, arr);
        arr.add(root.val);
        method2(root.right, arr);
    }
}
