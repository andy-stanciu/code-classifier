class Solution {

    List<Integer> ans = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return Collections.emptyList();
        method1(root.left);
        ans.add(root.val);
        method1(root.right);
        return ans;
    }
}
