class Solution {

    List<Integer> result = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return result;
        }
        List<Integer> arr = method1(root.left);
        result.add(root.val);
        arr = method1(root.right);
        return result;
    }
}
