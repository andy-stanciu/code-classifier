class Solution {

    List<Integer> res = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root != null) {
            method1(root.left);
            res.add(root.val);
            method1(root.right);
        }
        return res;
    }
}
