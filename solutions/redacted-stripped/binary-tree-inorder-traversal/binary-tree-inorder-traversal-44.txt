class Solution {

    List<Integer> list = new LinkedList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return new LinkedList<>();
        }
        method1(root.left);
        list.add(root.val);
        method1(root.right);
        return list;
    }
}
