class Solution {

    List<Integer> in = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return in;
        }
        method1(root.left);
        in.add(root.val);
        method1(root.right);
        return in;
    }
}
