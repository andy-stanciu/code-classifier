class Solution {

    public ListNode method1(ListNode head) {
        ListNode prvs = null;
        ListNode current = head;
        while (current != null) {
            ListNode next = current.next;
            current.next = prvs;
            prvs = current;
            current = next;
        }
        return prvs;
    }
}
