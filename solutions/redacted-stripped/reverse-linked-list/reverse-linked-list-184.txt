class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode nextNode = head.next;
        ListNode newHead = method1(nextNode);
        nextNode.next = head;
        head.next = null;
        return newHead;
    }
}
