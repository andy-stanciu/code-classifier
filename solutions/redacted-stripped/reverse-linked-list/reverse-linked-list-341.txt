class Solution {

    public ListNode method1(ListNode head) {
        ListNode curr = head;
        ListNode prev = null;
        if (curr == null)
            return head;
        while (curr != null) {
            ListNode newer = curr.next;
            curr.next = prev;
            prev = curr;
            curr = newer;
        }
        return prev;
    }
}
