class Solution {

    public ListNode method1(ListNode node) {
        if (node == null || node.next == null) {
            return node;
        }
        ListNode head = method1(node.next);
        ListNode p = head;
        while (p.next != null) p = p.next;
        p.next = node;
        node.next = null;
        return head;
    }
}
