class Solution {

    public ListNode method1(ListNode head) {
        return method2(head, null);
    }

    ListNode method2(ListNode current, ListNode prev) {
        if (current == null) {
            return prev;
        }
        ListNode next = current.next;
        current.next = prev;
        return method2(next, current);
    }
}
