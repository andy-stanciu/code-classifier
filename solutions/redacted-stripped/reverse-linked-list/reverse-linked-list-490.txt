class Solution {

    public ListNode method1(ListNode head) {
        ListNode curr = head;
        ListNode pri = null;
        ListNode Next = null;
        while (curr != null) {
            Next = curr.next;
            curr.next = pri;
            pri = curr;
            curr = Next;
        }
        return pri;
    }
}
