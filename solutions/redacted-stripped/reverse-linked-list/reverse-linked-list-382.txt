class Solution {

    public ListNode method1(ListNode head) {
        ListNode current = head;
        ListNode before = null;
        ListNode after = null;
        while (current != null) {
            after = current.next;
            current.next = before;
            before = current;
            current = after;
        }
        head = before;
        return head;
    }
}
