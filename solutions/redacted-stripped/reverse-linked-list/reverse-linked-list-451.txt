class Solution {

    public ListNode method1(ListNode head) {
        ListNode prev = null;
        ListNode curr = head;
        while (curr != null) {
            ListNode aageka = curr.next;
            curr.next = prev;
            prev = curr;
            curr = aageka;
        }
        return prev;
    }
}
