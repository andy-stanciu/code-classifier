class Solution {

    public ListNode method1(ListNode head) {
        ListNode ans = null;
        ListNode curr = head;
        while (curr != null) {
            ListNode temp = curr.next;
            curr.next = ans;
            ans = curr;
            curr = temp;
        }
        return ans;
    }
}
