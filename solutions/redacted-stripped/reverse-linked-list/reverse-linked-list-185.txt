class Solution {

    ListNode newHead;

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        newHead = method1(head.next);
        head.next.next = head;
        head.next = null;
        return newHead;
    }
}
