class Solution {

    public ListNode method1(ListNode head) {
        ListNode before = null;
        ListNode curr = head;
        ListNode after = null;
        while (curr != null) {
            after = curr.next;
            curr.next = before;
            before = curr;
            curr = after;
        }
        return before;
    }
}
