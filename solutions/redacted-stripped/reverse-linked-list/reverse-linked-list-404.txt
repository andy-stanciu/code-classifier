class Solution {

    public ListNode method1(ListNode head) {
        ListNode result = null;
        while (head != null) {
            ListNode temp = head;
            head = temp.next;
            temp.next = result;
            result = temp;
        }
        return result;
    }
}
