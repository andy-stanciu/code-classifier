class Solution {

    public ListNode method1(ListNode head) {
        ListNode prev = null;
        ListNode nextptr = null;
        ListNode curr = head;
        while (curr != null) {
            nextptr = curr.next;
            curr.next = prev;
            prev = curr;
            curr = nextptr;
        }
        return prev;
    }
}
