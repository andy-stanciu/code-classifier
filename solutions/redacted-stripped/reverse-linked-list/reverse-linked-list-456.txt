class Solution {

    public ListNode method1(ListNode head) {
        ListNode preNode = null;
        while (head != null) {
            ListNode temp = head;
            head = head.next;
            temp.next = preNode;
            preNode = temp;
        }
        return preNode;
    }
}
