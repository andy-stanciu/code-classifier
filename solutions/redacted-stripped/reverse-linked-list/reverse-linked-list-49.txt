class Solution {

    public ListNode method1(ListNode head) {
        return method2(null, head);
    }

    public ListNode method2(ListNode pre, ListNode cur) {
        if (cur == null)
            return pre;
        ListNode temp = cur.next;
        cur.next = pre;
        return method2(cur, temp);
    }
}
