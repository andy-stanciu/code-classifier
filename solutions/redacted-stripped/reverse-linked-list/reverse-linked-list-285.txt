class Solution {

    public ListNode method1(ListNode head) {
        ListNode prev = new ListNode();
        ListNode curr = head;
        while (curr != null) {
            ListNode next = curr.next;
            curr.next = prev.next;
            prev.next = curr;
            curr = next;
        }
        return prev.next;
    }
}
