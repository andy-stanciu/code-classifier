class Solution {

    public ListNode method1(ListNode head) {
        ListNode prev = null, curr = head, right;
        while (curr != null) {
            right = curr.next;
            curr.next = prev;
            prev = curr;
            curr = right;
        }
        return prev;
    }
}
