class Solution {

    public ListNode method1(ListNode head) {
        ListNode temp = head;
        ListNode pre = null;
        ListNode a;
        while (temp != null) {
            a = temp.next;
            temp.next = pre;
            pre = temp;
            temp = a;
        }
        return pre;
    }
}
