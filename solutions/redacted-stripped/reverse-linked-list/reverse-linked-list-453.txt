class Solution {

    public ListNode method1(ListNode head) {
        ListNode current = head;
        ListNode nextt = head;
        ListNode prev = null;
        while (current != null) {
            nextt = current.next;
            current.next = prev;
            prev = current;
            current = nextt;
        }
        return prev;
    }
}
