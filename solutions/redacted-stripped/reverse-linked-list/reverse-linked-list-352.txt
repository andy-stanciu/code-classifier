class Solution {

    public ListNode method1(ListNode head) {
        ListNode prev = null;
        ListNode now = head;
        ListNode next = null;
        while (now != null) {
            next = now.next;
            now.next = prev;
            prev = now;
            now = next;
        }
        return prev;
    }
}
