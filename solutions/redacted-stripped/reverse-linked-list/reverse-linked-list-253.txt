class Solution {

    public ListNode method1(ListNode head) {
        return method2(head, null);
    }

    private ListNode method2(ListNode cur, ListNode prev) {
        if (cur == null)
            return prev;
        ListNode post = cur.next;
        cur.next = prev;
        return method2(post, cur);
    }
}
