class Solution {

    public ListNode method1(ListNode head) {
        ListNode prv = null;
        while (head != null) {
            ListNode temp = head.next;
            head.next = prv;
            prv = head;
            head = temp;
        }
        return prv;
    }
}
