class Solution {

    public ListNode method1(ListNode current) {
        if (current == null)
            return null;
        ListNode prev = null;
        ListNode next = current.next;
        while (current != null) {
            next = current.next;
            current.next = prev;
            prev = current;
            current = next;
        }
        return prev;
    }
}
