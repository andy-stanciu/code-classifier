class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode curr = method1(head.next);
        ListNode rev = null;
        head.next.next = head;
        head.next = rev;
        return curr;
    }
}
