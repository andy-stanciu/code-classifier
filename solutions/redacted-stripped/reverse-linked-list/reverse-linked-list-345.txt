class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode ans = method1(head.next);
        head.next.next = head;
        head.next = null;
        return ans;
    }
}
