class Solution {

    public ListNode method1(ListNode head) {
        if (head == null) {
            return head;
        } else if (head.next == null) {
            return head;
        }
        ListNode newHead = method1(head.next);
        head.next.next = head;
        head.next = null;
        return newHead;
    }
}
