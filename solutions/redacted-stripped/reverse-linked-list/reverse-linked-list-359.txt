class Solution {

    public ListNode method1(ListNode head) {
        ListNode current = head;
        ListNode prev = null;
        while (current != null) {
            ListNode fast = current.next;
            current.next = prev;
            prev = current;
            current = fast;
        }
        return prev;
    }
}
