class Solution {

    public ListNode method1(ListNode head) {
        ListNode left = null;
        ListNode curr = head;
        while (curr != null) {
            ListNode right = curr.next;
            curr.next = left;
            left = curr;
            curr = right;
        }
        return left;
    }
}
