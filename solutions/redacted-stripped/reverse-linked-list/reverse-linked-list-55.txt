class Solution {

    public ListNode method1(ListNode head) {
        ListNode data = head;
        ListNode curr = null;
        ListNode prev = null;
        while (data != null) {
            curr = data;
            data = data.next;
            curr.next = prev;
            prev = curr;
        }
        head = curr;
        return head;
    }
}
