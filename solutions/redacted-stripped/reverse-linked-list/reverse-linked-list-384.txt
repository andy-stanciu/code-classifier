class Solution {

    public ListNode method1(ListNode head) {
        ListNode pos = head;
        while (pos != null && pos.next != null) {
            ListNode save = pos.next;
            pos.next = save.next;
            save.next = head;
            head = save;
        }
        return head;
    }
}
