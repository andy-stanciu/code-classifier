class Solution {

    public ListNode method1(ListNode head) {
        ListNode first;
        if (head == null || head.next == null)
            return head;
        first = method1(head.next);
        head.next.next = head;
        head.next = null;
        return first;
    }
}
