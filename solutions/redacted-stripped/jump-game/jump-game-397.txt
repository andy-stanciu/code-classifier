class Solution {

    public boolean method1(int[] nums) {
        int curr = nums[0];
        for (int i = 1; i < nums.length; i++) {
            if (curr-- == 0)
                return false;
            curr = Math.max(curr, nums[i]);
        }
        return true;
    }
}
