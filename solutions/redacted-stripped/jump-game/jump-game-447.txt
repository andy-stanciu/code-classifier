class Solution {

    public boolean method1(int[] nums) {
        int var1 = 0;
        for (int i = 0; i < nums.length; i++) {
            if (var1 < i)
                return false;
            var1 = Math.max(var1, i + nums[i]);
        }
        return true;
    }
}
