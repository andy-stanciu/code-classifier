class Solution {

    public boolean method1(int[] nums) {
        int res = 0;
        for (int i = 0; i <= res; i++) {
            res = Math.max(res, i + nums[i]);
            if (res >= nums.length - 1) {
                return true;
            }
        }
        return false;
    }
}
