class Solution {

    public boolean method1(int[] nums) {
        return method2(nums, 0);
    }

    public boolean method2(int[] num, int ind) {
        if (ind > num.length)
            return false;
        if (ind == num.length - 1)
            return true;
        for (int i = 1; i <= num[ind]; i++) {
            if (method2(num, ind + i)) {
                return true;
            }
        }
        return false;
    }
}
