class Solution {

    public boolean method1(int[] nums) {
        int temp = 0;
        for (int i = 0; i <= temp; i++) {
            temp = Math.max(temp, nums[i] + i);
            if (temp >= nums.length - 1)
                return true;
        }
        return false;
    }
}
