class Solution {

    public boolean method1(int[] nums) {
        int n = nums.length;
        boolean[] dp = new boolean[n];
        dp[0] = true;
        for (int i = 0; i < n; i++) {
            if (!dp[i]) {
                continue;
            }
            int var1 = nums[i];
            for (int j = i + 1; j <= i + var1 && j < n; j++) {
                dp[j] = true;
            }
        }
        return dp[n - 1];
    }
}
