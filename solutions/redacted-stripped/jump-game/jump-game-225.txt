class Solution {

    public boolean method1(int[] nums) {
        if (nums.length == 1) {
            return true;
        }
        int[] memo = new int[nums.length];
        return method2(nums, 0, memo);
    }

    private boolean method2(int[] nums, int index, int[] memo) {
        if (index >= nums.length) {
            return false;
        }
        if (memo[index] == 1) {
            return false;
        }
        memo[index] = 1;
        if (nums.length == index + 1) {
            return true;
        }
        int root = nums[index];
        while (root > 0) {
            if (method2(nums, index + root, memo))
                return true;
            root--;
        }
        return false;
    }
}
