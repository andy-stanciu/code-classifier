class Solution {

    public boolean method1(int[] nums) {
        int goal = nums.length - 1;
        for (int i = goal - 1; i >= 0; i--) {
            if (i + nums[i] >= goal) {
                goal = i;
            }
        }
        return goal == 0;
    }
}
