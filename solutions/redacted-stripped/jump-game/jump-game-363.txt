class Solution {

    public boolean method1(int[] nums) {
        int maxIndex = 0;
        for (int i = 0; i < nums.length; i++) {
            if (maxIndex < i) {
                return false;
            } else {
                maxIndex = Math.max(maxIndex, i + nums[i]);
            }
        }
        return true;
    }
}
