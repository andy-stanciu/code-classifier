class Solution {

    public boolean method1(int[] nums) {
        int flag = 0;
        for (int i = 0; i < nums.length && i <= flag; i++) {
            flag = Math.max(flag, i + nums[i]);
            if (flag >= nums.length - 1) {
                return true;
            }
        }
        return false;
    }
}
