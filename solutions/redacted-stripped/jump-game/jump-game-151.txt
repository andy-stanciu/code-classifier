class Solution {

    public boolean method1(int[] nums) {
        int curr = 0;
        for (int i = 0; i < nums.length; i++) {
            if (curr < i) {
                return false;
            }
            curr = Math.max(curr, (i + nums[i]));
        }
        return true;
    }
}
