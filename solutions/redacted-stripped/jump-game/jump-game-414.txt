class Solution {

    public boolean method1(int[] nums) {
        int target = nums.length - 1;
        for (int i = nums.length - 2; i >= 0; i--) {
            if (target <= i + nums[i])
                target = i;
        }
        if (target == 0)
            return true;
        return false;
    }
}
