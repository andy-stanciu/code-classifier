class Solution {

    public boolean method1(int[] nums) {
        int curr = 0;
        int n = nums.length;
        for (int i = 0; i < n; i++) {
            if (i > curr) {
                return false;
            }
            curr = Math.max(curr, i + nums[i]);
        }
        return true;
    }
}
