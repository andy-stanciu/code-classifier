class Solution {

    public void method1(int[] arr) {
        int n = arr.length;
        int index = 0;
        for (int ele : arr) {
            if (ele != 0)
                arr[index++] = ele;
        }
        for (int i = index; i < n; i++) {
            arr[i] = 0;
        }
    }
}
