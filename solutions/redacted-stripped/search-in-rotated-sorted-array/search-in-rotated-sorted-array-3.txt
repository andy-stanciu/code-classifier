class Solution {

    public int method1(int[] nums, int target) {
        int idx = 0;
        for (int num : nums) {
            if (num == target)
                return idx;
            idx++;
        }
        return -1;
    }
}
