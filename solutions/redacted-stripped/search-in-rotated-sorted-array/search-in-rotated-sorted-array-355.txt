class Solution {

    public int method1(int[] nums, int target) {
        return method2(nums, target, 0, nums.length - 1);
    }

    public static int method2(int[] arr, int target, int low, int high) {
        if (low > high)
            return -1;
        int mid = (low + high) / 2;
        if (arr[mid] == target)
            return mid;
        int leftHalfResult = method2(arr, target, low, mid - 1);
        int rightHalfResult = method2(arr, target, mid + 1, high);
        return leftHalfResult + rightHalfResult + 1;
    }
}
