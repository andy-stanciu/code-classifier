class Solution {

    public boolean method1(String s, List<String> param1) {
        int n = s.length();
        boolean[] dp = new boolean[n + 1];
        int maxlen = 0;
        for (String var1 : param1) {
            maxlen = Math.max(maxlen, var1.length());
        }
        dp[0] = true;
        for (int i = 0; i <= n; i++) {
            for (int j = i - 1; j >= 0; j--) {
                if (i - j > maxlen) {
                    continue;
                }
                if (dp[j] && param1.contains(s.substring(j, i))) {
                    dp[i] = true;
                    break;
                }
            }
        }
        return dp[n];
    }
}
