class Solution {

    public boolean method1(String s, List<String> param1) {
        HashSet<String> set = new HashSet<>(param1);
        return method2(s, set, 0, new Boolean[s.length()]);
    }

    public boolean method2(String s, HashSet<String> set, int start, Boolean[] memo) {
        if (start == s.length()) {
            return true;
        }
        if (memo[start] != null) {
            return memo[start];
        }
        for (int end = start; end < s.length(); end++) {
            if (set.contains(s.substring(start, end + 1)) && method2(s, set, end + 1, memo)) {
                memo[start] = true;
                return true;
            }
        }
        memo[start] = false;
        return false;
    }
}
