class Solution {

    public boolean method1(String s, List<String> param1) {
        int n = s.length();
        int var1 = 0;
        boolean[] dp = new boolean[n + 1];
        dp[0] = true;
        for (String str : param1) {
            var1 = Math.max(var1, str.length());
        }
        for (int i = 1; i <= n; i++) {
            for (int j = i - 1; j >= Math.max(i - var1 - 1, 0); j--) {
                if (dp[j]) {
                    if (param1.contains(s.substring(j, i))) {
                        dp[i] = true;
                        break;
                    }
                }
            }
        }
        return dp[n];
    }
}
