class Solution {

    public boolean method1(String s, List<String> param1) {
        String regexPattern = "^(" + String.join("|", param1) + ")*$";
        Pattern pattern = Pattern.compile(regexPattern, Pattern.CASE_INSENSITIVE);
        return pattern.matcher(s).find();
    }
}
