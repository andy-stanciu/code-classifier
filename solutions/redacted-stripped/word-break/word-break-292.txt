class Solution {

    public boolean method1(String s, List<String> param1) {
        boolean[] wm = new boolean[s.length() + 1];
        wm[0] = true;
        for (int i = 0; i <= s.length(); i++) {
            for (int j = 0; j < i; j++) {
                if (wm[j] == true && param1.contains(s.substring(j, i))) {
                    wm[i] = true;
                    break;
                }
            }
        }
        return wm[s.length()];
    }
}
