class Solution {

    public boolean method1(String s, List<String> param1) {
        int n = s.length();
        List<String> dp = new ArrayList<>();
        Set<String> var1 = new HashSet<>(param1);
        for (int i = 0; i <= n; i++) {
            dp.add("");
        }
        for (int i = n - 1; i >= 0; i--) {
            for (int j = i + 1; j <= n; j++) {
                String x = dp.get(j);
                if (var1.contains(s.substring(i, n - x.length()))) {
                    dp.set(i, s.substring(i, n));
                    break;
                }
            }
        }
        return dp.get(0).equals(s);
    }
}
