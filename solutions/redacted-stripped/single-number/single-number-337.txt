class Solution {

    public int method1(int[] nums) {
        Arrays.sort(nums);
        int ans = 0;
        if (nums.length == 1 || nums[0] != nums[0 + 1]) {
            ans = nums[0];
        }
        if (nums.length > 1 && nums[nums.length - 1] != nums[nums.length - 2]) {
            ans = nums[nums.length - 1];
        }
        for (int i = 1; i < nums.length - 1; i++) {
            if (nums[i] != nums[i + 1] && nums[i] != nums[i - 1]) {
                ans = nums[i];
            }
        }
        return ans;
    }
}
