class Solution {

    public int method1(int[] nums) {
        if (nums == null || nums.length == 0)
            return 0;
        int xor = 0;
        for (int i : nums) {
            xor ^= i;
        }
        return xor;
    }
}
