class Solution {

    public int method1(int[] nums) {
        return method2(nums, 0, new ArrayList<>(1));
    }

    private static int method2(int[] nums, int index, List<Integer> res) {
        int a = nums[index];
        if (res.contains(a)) {
            res.remove((Integer) a);
        } else {
            res.add(a);
        }
        return index == nums.length - 1 ? res.get(0) : method2(nums, index + 1, res);
    }
}
