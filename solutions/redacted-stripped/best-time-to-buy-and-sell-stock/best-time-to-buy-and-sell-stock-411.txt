class Solution {

    public int maxProfit(int[] prices) {
        int var1 = prices[0];
        int maxprofit = 0;
        for (int i = 1; i < prices.length; i++) {
            if (prices[i] > var1) {
                maxprofit = Math.max(maxprofit, prices[i] - var1);
            }
            var1 = Math.min(var1, prices[i]);
        }
        return maxprofit;
    }
}
