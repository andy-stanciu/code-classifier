class Solution {

    public int maxProfit(int[] prices) {
        int min = Integer.MAX_VALUE;
        int sumProfit = 0;
        for (int p : prices) {
            min = Math.min(min, p);
            sumProfit = Math.max(sumProfit, p - min);
        }
        return sumProfit;
    }
}
