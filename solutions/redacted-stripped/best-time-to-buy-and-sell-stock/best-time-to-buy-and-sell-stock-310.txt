class Solution {

    public int maxProfit(int[] prices) {
        if (prices.length == 1)
            return 0;
        int var1 = prices[0];
        int maxProfit = 0;
        for (int i = 0; i < prices.length; i++) {
            int currProfit = prices[i] - var1;
            if (currProfit > maxProfit) {
                maxProfit = currProfit;
            }
            var1 = Math.min(var1, prices[i]);
        }
        return maxProfit;
    }
}
