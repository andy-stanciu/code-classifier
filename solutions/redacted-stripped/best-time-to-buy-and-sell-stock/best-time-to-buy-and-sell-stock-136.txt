class Solution {

    public int maxProfit(int[] prices) {
        int var1 = prices[0], maxProfit = 0;
        for (int current : prices) {
            var1 = Math.min(var1, current);
            maxProfit = Math.max(maxProfit, current - var1);
        }
        return maxProfit;
    }
}
