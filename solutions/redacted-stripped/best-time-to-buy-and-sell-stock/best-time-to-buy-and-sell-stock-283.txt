class Solution {

    public int maxProfit(int[] prices) {
        int left = 0;
        int right = 0;
        int current = 0;
        int result = 0;
        boolean found = false;
        while (left != prices.length - 1 && prices[left + 1] <= prices[left]) {
            left++;
        }
        right = left + 1;
        while (right < prices.length) {
            if (prices[right] > prices[left]) {
                current = prices[right] - prices[left];
            }
            if (current > result) {
                result = current;
            }
            if (prices[right] < prices[left]) {
                left = right;
            }
            right++;
        }
        return result;
    }
}
