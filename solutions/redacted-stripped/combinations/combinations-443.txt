class Solution {

    List<List<Integer>> var1 = new ArrayList<>();

    int n, k;

    public List<List<Integer>> combine(int n, int k) {
        this.n = n;
        this.k = k;
        backtrack(1, new ArrayList<>());
        return var1;
    }

    void backtrack(int id, List<Integer> combs) {
        if (combs.size() == k) {
            var1.add(new ArrayList<>(combs));
            return;
        }
        for (int i = id; i <= n; i++) {
            combs.add(i);
            backtrack(i + 1, combs);
            combs.remove(combs.size() - 1);
        }
    }
}
