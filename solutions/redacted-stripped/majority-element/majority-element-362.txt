class Solution {

    public int method1(int[] arr) {
        int n = arr.length;
        int count = 0;
        int ele = 0;
        for (int i = 0; i < n; i++) {
            if (count == 0) {
                count++;
                ele = arr[i];
            } else if (ele == arr[i]) {
                count++;
            } else
                count--;
        }
        return ele;
    }
}
