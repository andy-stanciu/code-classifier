class Solution {

    public int[] method1(int n) {
        int count = 0;
        int[] arr = new int[n + 1];
        while (count <= n) {
            arr[count] = Integer.bitCount(count);
            count++;
        }
        return arr;
    }
}
