class Solution {

    public int[] method1(int n) {
        int[] arr = new int[n + 1];
        arr[0] = 0;
        int position = 0;
        for (int i = 1; i <= n; i++) {
            position = i / 2;
            if (i % 2 == 0) {
                arr[i] = arr[position];
            } else {
                arr[i] = arr[position] + 1;
            }
        }
        return arr;
    }
}
