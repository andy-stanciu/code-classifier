class Solution {

    public int[] method1(int n) {
        int[] cbit = new int[n + 1];
        for (int i = 0; i <= n; i++) {
            cbit[i] = Integer.bitCount(i);
        }
        return cbit;
    }
}
