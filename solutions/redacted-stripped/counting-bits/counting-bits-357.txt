class Solution {

    public int[] method1(int n) {
        int length = n + 1;
        int[] arr = new int[length];
        for (int i = 1, pow = 1; i <= n; i++) {
            if (i == pow * 2)
                pow *= 2;
            arr[i] = 1 + arr[i - pow];
        }
        return arr;
    }
}
