class Solution {

    public int[] method1(int n) {
        int[] ans = new int[n + 1];
        int k;
        for (int i = 1; i <= n; i++) {
            k = i & 1;
            ans[i] = ans[i >> 1] + k;
        }
        return ans;
    }
}
