class Solution {

    public int method1(int n) {
        if (n > 1)
            return n % 2 + method1(n / 2);
        return n % 2;
    }

    public int[] method2(int n) {
        int[] res = new int[n + 1];
        for (int i = 0; i <= n; i++) res[i] = method1(i);
        return res;
    }
}
