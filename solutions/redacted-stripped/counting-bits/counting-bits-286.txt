class Solution {

    public int[] method1(int n) {
        int[] ans = new int[n + 1];
        for (int i = 0; i <= n; i++) {
            int count = Integer.bitCount(i);
            ans[i] = count;
        }
        return ans;
    }
}
