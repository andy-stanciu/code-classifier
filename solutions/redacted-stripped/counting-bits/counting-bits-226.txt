class Solution {

    public int[] method1(int n) {
        int[] answer = new int[n + 1];
        answer[0] = 0;
        for (int i = 1; i <= n; i++) {
            answer[i] = answer[i / 2] + i % 2;
        }
        return answer;
    }
}
