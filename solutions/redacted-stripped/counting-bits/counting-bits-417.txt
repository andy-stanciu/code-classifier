class Solution {

    public int[] method1(int n) {
        int[] ans = new int[n + 1];
        for (int i = 1; i <= n; i++) {
            ans[i] = (i % 2) + ans[i / 2];
        }
        return ans;
    }
}
