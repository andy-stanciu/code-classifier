class Solution {

    public int[] method1(int n) {
        int[] ans = new int[n + 1];
        for (int i = 0; i <= n; i++) {
            ans[i] = method2(i);
        }
        return ans;
    }

    private int method2(int n) {
        int cnt = 0;
        while (n > 0) {
            n = n - (n & -n);
            cnt++;
        }
        return cnt;
    }
}
