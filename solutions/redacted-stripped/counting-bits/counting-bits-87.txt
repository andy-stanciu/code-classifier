class Solution {

    public int[] method1(int n) {
        int[] ans = new int[n + 1];
        int l = 1;
        for (int i = 1; i <= n; i++) {
            if (i != 0 && (i & (i - 1)) == 0)
                l = i;
            ans[i] = ans[i % l] + 1;
        }
        return ans;
    }
}
