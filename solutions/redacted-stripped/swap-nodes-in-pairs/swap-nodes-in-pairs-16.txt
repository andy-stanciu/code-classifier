class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode temp = head;
        head = head.next;
        ListNode var1 = head.next;
        head.next = temp;
        head.next.next = method1(var1);
        return head;
    }
}
