class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode curr = head, next = head.next, nextN = head.next.next;
        next.next = curr;
        curr.next = method1(nextN);
        return next;
    }
}
