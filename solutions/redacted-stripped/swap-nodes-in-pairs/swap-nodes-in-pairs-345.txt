class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode restList = method1(head.next.next);
        ListNode secondOne = head.next;
        secondOne.next = head;
        head.next = restList;
        return secondOne;
    }
}
