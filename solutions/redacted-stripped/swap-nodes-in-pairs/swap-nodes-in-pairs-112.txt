class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        return new ListNode(head.next.val, new ListNode(head.val, method1(head.next.next)));
    }
}
