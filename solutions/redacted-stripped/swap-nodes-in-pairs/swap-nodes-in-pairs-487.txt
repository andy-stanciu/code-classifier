class Solution {

    public ListNode method1(ListNode head) {
        if (head == null)
            return null;
        ListNode node = head;
        ListNode next = head.next;
        if (next != null) {
            node.next = method1(next.next);
            next.next = head;
            return next;
        }
        return node;
    }
}
