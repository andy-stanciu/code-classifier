class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode second = head.next;
        ListNode pr = method1(second.next);
        second.next = head;
        head.next = pr;
        return second;
    }
}
