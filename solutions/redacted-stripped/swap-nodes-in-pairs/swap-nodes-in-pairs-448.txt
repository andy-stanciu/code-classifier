class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode newNode = method1(head.next.next);
        ListNode newHead = head.next;
        head.next.next = head;
        head.next = newNode;
        return newHead;
    }
}
