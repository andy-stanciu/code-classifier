class Solution {

    public ListNode method1(ListNode left) {
        if (left == null || left.next == null)
            return left;
        ListNode right = left.next;
        left.next = method1(right.next);
        right.next = left;
        return right;
    }
}
