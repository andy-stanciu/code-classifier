class Solution {

    public ListNode method1(ListNode head) {
        ListNode dummy = new ListNode();
        dummy.next = head;
        method2(dummy, head);
        return dummy.next;
    }

    public void method2(ListNode prev, ListNode first) {
        if (first == null || first.next == null)
            return;
        ListNode second = first.next;
        ListNode nextfirst = second.next;
        first.next = nextfirst;
        second.next = first;
        prev.next = second;
        method2(first, nextfirst);
    }
}
