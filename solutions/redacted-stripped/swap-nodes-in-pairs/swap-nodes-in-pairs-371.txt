class Solution {

    public ListNode method1(ListNode head) {
        if (head != null) {
            ListNode next = head.next;
            if (next != null) {
                ListNode tail = next.next;
                next.next = head;
                head.next = method1(tail);
                return next;
            }
        }
        return head;
    }
}
