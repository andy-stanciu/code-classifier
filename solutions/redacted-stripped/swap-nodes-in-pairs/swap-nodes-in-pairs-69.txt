class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode nextNode = head.next;
        head.next = method1(nextNode.next);
        nextNode.next = head;
        return nextNode;
    }
}
