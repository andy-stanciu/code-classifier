class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode temp = head;
        ListNode p = head.next.next;
        head = head.next;
        head.next = temp;
        temp.next = method1(p);
        return head;
    }
}
