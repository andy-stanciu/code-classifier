class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode fnode = head;
        ListNode snode = head.next;
        fnode.next = method1(snode.next);
        snode.next = fnode;
        return snode;
    }
}
