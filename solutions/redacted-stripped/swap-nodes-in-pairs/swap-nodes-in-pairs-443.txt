class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode cur = head;
        ListNode next = cur.next;
        ListNode doubleNext = next.next;
        next.next = cur;
        cur.next = method1(doubleNext);
        return next;
    }
}
