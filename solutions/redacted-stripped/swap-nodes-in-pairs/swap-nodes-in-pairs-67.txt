class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode tHead = method1(head.next.next);
        ListNode temp = head.next;
        head.next = tHead;
        temp.next = head;
        return temp;
    }
}
