class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode prev = head;
        ListNode curr = prev.next;
        ListNode forward = curr.next;
        curr.next = prev;
        prev.next = method1(forward);
        return curr;
    }
}
