class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode ans = head.next;
        ListNode ptr = head.next.next;
        head.next.next = head;
        head.next = method1(ptr);
        return ans;
    }
}
