class Solution {

    public ListNode method1(ListNode head) {
        if (head == null || head.next == null)
            return head;
        ListNode b = head.next;
        ListNode c = b.next;
        b.next = head;
        head.next = method1(c);
        return b;
    }
}
