class Solution {

    public ListNode method1(ListNode head) {
        ListNode temp = head;
        while (temp != null && temp.next != null) {
            int temp1 = temp.val;
            temp.val = temp.next.val;
            temp.next.val = temp1;
            temp = temp.next.next;
        }
        return head;
    }
}
