class Solution {

    public List<List<String>> method1(String[] strs) {
        Map<String, List<String>> map = new HashMap();
        for (int i = 0; i < strs.length; i++) {
            char[] charsOfWord = strs[i].toCharArray();
            Arrays.sort(charsOfWord);
            String sortedWord = String.valueOf(charsOfWord);
            List<String> var1 = map.get(sortedWord);
            if (!map.containsKey(sortedWord)) {
                map.put(sortedWord, new ArrayList<>(List.of(strs[i])));
            } else {
                map.get(sortedWord).add(strs[i]);
            }
        }
        return new ArrayList<>(map.values());
    }
}
