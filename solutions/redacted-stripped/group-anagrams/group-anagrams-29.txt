class Solution {

    public List<List<String>> method1(String[] word) {
        Map<String, List<String>> m = new HashMap<>();
        for (String s : word) {
            char[] ch = s.toCharArray();
            Arrays.sort(ch);
            String sw = new String(ch);
            if (!m.containsKey(sw)) {
                m.put(sw, new ArrayList<>());
            }
            m.get(sw).add(s);
        }
        return new ArrayList<>(m.values());
    }
}
