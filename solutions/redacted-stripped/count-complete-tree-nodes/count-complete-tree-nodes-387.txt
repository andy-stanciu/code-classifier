class Solution {

    int ans = 0;

    void preorder(TreeNode root) {
        if (root == null)
            return;
        ans++;
        preorder(root.left);
        preorder(root.right);
    }

    public int method1(TreeNode root) {
        preorder(root);
        return ans;
    }
}
