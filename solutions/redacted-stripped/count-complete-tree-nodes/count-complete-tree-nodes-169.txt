class Solution {

    public int no(TreeNode a) {
        if (a == null)
            return 0;
        return 1 + no(a.left) + no(a.right);
    }

    public int method1(TreeNode root) {
        return no(root);
    }
}
