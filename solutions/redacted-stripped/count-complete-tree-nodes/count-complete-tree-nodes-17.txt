class Solution {

    public int method1(TreeNode root) {
        if (root == null) {
            return 0;
        } else {
            int left = method1(root.left);
            int right = method1(root.right);
            return 1 + left + right;
        }
    }
}
