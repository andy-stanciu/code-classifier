class Solution {

    public int method1(TreeNode root) {
        return method2(root);
    }

    private int method2(TreeNode node) {
        if (node == null) {
            return 0;
        }
        return 1 + method2(node.left) + method2(node.right);
    }
}
