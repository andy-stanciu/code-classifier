class Solution {

    int sum = 0;

    public int method1(TreeNode root) {
        if (root == null)
            return sum;
        sum = sum + 1;
        method1(root.left);
        method1(root.right);
        return sum;
    }
}
