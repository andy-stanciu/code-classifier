class Solution {

    public int method1(TreeNode root) {
        if (root == null) {
            return 0;
        }
        int lft = method1(root.left);
        int rht = method1(root.right);
        int ans = lft + rht + 1;
        return ans;
    }
}
