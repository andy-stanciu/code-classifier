class Solution {

    public int method1(TreeNode root) {
        if (root == null) {
            return 0;
        }
        int rightSide = method1(root.right);
        int leftSide = method1(root.left);
        return rightSide + leftSide + 1;
    }
}
