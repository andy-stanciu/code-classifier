class Solution {

    int size = 0;

    public int method1(TreeNode root) {
        if (root == null)
            return 0;
        size++;
        int x = method1(root.left);
        x = method1(root.right);
        return size;
    }
}
