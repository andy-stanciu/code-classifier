class Solution {

    public int method1(TreeNode root) {
        if (root == null) {
            return 0;
        }
        int leftNode = method1(root.left);
        int rightNode = method1(root.right);
        return 1 + leftNode + rightNode;
    }
}
