class Solution {

    int var1 = 0;

    public int method1(TreeNode root) {
        helper(root);
        return var1;
    }

    void helper(TreeNode node) {
        if (node == null) {
            return;
        }
        var1++;
        helper(node.left);
        helper(node.right);
    }
}
