class Solution {

    public int method1(TreeNode root) {
        if (root == null) {
            return 0;
        }
        int lc = method1(root.left);
        int rc = method1(root.right);
        return 1 + lc + rc;
    }
}
