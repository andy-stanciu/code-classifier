class Solution {

    int var1 = 0;

    public int method1(TreeNode root) {
        solve(root);
        return var1;
    }

    private void solve(TreeNode root) {
        if (root == null)
            return;
        var1++;
        solve(root.left);
        solve(root.right);
    }
}
