class Solution {

    public int strStr(String haystack, String needle) {
        int m = haystack.length();
        int n = needle.length();
        for (int start = 0; start <= m - n; start++) {
            if (haystack.substring(start, needle.length() + start).equals(needle)) {
                return start;
            }
        }
        return -1;
    }
}
