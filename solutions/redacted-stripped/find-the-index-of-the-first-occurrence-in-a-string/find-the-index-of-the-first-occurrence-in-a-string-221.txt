class Solution {

    public int strStr(String haystack, String needle) {
        int n = needle.length();
        for (int i = 0; i <= haystack.length() - n; i++) {
            String var1 = haystack.var1(i, i + n);
            if (var1.equals(needle)) {
                return i;
            }
        }
        return -1;
    }
}
