class Solution {

    public int method1(int[] nums) {
        int cur_sum = 0;
        int max_sum = nums[0];
        for (int i = 0; i < nums.length; i++) {
            cur_sum += nums[i];
            max_sum = max_sum > cur_sum ? max_sum : cur_sum;
            if (cur_sum < 0) {
                cur_sum = 0;
            }
        }
        return max_sum;
    }
}
