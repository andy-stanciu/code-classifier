class Solution {

    public int method1(int[] nums) {
        int result = nums[0], temp = 0;
        for (int num : nums) {
            temp = temp > 0 ? temp + num : num;
            if (result < temp)
                result = temp;
        }
        return result;
    }
}
