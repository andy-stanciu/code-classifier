class Solution {

    public int method1(int[] nums) {
        int var1 = -10000;
        int maxSubSum = nums[0];
        for (int num : nums) {
            var1 = Math.max(num + var1, num);
            maxSubSum = Math.max(var1, maxSubSum);
        }
        return maxSubSum;
    }
}
