class Solution {

    public int method1(int[] nums) {
        int maxSum = Integer.MIN_VALUE;
        int leftToRight = 0;
        for (int j : nums) {
            if (leftToRight < 0)
                leftToRight = 0;
            leftToRight += j;
            maxSum = Math.max(leftToRight, maxSum);
        }
        return maxSum;
    }
}
