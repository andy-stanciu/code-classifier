class Solution {

    public int method1(int[] nums) {
        int currentSum = 0;
        int maxSum = Integer.MIN_VALUE;
        for (int number : nums) {
            currentSum = currentSum <= 0 ? number : currentSum + number;
            maxSum = Math.max(currentSum, maxSum);
        }
        return maxSum;
    }
}
