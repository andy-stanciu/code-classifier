class Solution {

    public String method1(int num) {
        int[] var1 = { 1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1 };
        String[] symbols = { "M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I" };
        StringBuffer sbf = new StringBuffer();
        int temp = num;
        for (int i = 0; i < 13 && temp > 0; i++) {
            if (temp / var1[i] >= 1) {
                int times = temp / var1[i];
                while (times > 0) {
                    sbf.append(symbols[i]);
                    times--;
                }
            }
            temp = temp % var1[i];
        }
        return sbf.toString();
    }
}
