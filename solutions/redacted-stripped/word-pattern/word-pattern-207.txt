class Solution {

    public boolean method1(String param1, String s) {
        String[] arr = s.split(" ");
        HashMap<Character, String> mp1 = new HashMap<>();
        HashMap<String, Character> mp2 = new HashMap<>();
        if (param1.length() != arr.length)
            return false;
        for (int i = 0; i < param1.length(); i++) {
            char a = param1.charAt(i);
            String t = arr[i];
            if (mp1.containsKey(a) && !mp1.get(a).equals(t)) {
                return false;
            }
            if (mp2.containsKey(t) && mp2.get(t) != a) {
                return false;
            }
            mp1.put(a, t);
            mp2.put(t, a);
        }
        return true;
    }
}
