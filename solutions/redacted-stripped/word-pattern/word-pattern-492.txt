class Solution {

    public boolean method1(String param1, String s) {
        String[] var1 = s.split(" ");
        int var2 = var1.length;
        int var3 = param1.length();
        if (var2 != var3)
            return false;
        HashMap<Character, String> map = new HashMap<>();
        for (int i = 0; i < var2; i++) {
            char c = param1.charAt(i);
            String var4 = var1[i];
            if (map.containsKey(c)) {
                if (!map.get(c).equals(var4))
                    return false;
            } else {
                if (map.containsValue(var4))
                    return false;
                map.put(c, var4);
            }
        }
        return true;
    }
}
