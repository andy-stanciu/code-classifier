class Solution {

    public boolean method1(String param1, String str) {
        String[] splited_Str = str.split(" ");
        if (splited_Str.length != param1.length())
            return false;
        Map string_Map = new HashMap();
        for (Integer i = 0; i < splited_Str.length; ++i) {
            if (string_Map.put(param1.charAt(i), i) != string_Map.put(splited_Str[i], i))
                return false;
        }
        return true;
    }
}
