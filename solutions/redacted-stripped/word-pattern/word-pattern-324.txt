class Solution {

    public boolean method1(String param1, String s) {
        String[] s_arr = s.split(" ");
        if (s.length() == 0 || param1.length() == 0 || s_arr.length != param1.length())
            return false;
        HashMap<Character, String> hmap = new HashMap<>();
        for (int i = 0; i < s_arr.length; i++) {
            if (hmap.containsKey(param1.charAt(i))) {
                if (!hmap.get(param1.charAt(i)).equals(s_arr[i])) {
                    return false;
                }
            } else if (hmap.containsValue(s_arr[i]) && !hmap.containsKey(param1.charAt(i))) {
                return false;
            } else {
                hmap.put(param1.charAt(i), s_arr[i]);
            }
        }
        return true;
    }
}
