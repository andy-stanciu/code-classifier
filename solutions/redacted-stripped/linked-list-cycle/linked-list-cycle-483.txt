public class Solution {

    public boolean method1(ListNode head) {
        Set<ListNode> s = new HashSet<>();
        ListNode dummy = head;
        while (dummy != null) {
            if (s.contains(dummy)) {
                return true;
            }
            s.add(dummy);
            dummy = dummy.next;
        }
        return false;
    }
}
