public class Solution {

    public boolean method1(ListNode head) {
        ArrayList<ListNode> ref = new ArrayList<ListNode>();
        while (head != null) {
            if (!ref.contains(head)) {
                ref.add(head);
            } else {
                return true;
            }
            head = head.next;
        }
        return false;
    }
}
