public class Solution {

    public boolean method1(ListNode head) {
        int count = 0;
        while (head != null) {
            if (count > 10000)
                break;
            count++;
            head = head.next;
        }
        if (count > 10000)
            return true;
        else
            return false;
    }
}
