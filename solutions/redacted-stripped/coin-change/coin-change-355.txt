class Solution {

    public int method1(int[] param1, int amount) {
        if (amount == 0)
            return 0;
        int[] c = new int[amount + 1];
        int i = 0;
        while (i <= amount) {
            c[i] = Integer.MAX_VALUE;
            for (int var1 : param1) {
                if (i < var1) {
                    continue;
                } else if (i == var1) {
                    c[i] = 1;
                } else {
                    if (c[i - var1] != Integer.MAX_VALUE) {
                        c[i] = Math.min(c[i], 1 + c[i - var1]);
                    }
                }
            }
            i += 1;
        }
        if (c[amount] == Integer.MAX_VALUE)
            return -1;
        return c[amount];
    }
}
