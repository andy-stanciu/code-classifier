class Solution {

    public static int compress(char[] chars) {
        int writeindex = 0;
        int i = 0;
        while (i < chars.length) {
            char curr = chars[i];
            int point = i;
            while (i < chars.length && chars[i] == curr) {
                i++;
            }
            chars[writeindex++] = curr;
            if (i - point > 1) {
                for (char c : Integer.toString(i - point).toCharArray()) {
                    chars[writeindex++] = c;
                }
            }
        }
        return writeindex;
    }
}
