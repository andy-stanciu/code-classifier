class Solution {

    public ListNode method1(ListNode head, int n) {
        int length = 0;
        ListNode curr = head;
        while (curr != null) {
            length++;
            curr = curr.next;
        }
        int m = length - n;
        if (m == 0) {
            return head.next;
        }
        curr = head;
        for (int i = 1; i < m; i++) {
            curr = curr.next;
        }
        curr.next = curr.next.next;
        return head;
    }
}
