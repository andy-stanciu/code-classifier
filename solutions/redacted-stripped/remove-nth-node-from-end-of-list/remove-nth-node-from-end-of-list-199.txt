class Solution {

    int index;

    public ListNode method1(ListNode head, int n) {
        if (head == null) {
            index = 0;
            return null;
        }
        head.next = method1(head.next, n);
        index++;
        if (n == index) {
            return head.next;
        }
        return head;
    }
}
