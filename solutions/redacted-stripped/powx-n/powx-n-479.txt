class Solution {

    public double myPow(double x, int n) {
        double res;
        if (n < 0 && n != Integer.MIN_VALUE) {
            n = Math.abs(n);
            res = Math.pow(1 / x, n);
            return res;
        } else {
            return Math.pow(x, n);
        }
    }
}
