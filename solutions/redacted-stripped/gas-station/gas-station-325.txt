class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int n = param1.length;
        for (int i = 0; i < n; i++) {
            if (param1[i] < cost[i]) {
                continue;
            }
            int sum = 0;
            int k = i;
            int c = 0;
            for (int j = 0; j < n; j++) {
                c++;
                sum += param1[(k) % n] - cost[(k++) % n];
                if (sum <= 0) {
                    break;
                }
            }
            if (sum >= 0 && c == n) {
                return i;
            }
        }
        return -1;
    }
}
