class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int n = param1.length;
        int tot = 0;
        int curr = 0;
        int start = 0;
        for (int i = 0; i < n; i++) {
            curr += param1[i] - cost[i];
            tot += param1[i] - cost[i];
            if (curr < 0) {
                curr = 0;
                start = i + 1;
            }
        }
        return tot < 0 ? -1 : start;
    }
}
