class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int currGain = 0;
        int totalGain = 0;
        int answer = 0;
        for (int i = 0; i < param1.length; i++) {
            totalGain = totalGain + param1[i] - cost[i];
            currGain = currGain + param1[i] - cost[i];
            if (currGain < 0) {
                answer = i + 1;
                currGain = 0;
            }
        }
        return totalGain >= 0 ? answer : -1;
    }
}
