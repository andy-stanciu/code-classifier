class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int accu = 0;
        int start = -1;
        int min = 0;
        for (int i = 0; i < param1.length; i++) {
            accu += param1[i] - cost[i];
            if (accu < min) {
                min = accu;
                start = i;
            }
        }
        if (accu < 0) {
            return -1;
        } else {
            return start + 1;
        }
    }
}
