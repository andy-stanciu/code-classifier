class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int totalEarn = 0;
        int totalExpense = 0;
        for (int i = 0; i < param1.length; i++) {
            totalEarn += param1[i];
            totalExpense += cost[i];
        }
        if (totalEarn < totalExpense)
            return -1;
        totalEarn = 0;
        totalExpense = 0;
        int ans = 0;
        for (int i = 0; i < param1.length; i++) {
            totalEarn = totalEarn + param1[i] - cost[i];
            if (totalEarn < 0) {
                totalEarn = 0;
                ans = i + 1;
            }
        }
        return ans;
    }
}
