class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int currTank = 0, maxTank = 0, resInd = 0;
        for (int i = param1.length - 1; i >= 0; i--) {
            currTank += param1[i] - cost[i];
            if (currTank >= maxTank) {
                maxTank = currTank;
                resInd = i;
            }
        }
        return currTank < 0 ? -1 : resInd;
    }
}
