class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int start = 0, total = 0, currentSum = 0;
        for (int i = 0; i < param1.length; i++) {
            total += param1[i] - cost[i];
            currentSum += param1[i] - cost[i];
            if (currentSum < 0) {
                start = i + 1;
                currentSum = 0;
            }
        }
        return total >= 0 ? start : -1;
    }
}
