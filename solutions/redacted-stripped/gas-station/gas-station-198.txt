class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int n = param1.length;
        int leftOver = 0;
        int start = 0;
        int cur = 0;
        for (int i = 0; i < n; i++) {
            leftOver += param1[i] - cost[i];
            cur += param1[i] - cost[i];
            if (cur < 0) {
                start = i + 1;
                cur = 0;
            }
        }
        return leftOver >= 0 ? start : -1;
    }
}
