class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        if (getSum(param1) < getSum(cost))
            return -1;
        int total = 0;
        int startPoint = 0;
        for (int i = 0; i < param1.length; i++) {
            total += (param1[i] - cost[i]);
            if (total < 0) {
                total = 0;
                startPoint = i + 1;
            }
        }
        return startPoint;
    }

    public int getSum(int[] A) {
        int sum = 0;
        for (int i = 0; i < A.length; i++) sum += A[i];
        return sum;
    }
}
