class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int sum1 = 0, sum2 = 0;
        for (int i : param1) {
            sum1 += i;
        }
        for (int i : cost) {
            sum2 += i;
        }
        if (sum1 < sum2)
            return -1;
        int total = 0, index = 0;
        for (int i = 0; i < param1.length; i++) {
            total += param1[i] - cost[i];
            if (total < 0) {
                index = i + 1;
                total = 0;
            }
        }
        return index;
    }
}
