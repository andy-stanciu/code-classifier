class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int n = param1.length;
        int sum1 = 0, sum2 = 0;
        for (int i = 0; i < n; i++) {
            sum1 += param1[i];
            sum2 += cost[i];
        }
        if (sum1 < sum2) {
            return -1;
        }
        int remaining_fuel = 0;
        int ans = 0;
        for (int i = 0; i < n; i++) {
            remaining_fuel += param1[i] - cost[i];
            if (remaining_fuel < 0) {
                ans = i + 1;
                remaining_fuel = 0;
            }
        }
        return ans;
    }
}
