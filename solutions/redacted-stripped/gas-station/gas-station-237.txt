class Solution {

    public int canCompleteCircuit(int[] param1, int[] cost) {
        int n = param1.length;
        int netFuel = 0;
        int start = 0;
        int tank = 0;
        for (int i = 0; i < n; i++) {
            netFuel += param1[i] - cost[i];
            tank += param1[i] - cost[i];
            if (tank < 0) {
                tank = 0;
                start = i + 1;
            }
        }
        return (netFuel < 0) ? (-1) : (start);
    }
}
