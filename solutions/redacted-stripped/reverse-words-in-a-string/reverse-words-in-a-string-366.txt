class Solution {

    public String method1(String s) {
        String s1 = "";
        String[] str = s.split(" ");
        for (int i = str.length - 1; i >= 0; i--) {
            s1 = s1 + " " + str[i];
            s1 = s1.replace("  ", " ");
        }
        return s1.trim();
    }
}
