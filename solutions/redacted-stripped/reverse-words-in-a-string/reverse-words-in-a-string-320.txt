class Solution {

    public String method1(String s) {
        String[] a = s.split(" ");
        String ref = "";
        for (int i = a.length - 1; i >= 0; i--) {
            ref = ref + a[i] + " ";
        }
        return String.join(" ", ref.trim().split("\\\\s+"));
    }
}
