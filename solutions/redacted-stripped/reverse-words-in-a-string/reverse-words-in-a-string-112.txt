class Solution {

    public String method1(String s) {
        String[] var1 = s.trim().split(" ");
        s = "";
        for (int i = var1.length - 1; i >= 0; i--) {
            if (var1[i].trim() != "") {
                s += var1[i].trim();
                s += " ";
            }
        }
        return s.trim();
    }
}
