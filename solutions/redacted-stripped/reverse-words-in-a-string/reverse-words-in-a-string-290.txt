class Solution {

    public String method1(String s) {
        String[] array = s.trim().split(" ");
        StringBuilder ans = new StringBuilder();
        int length = array.length - 1;
        while (length > 0) {
            if (array[length].matches("[a-zA-Z0-9]+")) {
                ans.append(array[length]);
                ans.append(" ");
            }
            length--;
        }
        ans.append(array[0]);
        return ans.toString();
    }
}
