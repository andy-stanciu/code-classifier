class Solution {

    public String method1(String s) {
        s = s.trim();
        Scanner str = new Scanner(s);
        s = "";
        while (str.hasNext()) {
            s = str.next() + " " + s;
        }
        return s.trim();
    }
}
