class Solution {

    public String method1(String s) {
        String[] st = s.trim().split("\\\\s+");
        String output = "";
        for (int i = st.length - 1; i > 0; i--) {
            output += st[i] + " ";
        }
        return output + st[0];
    }
}
