class Solution {

    public String method1(String param1) {
        param1 = param1.trim();
        List<String> list = Arrays.asList(param1.split("\\\\s+"));
        Collections.reverse(list);
        return String.join(" ", list);
    }
}
