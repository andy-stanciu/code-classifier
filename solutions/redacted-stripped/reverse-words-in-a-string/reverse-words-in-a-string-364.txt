class Solution {

    public String method1(String s) {
        StringTokenizer str = new StringTokenizer(s);
        String rev = "";
        while (str.hasMoreTokens()) rev = str.nextToken() + " " + rev;
        return (rev.substring(0, rev.length() - 1));
    }
}
