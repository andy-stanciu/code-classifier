class Solution {

    public String method1(String s) {
        String[] splited = s.split("\\\\s+");
        StringBuilder var1 = new StringBuilder();
        for (int i = splited.length - 1; i >= 0; i--) {
            var1.append(splited[i]).append(" ");
        }
        return var1.toString().trim();
    }
}
