class Solution {

    public String method1(String s) {
        String[] arr = s.split("\\\\s+");
        Collections.reverse(Arrays.asList(arr));
        s = String.join(" ", arr);
        return s.trim();
    }
}
