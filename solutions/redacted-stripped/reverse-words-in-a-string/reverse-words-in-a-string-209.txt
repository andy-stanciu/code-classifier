class Solution {

    public String method1(String s) {
        List<String> list = new ArrayList<>(Arrays.stream(s.trim().split(" ")).filter(str -> !str.isBlank()).toList());
        Collections.reverse(list);
        return String.join(" ", list);
    }
}
