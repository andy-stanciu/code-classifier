class Solution {

    public String method1(String s) {
        String str = "";
        String[] arr = s.trim().split("\\\\s+");
        for (int i = arr.length - 1; i >= 0; i--) {
            str += " " + arr[i];
        }
        return str.trim();
    }
}
