class Solution {

    public int method1(int curr, int n, int count, int[] dp) {
        if (curr > n) {
            return count;
        }
        if (n == curr) {
            count++;
            return count;
        }
        if (dp[curr] != -1)
            return dp[curr];
        dp[curr] = method1(curr + 1, n, count, dp) + method1(curr + 2, n, count, dp);
        return dp[curr];
    }

    public int method2(int n) {
        int[] dp = new int[n + 1];
        Arrays.fill(dp, -1);
        return method1(0, n, 0, dp);
    }
}
