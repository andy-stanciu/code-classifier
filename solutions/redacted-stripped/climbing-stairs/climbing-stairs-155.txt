class Solution {

    public int method1(int n) {
        int[] climbStore = new int[n + 1];
        climbStore[0] = 1;
        climbStore[1] = 1;
        for (int i = 2; i <= n; i++) {
            climbStore[i] = climbStore[i - 1] + climbStore[i - 2];
        }
        return climbStore[n];
    }
}
