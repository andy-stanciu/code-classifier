class Solution {

    public int method1(int n) {
        if (n == 0 || n == 1)
            return 1;
        int oneStep = method1(n - 1);
        int twoSteps = method1(n - 2);
        return oneStep + twoSteps;
    }
}
