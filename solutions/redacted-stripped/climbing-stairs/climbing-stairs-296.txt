class Solution {

    public int method1(int n) {
        int[] ans = new int[n];
        ans[0] = 1;
        if (n > 1) {
            ans[1] = 2;
        }
        for (int i = 2; i < n; i++) {
            ans[i] = ans[i - 1] + ans[i - 2];
        }
        return ans[n - 1];
    }
}
