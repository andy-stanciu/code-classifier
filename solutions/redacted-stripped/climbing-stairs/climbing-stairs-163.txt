class Solution {

    public int method1(int n) {
        int prev = 1;
        int prevPrev = 0;
        for (int i = 1; i <= n; i++) {
            int temp = prev + prevPrev;
            prevPrev = prev;
            prev = temp;
        }
        return prev;
    }
}
