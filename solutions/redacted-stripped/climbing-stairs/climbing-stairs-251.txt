class Solution {

    public int method1(int n) {
        int previous = 0, current = 1;
        int next = 1;
        while (n-- > 0) {
            previous = current;
            current = next;
            next = previous + current;
        }
        return current;
    }
}
