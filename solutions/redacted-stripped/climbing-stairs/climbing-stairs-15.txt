class Solution {

    private Map<Integer, Integer> memo = new HashMap<>();

    public int method1(int n) {
        if (n == 1)
            return 1;
        if (n == 2)
            return 2;
        if (memo.containsKey(n)) {
            return memo.get(n);
        }
        int ways = method1(n - 1) + method1(n - 2);
        memo.put(n, ways);
        return ways;
    }
}
