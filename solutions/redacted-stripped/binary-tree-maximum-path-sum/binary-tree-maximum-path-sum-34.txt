class Solution {

    int var1 = Integer.MIN_VALUE;

    public int inOrder(TreeNode root) {
        if (root == null)
            return 0;
        int left = inOrder(root.left);
        int right = inOrder(root.right);
        int maxB = root.val + Math.max(left, right);
        maxB = Math.max(root.val, maxB);
        int var2 = Math.max(maxB, root.val + left + right);
        var1 = Math.max(var1, var2);
        return maxB;
    }

    public int method1(TreeNode root) {
        var1 = root.val;
        int var3 = inOrder(root);
        return var1;
    }
}
