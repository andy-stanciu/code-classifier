class Solution {

    static class Pair {

        int var1;

        int var2;

        public Pair(int var2, int var1) {
            this.var2 = var2;
            this.var1 = var1;
        }
    }

    public int method1(TreeNode root) {
        if (root == null)
            return 0;
        return helper(root).var2;
    }

    public Pair helper(TreeNode root) {
        if (root == null)
            return new Pair(Integer.MIN_VALUE, Integer.MIN_VALUE);
        Pair left = helper(root.left);
        Pair right = helper(root.right);
        int var1 = Math.max(Math.max(left.var1, right.var1), 0) + root.val;
        int var3 = Math.max(left.var2, right.var2);
        int var4 = Math.max(0, left.var1) + root.val + Math.max(0, right.var1);
        int var2 = Math.max(var4, Math.max(var1, var3));
        return new Pair(var2, var1);
    }
}
