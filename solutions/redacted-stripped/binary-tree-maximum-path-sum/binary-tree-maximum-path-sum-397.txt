class Solution {

    int var1 = Integer.MIN_VALUE;

    public int var1(TreeNode root) {
        method1(root);
        return var1;
    }

    public int method1(TreeNode curr) {
        if (curr == null)
            return 0;
        int var2 = method1(curr.left);
        int var3 = method1(curr.right);
        int var4 = Math.max(Math.max(Math.max(var2 + var3 + curr.val, var3 + curr.val), var2 + curr.val), curr.val);
        var1 = Math.max(var4, var1);
        int var5 = Math.max(var2, var3);
        if (var5 < 0)
            return curr.val;
        else
            return curr.val + var5;
    }
}
