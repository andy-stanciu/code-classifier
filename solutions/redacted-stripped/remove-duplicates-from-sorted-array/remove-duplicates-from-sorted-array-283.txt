class Solution {

    public int method1(int[] arr) {
        int x = 0;
        for (int i = 1; i < arr.length; i++) {
            if (arr[i] != arr[x]) {
                arr[++x] = arr[i];
            }
        }
        return x + 1;
    }
}
