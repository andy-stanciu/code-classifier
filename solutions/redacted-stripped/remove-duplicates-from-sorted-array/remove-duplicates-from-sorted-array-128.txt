class Solution {

    public int method1(int[] nums) {
        int idx = 1;
        for (int p = 1; p < nums.length; p++) {
            if (nums[p - 1] != nums[p]) {
                nums[idx] = nums[p];
                idx++;
            }
        }
        return idx;
    }
}
