class Solution {

    public int method1(int[] nums) {
        int cursor = 0;
        for (int i = 1; i < nums.length; i++) {
            if (nums[cursor] != nums[i]) {
                nums[++cursor] = nums[i];
            }
        }
        return ++cursor;
    }
}
