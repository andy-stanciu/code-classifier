class Solution {

    public int method1(int[] nums) {
        int curr = nums[0];
        int i = 1;
        for (int val : nums) {
            if (val == curr)
                continue;
            nums[i] = val;
            curr = val;
            i++;
        }
        return i;
    }
}
