class Solution {

    public int method1(int[][] obstacleGrid) {
        int ans = method2(obstacleGrid, 0, 0);
        return ans;
    }

    public int method2(int[][] obstacleGrid, int row, int col) {
        if (row == obstacleGrid.length - 1 && col == obstacleGrid[0].length - 1)
            return 1;
        if (obstacleGrid[row][col] == 1)
            return 0;
        int down = 0, right = 0;
        if (col + 1 <= obstacleGrid[0].length - 1)
            right = method2(obstacleGrid, row, col + 1);
        if (row + 1 <= obstacleGrid.length - 1)
            down = method2(obstacleGrid, row + 1, col);
        return down + right;
    }
}
