class Solution {

    public int method1(int[] nums) {
        Integer expectedSum = IntStream.range(0, nums.length + 1).sum();
        Integer actualSum = Arrays.stream(nums).reduce(0, Integer::sum);
        return expectedSum - actualSum;
    }
}
