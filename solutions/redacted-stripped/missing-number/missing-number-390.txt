class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int maxSum = 0;
        int actualSum = n * (n + 1) / 2;
        for (int num : nums) {
            maxSum += num;
        }
        return actualSum - maxSum;
    }
}
