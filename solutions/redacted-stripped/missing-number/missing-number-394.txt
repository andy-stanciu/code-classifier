class Solution {

    public int method1(int[] nums) {
        int length = nums.length;
        int max = length - 1;
        int sum_range = (length * (length + 1)) / 2;
        for (int i = 0; i < length; i++) {
            sum_range -= nums[i];
        }
        return sum_range;
    }
}
