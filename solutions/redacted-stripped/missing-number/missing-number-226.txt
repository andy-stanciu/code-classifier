class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        for (int i : nums) {
            sum = sum + i;
        }
        return (nums.length * (nums.length + 1) - 2 * sum) / 2;
    }
}
