class Solution {

    public int method1(int[] nums) {
        int l = nums.length;
        int expectedSum = l * (l + 1) / 2;
        int actualSum = 0;
        for (int i = 0; i < l; i++) {
            actualSum += nums[i];
        }
        return expectedSum - actualSum;
    }
}
