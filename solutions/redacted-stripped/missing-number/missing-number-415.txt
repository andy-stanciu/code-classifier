class Solution {

    public int method1(int[] nums) {
        int i;
        Arrays.sort(nums);
        for (i = 0; i <= nums.length; i++) {
            int index = Arrays.binarySearch(nums, i);
            if (index < 0) {
                break;
            }
        }
        return i;
    }
}
