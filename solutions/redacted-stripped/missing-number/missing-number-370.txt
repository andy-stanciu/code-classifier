class Solution {

    public int method1(int[] arr) {
        int n = arr.length;
        int totalsum = (n * (n + 1)) / 2;
        int s2 = 0;
        for (int i = 0; i < n; i++) {
            s2 += arr[i];
        }
        int msum = totalsum - s2;
        return msum;
    }
}
