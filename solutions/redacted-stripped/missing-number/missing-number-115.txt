class Solution {

    public int method1(int[] nums) {
        int length = nums.length;
        int idealTotalSum = (length * (length + 1)) / 2;
        int currentSum = 0;
        for (int each : nums) {
            currentSum += each;
        }
        return idealTotalSum - currentSum;
    }
}
