class Solution {

    public int method1(int[] nums) {
        int l = nums.length;
        int sum = 0;
        for (int i = 0; i < nums.length; i++) {
            sum = sum + nums[i];
        }
        int nsum = (l * (l + 1)) / 2;
        int miss = nsum - sum;
        return miss;
    }
}
