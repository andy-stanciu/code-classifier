class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int numsSize = nums.length;
        for (int num : nums) {
            sum += num;
        }
        return (numsSize * (numsSize + 1)) / 2 - sum;
    }
}
