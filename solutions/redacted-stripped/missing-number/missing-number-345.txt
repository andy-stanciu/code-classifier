class Solution {

    public int method1(int[] nums) {
        int tmp = 0;
        for (int i = 0; i < nums.length; i++) {
            tmp = tmp ^ nums[i];
            tmp = tmp ^ (i + 1);
        }
        return tmp;
    }
}
