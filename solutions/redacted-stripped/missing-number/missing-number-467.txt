class Solution {

    public int method1(int[] nums) {
        int total = 0;
        for (int i = 0; i < nums.length; i++) {
            total += nums[i];
        }
        return ((nums.length * (nums.length + 1)) / 2) - total;
    }
}
