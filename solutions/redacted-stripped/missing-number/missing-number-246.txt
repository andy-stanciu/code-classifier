class Solution {

    public int method1(int[] arr) {
        int n = arr.length;
        int sum = 0;
        for (int i = 0; i < n; i++) {
            sum += arr[i];
        }
        return ((n * (n + 1)) / 2) - sum;
    }
}
