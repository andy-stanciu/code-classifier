class Solution {

    public int method1(int[] nums) {
        int s = 0;
        int n = nums.length;
        int msum = (n * (n + 1)) / 2;
        for (int i = 0; i < n; i++) {
            s = s + nums[i];
        }
        return (msum - s);
    }
}
