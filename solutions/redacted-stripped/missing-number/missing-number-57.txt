class Solution {

    public int method1(int[] nums) {
        int size = nums.length;
        int sumOfNum = (size * (size + 1)) / 2;
        for (int num : nums) {
            sumOfNum -= num;
        }
        return sumOfNum;
    }
}
