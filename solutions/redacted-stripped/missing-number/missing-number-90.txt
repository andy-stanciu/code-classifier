class Solution {

    public int method1(int[] nums) {
        int result;
        int n = nums.length;
        result = n * (n + 1) / 2;
        int count = 0;
        for (int i = 0; i < nums.length; i++) {
            count += nums[i];
        }
        return result - count;
    }
}
