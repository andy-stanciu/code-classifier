class Solution {

    public int method1(int[] nums) {
        int ans = ((nums.length) * (nums.length + 1)) / 2;
        for (int n : nums) {
            ans = ans - n;
        }
        return ans;
    }
}
