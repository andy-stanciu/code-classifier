class Solution {

    public int method1(int[] nums) {
        for (int i = 1; i < nums.length; i++) {
            nums[i] += nums[i - 1];
        }
        return ((nums.length + 1) * (nums.length) / 2) - nums[nums.length - 1];
    }
}
