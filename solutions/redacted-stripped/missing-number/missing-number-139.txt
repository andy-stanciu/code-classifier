class Solution {

    public int method1(int[] nums) {
        int Gsum = (nums.length * (nums.length + 1)) / 2;
        int sum = 0;
        for (int i = 0; i < nums.length; i++) {
            sum += nums[i];
        }
        return Gsum - sum;
    }
}
