class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int res = 0;
        int n = nums.length;
        sum = (n * (n + 1) / 2);
        for (int i = 0; i < n; i++) {
            res += nums[i];
        }
        return sum - res;
    }
}
