class Solution {

    public int method1(int[] nums) {
        int N = nums.length;
        int tot_sum = N * (N + 1) / 2;
        int sum = 0;
        for (int i = 0; i < N; i++) {
            sum += nums[i];
        }
        return tot_sum - sum;
    }
}
