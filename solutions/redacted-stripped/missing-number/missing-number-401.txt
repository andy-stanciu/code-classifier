class Solution {

    public int method1(int[] nums) {
        int sum = 0;
        int len = nums.length;
        int totalsum = (int) len * (len + 1) / 2;
        for (int i = 0; i < nums.length; i++) {
            sum += nums[i];
        }
        return totalsum - sum;
    }
}
