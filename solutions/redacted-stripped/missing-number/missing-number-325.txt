class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int sum = IntStream.of(nums).sum();
        int tsum = (n * (n + 1)) / 2;
        return tsum - sum;
    }
}
