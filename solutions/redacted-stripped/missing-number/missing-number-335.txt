class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int dif;
        int sum = (n * (n + 1)) / 2;
        int sum2 = 0;
        for (int i = 0; i < n; i++) {
            sum2 = sum2 + nums[i];
        }
        dif = sum - sum2;
        return dif;
    }
}
