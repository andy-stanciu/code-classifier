class Solution {

    public int method1(int[] nums) {
        Arrays.sort(nums);
        for (int i = 0; i <= nums.length; i++) {
            if (nums.length == i) {
                return i;
            }
            if (nums[i] != i) {
                return i;
            }
        }
        return -1;
    }
}
