class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int sum = 0;
        for (int it : nums) {
            sum += it;
        }
        return ((n * (n + 1)) / 2) - sum;
    }
}
