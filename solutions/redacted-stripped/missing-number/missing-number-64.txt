class Solution {

    public int method1(int[] nums) {
        Arrays.sort(nums);
        int startIndex = 0;
        while (startIndex < nums.length) {
            if (nums[startIndex] == startIndex)
                startIndex++;
            else
                return startIndex;
        }
        return startIndex;
    }
}
