class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int sum = Arrays.stream(nums).sum();
        int sumN = (n * (n + 1)) / 2;
        return sumN - sum;
    }
}
