class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int num = (n * (n + 1)) / 2;
        for (int i = 0; i < nums.length; i++) {
            num = num - nums[i];
        }
        return (num);
    }
}
