class Solution {

    public int method1(int[] nums) {
        int len = nums.length;
        int count = len * (len + 1) / 2;
        int sum = 0;
        for (int i = 0; i < nums.length; i++) {
            sum += nums[i];
        }
        return count - sum;
    }
}
