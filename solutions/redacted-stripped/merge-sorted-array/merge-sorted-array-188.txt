class Solution {

    public void method1(int[] nums1, int m, int[] nums2, int n) {
        for (int i = n - 1, j = m + n - 1; i >= 0; --i, --j) {
            nums1[j] = nums2[i];
        }
        Arrays.sort(nums1);
    }
}
