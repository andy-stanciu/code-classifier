class Solution {

    public void method1(int[] nums1, int m, int[] nums2, int n) {
        int last = nums1.length - 1;
        int j = 0;
        for (int i = last; i > m - 1; i--) {
            nums1[i] = nums2[j];
            j++;
        }
        Arrays.sort(nums1);
    }
}
