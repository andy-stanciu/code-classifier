class Solution {

    public void method1(int[] nums1, int m, int[] nums2, int n) {
        int i = n - 1;
        int j = m + n - 1;
        while (i >= 0) {
            nums1[j] = nums2[i];
            i--;
            j--;
        }
        Arrays.sort(nums1);
    }
}
