class Solution {

    public boolean check(TreeNode leftNode, TreeNode rightNode) {
        if (leftNode == null && rightNode == null) {
            return true;
        }
        if ((leftNode == null || rightNode == null)) {
            return false;
        }
        if (leftNode.val == rightNode.val) {
            boolean lc = check(leftNode.left, rightNode.right);
            boolean rc = check(leftNode.right, rightNode.left);
            if (lc == true && rc == true) {
                return true;
            }
        }
        return false;
    }

    public boolean method1(TreeNode root) {
        if (root == null) {
            return true;
        }
        if (check(root.left, root.right) == true) {
            return true;
        }
        return false;
    }
}
