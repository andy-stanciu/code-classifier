class Solution {

    public int method1(int[] nums, int target) {
        int var1 = Integer.MAX_VALUE, var2 = Integer.MAX_VALUE;
        Arrays.sort(nums);
        for (int i = 0; i < nums.length - 2; i++) {
            int j = i + 1;
            int k = nums.length - 1;
            while (j < k) {
                int sum = nums[i] + nums[j] + nums[k];
                if (sum == target) {
                    return target;
                } else if (sum < target) {
                    j++;
                } else {
                    k--;
                }
                int close = Math.abs(sum - target);
                if (close < var1) {
                    var1 = close;
                    var2 = sum;
                }
            }
        }
        return var2;
    }
}
