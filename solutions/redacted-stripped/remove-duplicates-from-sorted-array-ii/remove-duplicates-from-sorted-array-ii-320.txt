class Solution {

    public int method1(int[] nums) {
        int n = nums.length;
        int idx = 2;
        for (int i = 2; i < n; i++) {
            if (nums[i] != nums[idx - 2]) {
                nums[idx++] = nums[i];
            }
        }
        return idx;
    }
}
