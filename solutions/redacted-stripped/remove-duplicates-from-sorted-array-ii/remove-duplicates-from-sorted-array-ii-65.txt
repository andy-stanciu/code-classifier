class Solution {

    public int method1(int[] nums) {
        int count = 0;
        for (final int num : nums) {
            if (count < 2 || num > nums[count - 2]) {
                nums[count++] = num;
            }
        }
        return count;
    }
}
