class Solution {

    public int method1(int[] nums) {
        int length = 2;
        for (int i = 2; i < nums.length; i++) {
            if (nums[i] != nums[length - 2]) {
                nums[length] = nums[i];
                length++;
            }
        }
        return length;
    }
}
