class Solution {

    public int method1(int[] nums) {
        int index = 1, cnt = 0;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] != nums[i - 1]) {
                nums[index++] = nums[i];
                cnt = 0;
            } else if (nums[i] == nums[i - 1] && cnt < 1) {
                nums[index++] = nums[i];
                cnt++;
            }
        }
        return index;
    }
}
