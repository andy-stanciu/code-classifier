class Solution {

    public int method1(int[] nums) {
        short k = 0;
        for (short i = 2; i < nums.length; i++) {
            if (nums[k] != nums[i])
                nums[++k + 1] = nums[i];
        }
        return Math.min(k + 2, nums.length);
    }
}
