class Solution {

    public ListNode method1(ListNode head, int k) {
        if (head == null || k == 0 || k == 1)
            return head;
        ListNode curr = head, fwd = null, prev = null, temp = head;
        int count = 0, tally = 0;
        while (temp != null) {
            temp = temp.next;
            tally++;
        }
        if (tally < k)
            return head;
        while (curr != null && count < k) {
            fwd = curr.next;
            curr.next = prev;
            prev = curr;
            curr = fwd;
            count++;
        }
        if (fwd != null)
            head.next = method1(fwd, k);
        return prev;
    }
}
