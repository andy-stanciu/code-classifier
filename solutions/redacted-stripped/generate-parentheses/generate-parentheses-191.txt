class Solution {

    private void method1(int n, String s, int open, int close, List<String> res) {
        if (open == n && close == n) {
            res.add(s);
            return;
        }
        if (open < n)
            method1(n, s + "(", open + 1, close, res);
        if (close < open)
            method1(n, s + ")", open, close + 1, res);
    }

    public List<String> method2(int n) {
        List<String> res = new ArrayList<>();
        method1(n, "", 0, 0, res);
        return res;
    }
}
