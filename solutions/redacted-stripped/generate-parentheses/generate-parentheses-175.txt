class Solution {

    public List<String> method1(int n) {
        List<String> result = new ArrayList<>();
        method2(result, "", 0, 0, n);
        return result;
    }

    private static void method2(List<String> result, String current, int open, int close, int max) {
        if (current.length() == max * 2) {
            result.add(current);
            return;
        }
        if (open < max) {
            method2(result, current + "(", open + 1, close, max);
        }
        if (close < open) {
            method2(result, current + ")", open, close + 1, max);
        }
    }
}
