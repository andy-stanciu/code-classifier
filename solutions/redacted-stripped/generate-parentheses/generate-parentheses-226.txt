class Solution {

    public List<String> method1(int n) {
        List<String> res = new ArrayList<>();
        method2(n, n, "", res);
        return res;
    }

    private void method2(int open, int close, String curr, List<String> res) {
        if (open == 0 && close == 0) {
            res.add(curr);
            return;
        }
        if (open > 0) {
            method2(open - 1, close, curr + "(", res);
        }
        if (close > open) {
            method2(open, close - 1, curr + ")", res);
        }
    }
}
