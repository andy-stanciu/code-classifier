class Solution {

    void method1(int n, int open, int closed, String curr, List<String> res) {
        if (closed >= n) {
            res.add(curr);
            return;
        }
        if (open < n) {
            method1(n, open + 1, closed, curr + "(", res);
        }
        if (open > closed) {
            method1(n, open, closed + 1, curr + ")", res);
        }
    }

    public List<String> method2(int n) {
        List<String> res = new ArrayList<>();
        method1(n, 0, 0, "", res);
        return res;
    }
}
