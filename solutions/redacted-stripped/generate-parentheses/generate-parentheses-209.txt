class Solution {

    List<String> ans;

    public void method1(int open, int close, int n, String s) {
        if (s.length() == 2 * n) {
            ans.add(s);
            return;
        }
        if (open < n)
            method1(open + 1, close, n, s + "(");
        if (close < open)
            method1(open, close + 1, n, s + ")");
    }

    public List<String> method2(int n) {
        ans = new ArrayList<>();
        method1(0, 0, n, "");
        return ans;
    }
}
