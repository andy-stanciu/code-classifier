class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> stack = new Stack<>();
        int n = param1.length;
        int mod = 0;
        for (int i = 0; i < n; i++) {
            if (stack.isEmpty() || param1[i] > 0) {
                stack.push(param1[i]);
            } else {
                while (!stack.isEmpty()) {
                    int top = stack.peek();
                    if (top < 0) {
                        stack.push(param1[i]);
                        break;
                    }
                    mod = Math.abs(param1[i]);
                    {
                        if (mod == top) {
                            stack.pop();
                            break;
                        } else if (mod < top) {
                            break;
                        } else {
                            stack.pop();
                            if (stack.isEmpty()) {
                                stack.push(param1[i]);
                                break;
                            }
                        }
                    }
                }
            }
        }
        int[] ans = new int[stack.size()];
        for (int i = stack.size() - 1; i >= 0; i--) {
            ans[i] = stack.pop();
        }
        return ans;
    }
}
