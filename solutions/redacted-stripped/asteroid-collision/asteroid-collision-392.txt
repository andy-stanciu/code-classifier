class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> stack = new Stack<>();
        int i = 0;
        while (i < param1.length) {
            if (stack.empty() || param1[i] > 0) {
                stack.push(param1[i]);
                i++;
            } else if (param1[i] < 0) {
                if (stack.peek() < 0) {
                    stack.push(param1[i]);
                    i++;
                } else if (stack.peek() > Math.abs(param1[i])) {
                    i++;
                } else {
                    if (stack.peek() == Math.abs(param1[i]))
                        i++;
                    stack.pop();
                }
            }
        }
        int[] arr = new int[stack.size()];
        for (int j = stack.size() - 1; j >= 0; j--) {
            arr[j] = stack.pop();
        }
        return arr;
    }
}
