class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> stack = new Stack<>();
        int StringLength = param1.length;
        for (int i = 0; i < StringLength; i++) {
            if (stack.isEmpty() || param1[i] > 0) {
                stack.push(param1[i]);
            } else {
                while (!stack.isEmpty()) {
                    if (stack.peek() < 0) {
                        stack.push(param1[i]);
                        break;
                    }
                    int absValue = Math.abs(param1[i]);
                    if (absValue > stack.peek()) {
                        stack.pop();
                        if (stack.isEmpty()) {
                            stack.push(param1[i]);
                            break;
                        }
                    } else if (absValue == stack.peek()) {
                        stack.pop();
                        break;
                    } else {
                        break;
                    }
                }
            }
        }
        int len = stack.size();
        int[] result = new int[len];
        for (int i = len - 1; i >= 0; i--) {
            result[i] = stack.pop();
        }
        return result;
    }
}
