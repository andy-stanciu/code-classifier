class Solution {

    public int[] method1(int[] param1) {
        Stack<Integer> st = new Stack<>();
        int i = 0;
        while (i < param1.length) {
            if (st.size() > 0 && (st.peek() > 0 && param1[i] < 0)) {
                if (Math.abs(st.peek()) < Math.abs(param1[i])) {
                    st.pop();
                    continue;
                } else if (Math.abs(st.peek()) == Math.abs(param1[i])) {
                    st.pop();
                    i++;
                    continue;
                } else {
                    i++;
                    continue;
                }
            }
            st.push(param1[i]);
            i++;
        }
        int[] result = new int[st.size()];
        i = st.size() - 1;
        while (st.size() > 0) {
            result[i] = st.pop();
            i--;
        }
        return result;
    }
}
