class Solution {

    public int method1(int[][] points) {
        Arrays.sort(points, (p1, p2) -> Long.compare(p1[1], p2[1]));
        int res = 1;
        long var1 = points[0][1];
        for (int index = 1; index < points.length; index++) {
            if (points[index][0] > var1) {
                res++;
                var1 = points[index][1];
            }
        }
        return res;
    }
}
