class Solution {

    List<List<Integer>> listFinal = new ArrayList<List<Integer>>();

    int n;

    public List<List<Integer>> method1(int[] candidates, int target) {
        n = candidates.length;
        Arrays.sort(candidates);
        solve(candidates, target, 0, new ArrayList(), 0);
        return listFinal;
    }

    void solve(int[] candidates, int target, int idx, List<Integer> list, int param1) {
        if (idx > n || param1 > target) {
            return;
        }
        if (param1 == target) {
            listFinal.add(new ArrayList(list));
            return;
        }
        for (int i = idx; i < n && (param1 + candidates[i]) <= target; i++) {
            if (i > idx && candidates[i] == candidates[i - 1]) {
                continue;
            }
            list.add(candidates[i]);
            solve(candidates, target, i + 1, list, param1 + candidates[i]);
            list.remove(list.size() - 1);
        }
    }
}
