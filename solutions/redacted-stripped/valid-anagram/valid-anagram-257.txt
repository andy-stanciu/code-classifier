class Solution {

    public boolean method1(String s, String t) {
        char[] ch = s.toCharArray();
        char[] ar = t.toCharArray();
        Arrays.sort(ch);
        Arrays.sort(ar);
        return Arrays.equals(ch, ar);
    }
}
