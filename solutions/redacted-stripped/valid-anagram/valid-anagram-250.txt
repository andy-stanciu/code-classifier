class Solution {

    public boolean method1(String s, String t) {
        char[] ar = s.toCharArray();
        Arrays.sort(ar);
        char[] br = t.toCharArray();
        Arrays.sort(br);
        String i = new String(ar);
        String j = new String(br);
        return i.equals(j);
    }
}
