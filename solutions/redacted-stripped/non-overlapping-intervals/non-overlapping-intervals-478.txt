class Solution {

    public int method1(int[][] param1) {
        Arrays.sort(param1, (a, b) -> Integer.compare(a[1], b[1]));
        int prev = param1[0][1], count = 0;
        for (int i = 1; i < param1.length; i++) {
            if (param1[i][0] < prev) {
                count++;
            } else {
                prev = param1[i][1];
            }
        }
        return count;
    }
}
