class Solution {

    public int method1(int[][] param1) {
        Arrays.sort(param1, (interval1, interval2) -> interval1[0] - interval2[0]);
        int res = 0;
        int prevEnd = param1[0][1];
        for (int i = 1; i < param1.length; i++) {
            if (param1[i][0] >= prevEnd) {
                prevEnd = param1[i][1];
            } else {
                res++;
                prevEnd = Math.min(prevEnd, param1[i][1]);
            }
        }
        return res;
    }
}
