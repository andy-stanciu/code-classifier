class Solution {

    public int method1(int[][] param1) {
        Arrays.sort(param1, (a, b) -> a[0] - b[0]);
        int end = param1[0][1], res = 0;
        for (int i = 1; i < param1.length; i++) {
            if (param1[i][0] < end) {
                end = Math.min(end, param1[i][1]);
                res++;
            } else
                end = param1[i][1];
        }
        return res;
    }
}
