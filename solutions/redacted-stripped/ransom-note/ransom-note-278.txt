class Solution {

    public boolean canConstruct(String param1, String magazine) {
        if (param1.length() > magazine.length()) {
            return false;
        }
        HashMap<Character, Integer> charCount = new HashMap<>();
        for (char m : magazine.toCharArray()) {
            charCount.put(m, charCount.getOrDefault(m, 0) + 1);
        }
        for (char m : param1.toCharArray()) {
            int count = charCount.getOrDefault(m, 0);
            if (count == 0) {
                return false;
            }
            charCount.put(m, charCount.getOrDefault(m, 0) - 1);
        }
        return true;
    }
}
