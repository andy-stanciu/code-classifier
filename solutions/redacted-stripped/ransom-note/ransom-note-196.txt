class Solution {

    public boolean canConstruct(String param1, String magazine) {
        Map<Character, Integer> k = new HashMap<>();
        boolean returnb = true;
        for (int i = 0; i < magazine.length(); i++) {
            char g = magazine.charAt(i);
            if (!k.containsKey(g)) {
                k.put(g, 1);
            } else {
                int count = k.get(g);
                k.put(g, count + 1);
            }
        }
        System.out.println(k);
        for (int j = 0; j < param1.length(); j++) {
            if (k.containsKey(param1.charAt(j)) && k.get(param1.charAt(j)) > 0) {
                int count = k.get(param1.charAt(j));
                k.put(param1.charAt(j), count - 1);
            } else {
                returnb = false;
            }
        }
        return returnb;
    }
}
