class Solution {

    public boolean canConstruct(String param1, String magazine) {
        if (param1.length() > magazine.length()) {
            return false;
        }
        StringBuilder magazineBuilder = new StringBuilder(magazine);
        boolean result = true;
        int index = 0;
        for (int i = 0; i < param1.length(); i++) {
            index = magazineBuilder.indexOf(String.valueOf(param1.charAt(i)));
            if (index >= 0) {
                magazineBuilder.deleteCharAt(index);
            } else {
                result = false;
                break;
            }
        }
        return result;
    }
}
