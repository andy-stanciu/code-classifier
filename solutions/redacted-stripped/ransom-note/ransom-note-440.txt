class Solution {

    public boolean canConstruct(String param1, String magazine) {
        for (int i = 0; i < param1.length(); i++) {
            int idx = magazine.indexOf(param1.charAt(i));
            if (idx >= 0) {
                magazine = magazine.substring(0, idx) + magazine.substring(idx + 1);
            } else {
                return false;
            }
        }
        return true;
    }
}
