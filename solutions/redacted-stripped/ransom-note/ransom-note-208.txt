class Solution {

    public boolean canConstruct(String param1, String magazine) {
        HashMap<Character, Integer> maps = new HashMap<>();
        for (int i = 0; i < magazine.length(); i++) {
            maps.put(magazine.charAt(i), maps.getOrDefault(magazine.charAt(i), 0) + 1);
        }
        for (int i = 0; i < param1.length(); i++) {
            if (maps.containsKey(param1.charAt(i)) && maps.get(param1.charAt(i)) > 0)
                maps.put(param1.charAt(i), maps.get(param1.charAt(i)) - 1);
            else {
                System.out.println(param1.charAt(i) + "" + i);
                return false;
            }
        }
        return true;
    }
}
