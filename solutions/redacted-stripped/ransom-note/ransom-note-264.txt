class Solution {

    public boolean canConstruct(String param1, String magazine) {
        Map<Character, Integer> magazineLetters = new HashMap<>();
        fillLetterMap(magazineLetters, magazine);
        return method1(magazineLetters, param1);
    }

    private void populateLetterMap(Map<Character, Integer> map, String str) {
        for (char c : str.toCharArray()) {
            if (map.containsKey(c)) {
                map.put(c, map.get(c) + 1);
            } else {
                map.put(c, 1);
            }
        }
    }

    private boolean method1(Map<Character, Integer> map, String param1) {
        for (char c : param1.toCharArray()) {
            if (map.containsKey(c) && map.get(c) > 0) {
                map.put(c, map.get(c) - 1);
            } else {
                return false;
            }
        }
        return true;
    }
}
