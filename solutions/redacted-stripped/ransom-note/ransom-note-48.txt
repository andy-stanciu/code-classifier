class Solution {

    public boolean canConstruct(String param1, String magazine) {
        int size = magazine.length();
        int ranSize = param1.length();
        int count = 1;
        HashMap<Character, Integer> magaMap = new HashMap<>();
        for (int i = 0; i < size; i++) {
            if (magaMap.containsKey(magazine.charAt(i))) {
                count = magaMap.get(magazine.charAt(i));
                magaMap.put(magazine.charAt(i), ++count);
                count = 1;
            } else {
                magaMap.put(magazine.charAt(i), count);
            }
        }
        for (int i = 0; i < ranSize; i++) {
            if (magaMap.containsKey(param1.charAt(i))) {
                if (magaMap.get(param1.charAt(i)) == 0) {
                    return false;
                } else {
                    count = magaMap.get(param1.charAt(i));
                    magaMap.put(param1.charAt(i), --count);
                }
            } else {
                return false;
            }
        }
        return true;
    }
}
