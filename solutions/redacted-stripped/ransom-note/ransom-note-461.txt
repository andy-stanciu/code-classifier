class Solution {

    public boolean canConstruct(String param1, String magazine) {
        if (param1.length() > magazine.length())
            return false;
        var a = param1.toCharArray();
        var b = magazine.toCharArray();
        Arrays.sort(a);
        Arrays.sort(b);
        var flag = false;
        for (int i = 0, j = 0; i < a.length; ) {
            if (j == b.length)
                return false;
            else if (a[i] == b[j]) {
                flag = true;
                i++;
            } else
                flag = false;
            j++;
        }
        return flag;
    }
}
