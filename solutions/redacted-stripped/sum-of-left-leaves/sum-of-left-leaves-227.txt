class Solution {

    public void traverse(TreeNode root, int[] param1, boolean param2) {
        if (root == null)
            return;
        if (root.left == null && root.right == null && param2) {
            param1[0] += root.val;
        }
        traverse(root.left, param1, true);
        traverse(root.right, param1, false);
    }

    public int method1(TreeNode root) {
        if (root.left == null && root.right == null)
            return 0;
        int[] param1 = new int[1];
        traverse(root, param1, false);
        return param1[0];
    }
}
