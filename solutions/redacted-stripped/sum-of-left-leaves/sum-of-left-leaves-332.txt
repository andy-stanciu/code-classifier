class Solution {

    public int traversal(TreeNode curNode, int param1, boolean param2) {
        if (curNode == null) {
            return param1;
        }
        if (curNode.left == null && curNode.right == null && param2) {
            param1 += curNode.val;
        }
        param1 = traversal(curNode.left, param1, true);
        param1 = traversal(curNode.right, param1, false);
        return param1;
    }

    public int method1(TreeNode root) {
        int param1 = 0;
        return traversal(root, param1, false);
    }
}
