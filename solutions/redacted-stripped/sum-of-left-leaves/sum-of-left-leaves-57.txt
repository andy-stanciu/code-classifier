class Solution {

    public int method1(TreeNode r) {
        if (r == null)
            return 0;
        int var1 = 0;
        if (r.left != null && r.left.left == null && r.left.right == null)
            var1 += r.left.val;
        return var1 + method1(r.left) + method1(r.right);
    }
}
