class Solution {

    public int maxProfit(int[] prices) {
        if (prices.length == 0 || prices.length == 1)
            return 0;
        int var1 = Integer.MAX_VALUE;
        int sum = 0;
        for (int i = 0; i < prices.length; i++) {
            var1 = Math.min(var1, prices[i]);
            if (i < prices.length - 1 && prices[i] > prices[i + 1]) {
                sum += prices[i] - var1;
                var1 = prices[i + 1];
            }
            if (i == prices.length - 1 && prices[i] >= prices[i - 1]) {
                sum += prices[i] - var1;
            }
        }
        return sum;
    }
}
