class Solution {

    public int maxProfit(int[] prices) {
        int i = 0, j = 1, k = 0, res = 0;
        if (prices.length == 0) {
            return 0;
        } else if (prices.length == 1) {
            return 0;
        } else if (prices.length == 2) {
            if (prices[i] < prices[j]) {
                return prices[j] - prices[i];
            } else
                return 0;
        }
        while (j != prices.length) {
            if (prices[i] < prices[j]) {
                k = prices[j] - prices[i];
                res = k + res;
                i = j;
                j++;
            } else if (prices[i] >= prices[j]) {
                i = j;
                j++;
            }
        }
        return res;
    }
}
