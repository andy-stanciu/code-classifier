class Solution {

    public int maxProfit(int[] p) {
        int maxProfit = 0;
        for (int i = 0; i < p.length - 1; i++) {
            if (p[i] < p[i + 1]) {
                maxProfit += p[i + 1] - p[i];
            }
        }
        return maxProfit;
    }
}
