class Solution {

    ListNode staticList;

    public ListNode method1(ListNode[] param1) {
        for (ListNode list : param1) {
            while (list != null) {
                method2(list.val);
                list = list.next;
            }
        }
        return staticList;
    }

    void method2(int val) {
        ListNode newNode = new ListNode(val);
        if (staticList == null || val < staticList.val) {
            newNode.next = staticList;
            staticList = newNode;
            return;
        }
        ListNode current = staticList;
        while (current.next != null && current.next.val < val) {
            current = current.next;
        }
        newNode.next = current.next;
        current.next = newNode;
    }
}
