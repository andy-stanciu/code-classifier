class Solution {

    int ri = 0;

    public int[] findOrder(int param1, int[][] prerequisites) {
        List<List<Integer>> graph = new ArrayList<List<Integer>>();
        int rlen = prerequisites.length;
        int[] result = new int[param1];
        for (int i = 0; i < param1; i++) {
            graph.add(new ArrayList<Integer>());
        }
        for (int i = 0; i < rlen; i++) {
            graph.get(prerequisites[i][0]).add(prerequisites[i][1]);
        }
        HashSet<Integer> visited = new HashSet<Integer>();
        HashSet<Integer> rec = new HashSet<Integer>();
        boolean ans = true;
        for (int i = 0; i < param1; i++) {
            ans = ans & !check(graph, visited, rec, i, result);
        }
        if (ans == true) {
            return result;
        }
        return new int[0];
    }

    public boolean check(List<List<Integer>> graph, HashSet<Integer> visited, HashSet<Integer> rec, int param2, int[] result) {
        if (!visited.contains(param2)) {
            visited.add(param2);
            rec.add(param2);
            for (int adj : graph.get(param2)) {
                if (!visited.contains(adj) && check(graph, visited, rec, adj, result)) {
                    return true;
                } else if (rec.contains(adj)) {
                    return true;
                }
            }
            rec.remove(param2);
            result[ri++] = param2;
        }
        return false;
    }
}
