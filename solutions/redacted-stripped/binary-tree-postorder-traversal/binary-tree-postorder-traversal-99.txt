class Solution {

    List<Integer> list;

    public void method1(TreeNode node) {
        if (node == null)
            return;
        if (node.left != null)
            method1(node.left);
        if (node.right != null)
            method1(node.right);
        list.add(node.val);
    }

    public List<Integer> method2(TreeNode root) {
        list = new ArrayList<>();
        method1(root);
        return list;
    }
}
