class Solution {

    List<Integer> lis = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return lis;
        }
        method1(root.left);
        method1(root.right);
        lis.add(root.val);
        return lis;
    }
}
