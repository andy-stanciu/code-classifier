class Solution {

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return new ArrayList<>();
        }
        List<Integer> answer = new ArrayList<>();
        answer.addAll(method1(root.left));
        answer.addAll(method1(root.right));
        answer.add(root.val);
        return answer;
    }
}
