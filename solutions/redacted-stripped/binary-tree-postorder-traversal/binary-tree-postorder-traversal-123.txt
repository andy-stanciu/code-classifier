class Solution {

    public List<Integer> method1(TreeNode root) {
        ArrayList<Integer> list = new ArrayList<>();
        method2(root, list);
        return list;
    }

    static ArrayList<Integer> method2(TreeNode node, ArrayList<Integer> list) {
        if (node == null) {
            return list;
        }
        method2(node.left, list);
        method2(node.right, list);
        list.add(node.val);
        return list;
    }
}
