class Solution {

    ArrayList<Integer> arr = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return arr;
        }
        method1(root.left);
        method1(root.right);
        arr.add(root.val);
        return arr;
    }
}
