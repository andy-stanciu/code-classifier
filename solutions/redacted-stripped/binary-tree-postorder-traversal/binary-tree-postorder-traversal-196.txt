class Solution {

    public List<Integer> method1(TreeNode root) {
        List<Integer> ans = new ArrayList<>();
        method2(root, ans);
        return ans;
    }

    private void method2(TreeNode root, List<Integer> ans) {
        if (root == null) {
            return;
        }
        method2(root.left, ans);
        method2(root.right, ans);
        ans.add(root.val);
    }
}
