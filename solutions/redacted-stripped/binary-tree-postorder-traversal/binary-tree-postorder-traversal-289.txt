class Solution {

    List<Integer> ans = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return new ArrayList<>();
        }
        method1(root.left);
        method1(root.right);
        ans.add(root.val);
        return ans;
    }
}
