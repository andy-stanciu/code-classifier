class Solution {

    List<Integer> post = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return post;
        }
        method1(root.left);
        method1(root.right);
        post.add(root.val);
        return post;
    }
}
