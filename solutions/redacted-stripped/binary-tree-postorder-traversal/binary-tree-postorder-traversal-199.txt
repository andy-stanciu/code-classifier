class Solution {

    List<Integer> res = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return res;
        method1(root.left);
        method1(root.right);
        res.add(root.val);
        return res;
    }
}
