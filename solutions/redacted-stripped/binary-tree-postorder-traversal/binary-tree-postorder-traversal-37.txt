class Solution {

    List<Integer> list = new ArrayList<Integer>();

    public List<Integer> method1(TreeNode root) {
        if (root != null) {
            method1(root.left);
            method1(root.right);
            list.add(root.val);
        }
        return list;
    }
}
