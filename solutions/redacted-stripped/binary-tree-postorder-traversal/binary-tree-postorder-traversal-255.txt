class Solution {

    public void method1(TreeNode node, List<Integer> list) {
        if (node == null)
            return;
        method1(node.left, list);
        method1(node.right, list);
        list.add(node.val);
    }

    public List<Integer> method2(TreeNode root) {
        List<Integer> ans = new ArrayList();
        method1(root, ans);
        return ans;
    }
}
