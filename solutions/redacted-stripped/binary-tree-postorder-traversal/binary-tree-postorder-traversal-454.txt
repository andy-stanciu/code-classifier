class Solution {

    List<Integer> result = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return result;
        method1(root.left);
        method1(root.right);
        result.add(root.val);
        return result;
    }
}
