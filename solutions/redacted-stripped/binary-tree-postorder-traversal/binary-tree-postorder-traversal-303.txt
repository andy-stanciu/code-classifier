class Solution {

    public List<Integer> method1(TreeNode root) {
        ArrayList<Integer> arr = new ArrayList<>();
        method2(root, arr);
        return arr;
    }

    private void method2(TreeNode root, List<Integer> arr) {
        if (root == null) {
            return;
        }
        method2(root.left, arr);
        method2(root.right, arr);
        arr.add(root.val);
    }
}
