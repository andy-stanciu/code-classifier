class Solution {

    List<Integer> arr = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root != null) {
            method1(root.left);
            method1(root.right);
            arr.add(root.val);
        }
        return arr;
    }
}
