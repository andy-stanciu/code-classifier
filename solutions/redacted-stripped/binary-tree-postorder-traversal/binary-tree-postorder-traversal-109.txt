class Solution {

    List a = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return a;
        } else {
            method1(root.left);
            method1(root.right);
            a.add(root.val);
        }
        return a;
    }
}
