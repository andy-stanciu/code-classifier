class Solution {

    List<Integer> arr = new ArrayList<>();

    public List<Integer> method1(TreeNode node) {
        if (node != null) {
            method1(node.left);
            method1(node.right);
            arr.add(node.val);
        }
        return arr;
    }
}
