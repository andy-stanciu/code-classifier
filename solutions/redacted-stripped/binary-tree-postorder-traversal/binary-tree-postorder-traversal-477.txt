class Solution {

    List<Integer> nodes = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return nodes;
        method1(root.left);
        method1(root.right);
        nodes.add(root.val);
        return nodes;
    }
}
