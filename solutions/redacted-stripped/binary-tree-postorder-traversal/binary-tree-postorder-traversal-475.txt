class Solution {

    List<Integer> alt = new ArrayList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null) {
            return alt;
        }
        method1(root.left);
        method1(root.right);
        alt.add(root.val);
        return alt;
    }
}
