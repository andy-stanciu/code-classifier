class Solution {

    List<Integer> ans = new LinkedList<>();

    public List<Integer> method1(TreeNode root) {
        if (root == null)
            return ans;
        method1(root.left);
        method1(root.right);
        ans.add(root.val);
        return ans;
    }
}
