class Solution {

    public List<String> method1(TreeNode root) {
        List<String> result = new ArrayList<>();
        method2(root, new ArrayList<>(), result);
        return result;
    }

    private void method2(TreeNode root, List<String> curr, List<String> result) {
        if (root == null) {
            return;
        }
        curr.add(String.valueOf(root.val));
        method2(root.left, curr, result);
        method2(root.right, curr, result);
        if (root.left == null && root.right == null)
            result.add(String.join("->", curr));
        curr.removeLast();
    }
}
