class Solution {

    List<String> res = new ArrayList();

    public List<String> method1(TreeNode root) {
        method2(root, "");
        return res;
    }

    private void method2(TreeNode root, String str) {
        if (root == null)
            return;
        if (isLeaf(root))
            res.add(str.concat(String.valueOf(root.val)));
        method2(root.left, str.concat(String.valueOf(root.val) + "->"));
        method2(root.right, str.concat(String.valueOf(root.val) + "->"));
    }

    private boolean isLeaf(TreeNode root) {
        return root.left == null && root.right == null;
    }
}
