class Solution {

    public List<String> method1(TreeNode root) {
        List<String> result = new ArrayList();
        addTo(root, new StringBuilder(), result);
        return result;
    }

    public void addTo(TreeNode node, StringBuilder path, List<String> result) {
        path.append(node.val);
        if (node.left == null && node.right == null) {
            result.add(path.toString());
        } else {
            path.append("->");
            if (node.left != null) {
                addTo(node.left, new StringBuilder(path), result);
            }
            if (node.right != null) {
                addTo(node.right, new StringBuilder(path), result);
            }
        }
    }
}
