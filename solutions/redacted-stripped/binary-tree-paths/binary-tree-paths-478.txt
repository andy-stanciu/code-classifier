class Solution {

    public List<String> method1(TreeNode root) {
        List<String> ls = new ArrayList<>();
        method2(root, "", ls);
        return ls;
    }

    public void method2(TreeNode root, String s, List<String> ls) {
        if (root == null) {
            return;
        }
        s += root.val;
        if (root.right == null && root.left == null) {
            ls.add(s);
        }
        method2(root.left, s + "->", ls);
        method2(root.right, s + "->", ls);
    }
}
