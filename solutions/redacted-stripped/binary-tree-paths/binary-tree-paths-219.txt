class Solution {

    public List<String> method1(TreeNode root) {
        List<String> list = new ArrayList<>();
        StringBuilder sb = new StringBuilder();
        addPath(root, sb, list);
        return list;
    }

    public void addPath(TreeNode root, StringBuilder sb, List<String> list) {
        if (root == null) {
            return;
        }
        int len = sb.length();
        sb.append(root.val);
        if (root.right == null && root.left == null) {
            list.add(sb.toString());
        }
        sb.append("->");
        addPath(root.left, sb, list);
        addPath(root.right, sb, list);
        sb.setLength(len);
    }
}
