public class Solution {

    public bool method1(int x) {
        long rev = 0, xCopy = x;
        while (xCopy > 0) {
            rev = rev * 10 + xCopy % 10;
            xCopy /= 10;
        }
        return (rev == x && xCopy > -1);
    }
}
