class Solution {

    public boolean method1(int x) {
        String t = String.valueOf(x);
        int n = t.length();
        for (int i = 0; i < n / 2; i++) {
            if (t.charAt(i) != t.charAt(n - i - 1))
                return false;
        }
        return true;
    }
}
