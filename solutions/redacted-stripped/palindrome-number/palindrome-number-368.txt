class Solution {

    public boolean method1(int x) {
        StringBuffer sb = new StringBuffer(Integer.toString(x));
        sb.reverse();
        if (sb.toString().equals(Integer.toString(x))) {
            return true;
        } else
            return false;
    }
}
