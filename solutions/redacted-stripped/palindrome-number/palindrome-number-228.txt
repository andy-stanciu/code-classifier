class Solution {

    public boolean method1(int x) {
        if (x < 0)
            return false;
        String num = String.valueOf(x);
        String invert = new StringBuilder(num).reverse().toString();
        return num.equals(invert);
    }
}
