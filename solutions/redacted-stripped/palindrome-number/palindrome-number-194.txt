class Solution {

    public boolean method1(int x) {
        return x == rev(Math.abs(x));
    }

    int sum = 0;

    int rev(int n) {
        if (n == 0) {
            return 0;
        }
        int rem = n % 10;
        sum = sum * 10 + rem;
        rev(n / 10);
        return sum;
    }
}
