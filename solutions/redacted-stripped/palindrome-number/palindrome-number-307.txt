class Solution {

    public boolean method1(int x) {
        int b = x;
        int f = 0;
        if (x < 0) {
            return false;
        }
        while (b != 0) {
            f = f * 10 + b % 10;
            b /= 10;
        }
        return x == f;
    }
}
