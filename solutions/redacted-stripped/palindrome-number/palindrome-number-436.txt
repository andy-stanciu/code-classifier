class Solution {

    public boolean method1(int x) {
        String str = x + "";
        for (int i = 0, j = str.length() - 1; i < j; i++) {
            if (str.charAt(i) == str.charAt(j)) {
                j--;
            } else {
                return false;
            }
        }
        return true;
    }
}
