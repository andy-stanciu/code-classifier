class Solution {

    public boolean method1(int x) {
        int rev = 0, temp = x;
        while (temp > 0) {
            rev = (rev * 10) + (temp % 10);
            temp = temp / 10;
        }
        return x == rev ? true : false;
    }
}
