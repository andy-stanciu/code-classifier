class Solution {

    public boolean method1(int x) {
        int temp = x;
        int c = 0;
        while (x > 0) {
            int r = x % 10;
            c = c * 10 + r;
            x = x / 10;
        }
        if (temp == c)
            return true;
        return false;
    }
}
