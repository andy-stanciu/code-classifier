class Solution {

    public int mySqrt(int x) {
        double y;
        if (x > 0) {
            y = Math.pow(x, 0.5);
        } else {
            return 0;
        }
        return (int) y;
    }
}
