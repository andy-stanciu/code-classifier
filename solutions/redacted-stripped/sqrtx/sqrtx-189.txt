class Solution {

    public int mySqrt(int x) {
        double value = Math.sqrt(x);
        int val = (int) value;
        if (val < 0) {
            return val * -1;
        }
        return val;
    }
}
