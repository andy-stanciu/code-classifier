class Solution {

    public int mySqrt(int x) {
        double result = x;
        for (int i = 0; i < 20; i++) {
            result = (result + (x / result)) / 2;
        }
        return (int) result;
    }
}
