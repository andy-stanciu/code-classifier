class Solution {

    public int mySqrt(int x) {
        long multi = 0;
        long i = 0;
        while (x >= multi) {
            multi = i * i;
            if (x == multi) {
                return (int) i;
            }
            i++;
        }
        return (int) i - 2;
    }
}
