class Solution {

    public int mySqrt(int x) {
        if (x < 0) {
            return -1;
        }
        if (x == 0 || x == 1) {
            return x;
        }
        double a = x;
        double b = 0.5;
        double y = Math.pow(a, b);
        return (int) Math.floor(y);
    }
}
