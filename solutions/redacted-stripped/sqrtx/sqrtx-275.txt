class Solution {

    public int mySqrt(int n) {
        double guess = n;
        double precision = 0.000001;
        while ((guess - (n / guess)) > precision) {
            guess = (guess + (n / guess)) / 2;
        }
        return (int) guess;
    }
}
